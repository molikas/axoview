{
  "name": "axoview-worker",
  "version": "3.7.0",
  "description": "Cloudflare Worker (Hono) backend for Axoview Pages deploy",
  "private": true,
  "type": "module",
  "main": "src/app.ts",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler pages dev ../axoview-app/build --compatibility-date=2025-01-01",
    "dev:mcp": "wrangler dev --config wrangler.dev.toml",
    "test": "npx --no-install jest"
  },
  "dependencies": {
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260426.1",
    "wrangler": "^4.103.0"
  }
}
