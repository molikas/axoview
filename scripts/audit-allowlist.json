{
  "_comment": [
    "Accepted-risk allowlist for scripts/check-audit.js.",
    "",
    "CI fails on any advisory NOT listed here, so adding an entry is a deliberate,",
    "reviewable act — not a silent suppression. Each entry must say why the",
    "advisory cannot bite THIS codebase, verified against actual usage rather than",
    "the advisory text, and what would make it bite (the tripwire).",
    "",
    "`reviewBy` does not fail the build; it prints a loud warning once passed, so",
    "an acceptance cannot quietly become permanent. Established by the 2026-07-29",
    "review — see docs/reviews/technical-review-2026-07-29.md §3 finding 4.",
    "",
    "2026-08-09 review pass (exploratory-program close-out /audit): 12 advisories",
    "had accumulated unreviewed since the last pass and the gate had been red on",
    "master for some time. Each was verified against real usage — imports checked,",
    "not advisory text read — and none is exploitable here. The stale react-router",
    "entry (GHSA-qwww-vcr4-c8h2) was removed: the advisory no longer matches, so",
    "keeping it would have made the list lie about what it is protecting.",
    "",
    "2026-09-24 pass (blanket `npm audit fix`, shipped with the self-host fix): the",
    "10 advisories published since the last pass all had non-breaking fixes, so",
    "they were upgraded rather than allowlisted. The same upgrade fixed 11 entries",
    "below (dompurify, hono x4, js-yaml, undici x5); they were removed for the same",
    "reason as the react-router entry above.",
    "",
    "`scope` names the tree an entry was reviewed against: \"root\" (the workspace",
    "lockfile) or \"backend\" (packages/axoview-backend/package-lock.json, prod deps",
    "only: the tree the Docker image installs). Use [\"root\", \"backend\"] when the",
    "advisory is in both. An entry without `scope` covers \"root\" only, so a",
    "dev-only acceptance can never silently cover a package the image ships."
  ],
  "allow": [
    {
      "id": "GHSA-v3m3-f69x-jf25",
      "package": "quill",
      "title": "Quill XSS via HTML export feature",
      "shipped": true,
      "why": "Axoview DOES call the affected getSemanticHTML(), but every call site pipes the result through sanitizeHtml() before it is stored (TextBoxInlineEditor.tsx:115,134), the model is re-sanitized on load (useInitialDataManager.ts:85), and the sole dangerouslySetInnerHTML sink re-sanitizes at render (TextBox.tsx:254,336). ADR 0029's defence-in-depth absorbs the payload.",
      "noForwardFix": "npm's only remedy is a BREAKING downgrade to react-quill-new@3.7.0.",
      "tripwire": "If the ADR 0029 sanitize is removed from ANY of those three points, this becomes live stored-XSS. renderedGeometry-style contract coverage on sanitizeHtml call sites would make that unmissable.",
      "acceptedOn": "2026-07-29",
      "reviewBy": "2026-10-29"
    },
    {
      "id": "GHSA-3jxr-9vmj-r5cp",
      "package": "brace-expansion",
      "title": "brace-expansion DoS via exponential-time expansion",
      "shipped": false,
      "why": "devDependency only, reached via nodemon. Never bundled, never served, never processes user input.",
      "tripwire": "brace-expansion appearing under a runtime dependency.",
      "acceptedOn": "2026-07-29",
      "reviewBy": "2026-10-29"
    },
    {
      "id": "GHSA-mh99-v99m-4gvg",
      "package": "brace-expansion",
      "title": "brace-expansion DoS via unbounded expansion length (OOM)",
      "shipped": false,
      "why": "devDependency only, reached via nodemon. Never bundled, never served, never processes user input.",
      "tripwire": "brace-expansion appearing under a runtime dependency.",
      "acceptedOn": "2026-07-29",
      "reviewBy": "2026-10-29"
    },
    {
      "id": "GHSA-rgw5-rvv9-x895",
      "package": "brace-expansion",
      "title": "brace-expansion: DoS via unbounded intermediate arrays, bypassing the CVE-2026-14257 mitigation",
      "shipped": false,
      "why": "Test/build tooling only. `npm ls` puts it under jest → @jest/reporters → glob (axoview-app) and under the backend's dev tree; no workspace lists it as a runtime dependency (worker deps are hono alone; backend deps are cors/dotenv/express/helmet). The patterns it expands are our own glob literals in configs, never user input.",
      "tripwire": "brace-expansion appearing under a runtime dependency, or any code path expanding a user-supplied brace pattern.",
      "acceptedOn": "2026-08-09",
      "reviewBy": "2026-11-09"
    }
  ]
}
