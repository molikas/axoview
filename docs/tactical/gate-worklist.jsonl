{"row":47,"adr":"0003","section":"Decision — the strip rule + \"applies to every write path\"","class":"A2a","claim":"ADR 0003 says the strip rule is `keep icon ⟺ icon.id ∉ bundledFixtures.byId ∨ icon differs from the fixture in any user-visible field`. No write path in the code implements that rule. Every write path keeps icons iff `icon.collection === 'imported'` — a different rule with the opposite outcome for pack icons.","evidence":"packages/axoview-app/src/services/storage/leanModel.ts `leanIfModel`: `icons: modelIcons.filter((icon) => icon.collection === 'imported')`. Call sites: LocalStorageProvider.ts `sessionSaveDiagram` (`const lean = leanIfModel(data)`) and `serverSaveDiagram` (`body: JSON.stringify(leanIfModel(data))`); GoogleDriveProvider.ts `uploadMedia`/`uploadCreate`. Same rule re-inlined at DiagramLifecycleProvider.tsx `buildSaveData` and `executeSave` (`.filter((icon) => icon.collection === 'imported')`). …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":48,"adr":"0003","section":"Context — \"The bundled catalog already exists in code at packages/axoview-lib/src/fixtures/icons.ts. Persisting it is pu…","class":"A2a","claim":"ADR 0003's entire Context premise is dead: there is no bundled catalog. `fixtures/icons.ts` exports an empty array, so `stripDefaultIcons` — the helper this ADR mandates — is a structural no-op that can never strip anything. ADR 0002 records this fact explicitly; ADR 0003 never does, despite carrying an addendum dated one day AFTER 0002's note.","evidence":"packages/axoview-lib/src/fixtures/icons.ts is, in full: `import { Model } from 'src/types'; export const icons: Model['icons'] = [];`. Therefore packages/axoview-lib/src/utils/leanSave.ts `getBundledFixturesById` returns an empty Map and `stripDefaultIcons`'s filter hits `if (!fixture) return true` (keep) for every icon. Corroborated on three independent surfaces: (a) docs/adr/0002-icon-catalog-merge-on-load.md Decision — \"**As of 2026-05-01 this array is empty by design** — the real catalog is …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":49,"adr":"0003","section":"Implementation notes — \"so server, session, and export call sites share one implementation\"","class":"A2a","claim":"ADR 0003 asserts that leanSave.ts is a single shared strip implementation for server, session and export. There are four independent implementations, and neither the server nor the session path uses leanSave.ts at all.","evidence":"(1) packages/axoview-lib/src/utils/leanSave.ts `stripDefaultIcons` — fixture-diff rule; only caller is exportOptions.ts `exportAsJSON`. (2) packages/axoview-app/src/services/storage/leanModel.ts `leanIfModel` — used by LocalStorageProvider `sessionSaveDiagram`/`serverSaveDiagram` and GoogleDriveProvider `uploadMedia`/`uploadCreate`; its own header comment claims it is the shared one (\"Shared by every StorageProvider that persists a model\"). (3) …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":50,"adr":"0005","section":"§1 — Amendment 2026-05-19 (LEFT-zone brand mark)","class":"A2a","claim":"The 2026-05-19 amendment says the LEFT-zone brand mark is an \"18px favicon SVG + muted `Axoview` wordmark (body2, `text.secondary`)\" and explicitly \"Non-interactive.\" All four specifics are false in code: it is a 24px PNG, a bold 16px wordmark in hardcoded brand colours, and it is an `<a href>` link to the landing root. ADR 0040 R1 made it a home link and never mentions ADR 0005; ADR 0005 was never extended.","evidence":"packages/axoview-app/src/components/AppToolbar.tsx — `AppToolbar`, the `className=\"toolbar-left\"` Box: `component=\"a\"`, `href={`${(process.env.PUBLIC_URL || '').replace(/\\/$/, '')}/`}`, `aria-label=\"Axoview — home\"`, `cursor: 'pointer'`, `'&:hover': { opacity: 0.82 }` → interactive, not \"Non-interactive\". Its `component=\"img\"` child is `src=favicon-96x96.png` at `sx={{ width: 24, height: 24 }}` → 24px PNG, not an 18px SVG. Its `<Typography component=\"span\">` is `fontSize: 16, fontWeight: 600, …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":51,"adr":"0005","section":"§1 → Group 3 (Document actions) Visibility cell","class":"A2a","claim":"ADR 0005's Group 3 Visibility cell says \"Always rendered; Share/Preview disabled in session mode.\" The Share half is true; the Preview half is false — Preview is gated only on whether a diagram exists, not on storage mode, so in session mode with a saved diagram Preview is ENABLED and works.","evidence":"packages/axoview-app/src/components/AppToolbar.tsx — `AppToolbar`: the `data-axoview-id=\"toolbar-preview\"` IconButton is `disabled={!currentDiagramId}` with no storage-mode term, and its tooltip is diagram-existence-based (\"Save first to present\"), not mode-based. Contrast the Share button in the same group, which IS mode-gated and matches the ADR: `const shareDisabled = driveActive ? !currentDiagramId : !serverStorageAvailable || !currentDiagramId`. The handler confirms Preview is functional …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":52,"adr":"0005","section":"§1 → Group 2 (Save group) Visibility cell","class":"A2a","claim":"Group 2's Visibility cell makes two claims, both contradicted by code: \"Status cluster always visible\" (StatusCluster renders nothing when there is nothing to say) and \"Save action visible only when `!serverStorageAvailable`\" (the code gates on `!remoteStorageActive`, which additionally hides Save in the Google Drive place — a place that did not exist when this cell was written).","evidence":"packages/axoview-app/src/components/AppToolbar.tsx — `AppToolbar` renders the `toolbar-save` IconButton under `{!remoteStorageActive && (...)}`, not `!serverStorageAvailable`. packages/axoview-app/src/providers/AppStorageContext.tsx defines the two flags as materially different: `const serverStorageAvailable = isServerStorage && isInitialized;` and `const remoteStorageActive = serverStorageAvailable || activeProviderId === 'google-drive';`. So in the Drive place on a storage-less deployment …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":53,"adr":"0006","section":"§2 gesture matrix","class":"A2a","claim":"ADR 0006 §2 is the designated authoritative gesture matrix, yet it never records that a Ctrl/⌘+click on a CONNECTOR toggles the connector PLUS its tile-bound waypoint anchors as ONE group. The matrix's flat row — 'Ctrl/⌘+click an item | toggles `item` in/out of `selectedIds`' — reads as a single-ref toggle; the code went further and the ADR was never extended. ux-principles §4.4 documents this as a MUST-level design rule while pointing at ADR 0006 for the 'full contract' — so the doc that …[truncated]","evidence":"packages/axoview-lib/src/interaction/modes/Cursor.ts — `handleConnectorClickSelection` branches on ctrl to `toggleConnectorGroupSelection`, which builds `groupIds` from `connectorId` + `getConnectorWaypointRefs(connector)` and toggles the whole group via `setSelectedIds(next)`; non-connector clicks take the separate flat `handleItemClickSelection` path. docs/guidelines/ux-principles.md §4.4 carries the row ADR 0006 lacks ('Ctrl/⌘+click a **connector** | toggles connector **plus its tile-bound …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":54,"adr":"0007","section":"§3 — \"The regression suite is a frozen behavioural baseline, not a unit-test grab-bag\"","class":"A2a","claim":"ADR 0007 §3 decides that `__perf_refactor_regression__/` is a **frozen** pre-refactor behavioural baseline whose specs must not be modified during a refactor and must be green \"before any refactoring begins\". The suite's own charter README — the document §3 cites as the source of those rules — now says the exact opposite: the refactoring it guarded is done and the folder is \"**no longer frozen**\", having become a general behavioural-contract suite. ADR 0007 is Accepted, has no addendum, and no …[truncated]","evidence":"`packages/axoview-lib/src/__perf_refactor_regression__/README.md` — opening blockquote: \"Folder name is historical (`__perf_refactor_regression__`). These tests began as guards for the performance refactoring (the Canvas2D → WebGL substrate work, ADR 0019 → ADR 0038, shipped 2026-07-08). That refactoring is **done**; the folder is now a general **behavioural-contract** regression suite and is **no longer frozen**.\" README section `## What lives here` further widens scope to \"store/reducer …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":55,"adr":"0007","section":"Context §1 — \"`localStorage.setItem('axoview_perf_enabled', 'true')` in prod\"","class":"A2a","claim":"ADR 0007's Context §1 documents the production enable incantation for the DiagnosticsOverlay as `localStorage.setItem('axoview_perf_enabled', 'true')`. The code compares the flag to the string `'1'`, not `'true'`. A reader who follows the ADR literally sets the key to `'true'`, `readEnabled()` returns `false`, and the overlay stays dark with no error — the documented command silently does not work. This was wrong on the ADR's first day, not drift: the `=== '1'` comparison was already present at …[truncated]","evidence":"`packages/axoview-app/src/stores/diagnosticsStore.ts` — `const STORE_KEY = 'axoview_perf_enabled';` and, inside `readEnabled()`: `return localStorage.getItem(STORE_KEY) === '1';`. The writer agrees: `writeEnabled(v)` does `localStorage.setItem(STORE_KEY, '1')`. `git show e1cd620:packages/axoview-app/src/stores/diagnosticsStore.ts` (e1cd620 = the ADR's only commit, 2026-06-10) shows the identical `=== '1'` check, so the ADR never matched the code.","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":56,"adr":"0007","section":"§1 — \"DiagnosticsOverlay (runtime sampling) — off unless toggled from the BottomDock or enabled via the `axoview_perf_en…","class":"A2a","claim":"ADR 0007 §1 (the Decision section, not just Context) states the DiagnosticsOverlay's gating as exactly two paths and asserts it is \"off unless\" one of them fires. The code has neither an accurate on-path list nor an \"off by default\" default: in dev builds `readEnabled()` returns `true` unconditionally and the BottomDock checkbox is rendered `disabled`, so the overlay is always on and cannot be toggled off; and a third gate the ADR never mentions — `axoview-perf-harness` — force-DISABLES the …[truncated]","evidence":"`packages/axoview-app/src/stores/diagnosticsStore.ts` — `readEnabled()` body, in order: `if (localStorage.getItem('axoview-perf-harness') === '1') return false;` then `if (IS_DEV) return true;` then `return localStorage.getItem(STORE_KEY) === '1';`. `packages/axoview-app/src/components/DiagnosticsOverlay.tsx` — file header comment \"Dev: always enabled (cannot be turned off).\" and the enable checkbox `<input type=\"checkbox\" checked={enabled} disabled={IS_DEV} ...>`. Drift proof: `git show …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":57,"adr":"0007","section":"§4 — \"The diagnostic pyramid is the canonical order; the playbook is its living record\"","class":"A2a","claim":"ADR 0007 §4 decides, without qualification, that the four-step pyramid in perf-troubleshooting.md is *the* canonical order \"when a slow-interaction report lands\", and that the playbook is *the* living record gaining a Case study per landed investigation. The playbook has since disclaimed the majority of the render surface: it now declares itself the React/DOM layer only and routes every canvas-region symptom to a sibling doc, `canvas-rendering-guidelines.md`, that ADR 0007 does not mention. …[truncated]","evidence":"`docs/guidelines/perf-troubleshooting.md` — blockquote heading `> ## ⚠️ Scope — this playbook is the React/DOM layer, not the GPU`, body: \"Since [ADR 0038](../adr/0038-webgl-instanced-render-substrate.md) (2026-07-08) the *bulk* of what you see — nodes, labels, connectors, rectangles — is drawn on **WebGL2**, and this document has no coverage of it. **If the symptom is inside the canvas region** ... go to **[canvas-rendering-guidelines.md](canvas-rendering-guidelines.md)**\". Same file's `## …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":58,"adr":"0008","section":"§8 (Code-level naming — boolean fields)","class":"A2a","claim":"ADR 0008 §8 states, as accomplished past fact, that \"`serverStorage: boolean` (the dead `RuntimeConfig` field deleted by ADR 0009 Decision 2) was rightly removed\". The field was never removed: `interface RuntimeConfig` in packages/axoview-app/src/hooks/useRuntimeConfig.ts still declares `serverStorage: boolean` and it is load-bearing today.","evidence":"packages/axoview-app/src/hooks/useRuntimeConfig.ts — `export interface RuntimeConfig { … serverStorage: boolean }` plus `DEFAULT_CONFIG.serverStorage = false`; consumed in packages/axoview-app/src/providers/AppStorageContext.tsx `manager.setServerStorage(config.serverStorage)` (with an \"ADR 0009 D2\" comment beside it) and asserted in hooks/__tests__/useRuntimeConfig.test.ts (`expect(cfg.serverStorage).toBe(false)`). DECISIVE: `git log -S 'serverStorage: boolean' -- …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":59,"adr":"0008","section":"§6 (Package naming)","class":"A2a","claim":"ADR 0008 §6 rules that \"the `name` field in `package.json` matches the directory exactly (no `@axoview/foo` scoping today; that's a future-publication question if the lib goes scoped)\". Two of five packages violate it, and both violated it before the ADR was written — the parenthetical \"no `@axoview/foo` scoping today\" was factually false on the day it was authored.","evidence":"packages/axoview-lib/package.json → `\"name\": \"axoview\"` (directory is `axoview-lib`, so the name does not match the directory). packages/axoview-e2e/package.json → `\"name\": \"@axoview/e2e\"` — an `@axoview/foo`-scoped name, the exact form the ADR says does not exist. Conformant: axoview-app/axoview-worker/axoview-backend all match their directories. TIMING PROOF: `git show 7838ff2:packages/axoview-lib/package.json` → `\"name\": \"axoview\"` and `git show 7838ff2:packages/axoview-e2e/package.json` → …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":60,"adr":"0008","section":"§5 (`data-axoview-id` — one anchor namespace)","class":"A2a","claim":"§5 rules \"Do not introduce `data-testid` in parallel — one anchor namespace, two consumers.\" New `data-testid` anchors have been introduced into lib source AFTER the ADR (2026-07-02 → 2026-07-04), and the E2E suite queries `data-testid` directly in specs, contradicting both the rule and the e2e helper's own stated posture.","evidence":"106 `data-testid` occurrences vs 306 `data-axoview-id`. Post-ADR introductions (ADR is 2026-05-20): `git log -S 'data-testid' -- packages/axoview-lib/src/components/SceneLayers/Labels/LabelHitLayer.tsx` → 1560daa (2026-07-02) adding `data-testid=\"label-inline-editor\"`; same probe on components/TopBarStyleControls/TopBarStyleControls.tsx → e348392 (2026-07-04), e53ab99 (2026-07-04), 50a84f1 (2026-07-03), 54e2eeb (2026-07-03). E2E specs query them raw, not via the sanctioned transitional …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":61,"adr":"0009","section":"§4a — the `ENABLE_SERVER_STORAGE` row in the Self-host env-var table","class":"A2a","claim":"ADR §4a says `ENABLE_SERVER_STORAGE` has Default `false` and that '**The implicit-off default is intentional** — a fresh container without this flag set behaves as if it were a static-only deploy and the SPA falls back to Local mode.' The code ships server storage ON in every container path: no fresh container from this repo's image can lack the flag, because the image itself sets it.","evidence":"`Dockerfile` — `ENV ENABLE_SERVER_STORAGE=true` (git log -L confirms it was added in `bf3a30f`, i.e. it PREDATES the 2026-05-20 ADR — the row was wrong at authoring). `compose.yml` — `ENABLE_SERVER_STORAGE=${ENABLE_SERVER_STORAGE:-true}`. `.env.example` — `ENABLE_SERVER_STORAGE=true`. `docker-entrypoint.sh` — `if [ \"$ENABLE_SERVER_STORAGE\" = \"true\" ]` starts the backend. `packages/axoview-backend/server.js` — the startup guard `if (STORAGE_ENABLED && AUTH_MODE === 'none')` emits a `[SECURITY] …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":62,"adr":"0009","section":"§1 — 'one HTTP contract, one adapter, one Worker short-circuit' framing","class":"A2a","claim":"§1 says 'The HTTP contract is **single-source** (routes.js). Both runtimes expose the same `/api/*` surface; they differ only in **which routes carry storage**' and 'If/when Cloudflare gains real storage (Drive on Phase 3B, or R2/D1 later), it will land as a new `StorageAdapter` implementation per ADR 0010, **not as a second route layer**.' Drive shipped, and it landed as exactly a second route layer: a Cloudflare-only `/api/*` route defined directly in the Worker, outside routes.js, with no …[truncated]","evidence":"`packages/axoview-worker/src/app.ts` — `app.get('/api/public/drive/:fileId', …)`, a full Drive read-proxy handler (metadata gate, trashed→410, 10 MB cap, content fetch) with **no** corresponding export in `packages/axoview-backend/src/routes.js` and **no** mount in `packages/axoview-backend/server.js`. `packages/axoview-worker/src/auth.ts` `isPublicRoute` carries a dedicated `/^\\/api\\/public\\/drive\\/[A-Za-z0-9_-]{10,120}$/` bypass that server.js's `isPublicRoute`/`PUBLIC_SNAPSHOT_RE` does not …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":63,"adr":"0009","section":"§4b — Cloudflare (Worker + Pages Functions) env-var table","class":"A2a","claim":"§4b is headed 'Env-var contract — one section per target' and Consequences claim 'The env-var contract is the deploy operator's single reference' — but the Cloudflare table omits `GOOGLE_API_KEY` and `GOOGLE_PROJECT_NUMBER`, two vars the Worker reads today, one of which gates an entire shipped feature.","evidence":"`packages/axoview-worker/src/app.ts` — `interface Env` declares `GOOGLE_API_KEY?: string` and `GOOGLE_PROJECT_NUMBER?: string`; the `/api/config` handler returns `drivePublicPreview: !!c.env.GOOGLE_API_KEY` and `googleProjectNumber: c.env.GOOGLE_PROJECT_NUMBER || null`; `app.get('/api/public/drive/:fileId')` opens with `const key = c.env.GOOGLE_API_KEY; if (!key) return c.json({ error: 'preview-disabled' }, 503)` — so an operator who provisions exactly §4b's five vars gets anonymous Drive …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":64,"adr":"0010","section":"§6 Snapshots and share namespace — \"the only auth exception\" / \"Worker: same check\"","class":"A2a","claim":"ADR 0010 §6 says \"Public-namespace reads bypass the auth middleware on both runtimes (Express: `isPublicRoute` check; Worker: same check in `authMiddleware`). This is **the only auth exception** in the contract.\" Both halves are now false. ADR 0042 (2026-07-13) added a second public-namespace bypass, `GET /api/public/drive/:fileId`, to the Worker only — so the two runtimes no longer run \"the same check\", and the public-namespace read is no longer the only exception. ADR 0042's Supersedes names …[truncated]","evidence":"packages/axoview-worker/src/auth.ts — `isPublicRoute(method, pathname)` has THREE exempting branches: `/api/config`, `/^\\/api\\/public\\/drive\\/[A-Za-z0-9_-]{10,120}$/` (comment: \"Anonymous read-proxy … (ADR 0042 / ADR 0043 #3)\"), and `/^\\/api\\/public\\/diagrams\\/[A-Za-z0-9_-]{21,64}$/`. packages/axoview-backend/server.js — `isPublicRoute(req)` has only two (`/api/config` + `PUBLIC_SNAPSHOT_RE` for diagrams); the divergence is deliberate, per `drivePublicPreview: false` in …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":65,"adr":"0011","section":"§6 (i18n fallback rationale) + Consequences/Negative ('The 7 non-English locales lag behind')","class":"A2a","claim":"ADR 0011 twice states there are 7 non-English locale files. There are 12 (13 total including en-US). Stronger: this was never true — all 13 locale files already existed at the ADR's own commit (7838ff2), so the count was wrong on the day it was written, not merely stale.","evidence":"`ls packages/axoview-app/src/i18n/*.json | wc -l` → 13: bn-BD, de-DE, en-US, es-ES, fr-FR, hi-IN, id-ID, it-IT, pl-PL, pt-BR, ru-RU, tr-TR, zh-CN. `git ls-tree --name-only 7838ff2 packages/axoview-app/src/i18n/` → the same 13 files at the ADR's own authoring commit. ADR text L129 ('The 7 other locale files (`fr-FR.json`, `zh-CN.json`, …)') and L160 ('**The 7 non-English locales lag behind.**'). The underlying DECISION is intact and correctly implemented: a per-locale check shows …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":66,"adr":"0011","section":"§2 (Dialog shape — locked, the code template) + Implementation notes bullet 1","class":"A2a","claim":"§2's copy-paste template specifies `PaperProps={{ sx: ... }}`, but zero of the 6 conformant dialogs use it — all 6 use MUI v7's `slotProps={{ paper: { sx: ... } }}`. An author following the ADR's 'locked' template literally would write a prop the codebase abandoned. Relatedly, Implementation notes' 'The current MUI version's `Dialog` API is stable; no API churn expected' was falsified by the MUI upgrade to ^7.3.9 (PaperProps → slotProps.paper is exactly that churn).","evidence":"ADR §2 snippet: `<Dialog ... PaperProps={{ sx: { boxShadow: ..., borderRadius: 2 } }}>`. All 6 dialogs instead use `slotProps={{ paper: { sx: { boxShadow: '0px 10px 20px -2px rgba(0,0,0,0.25)', borderRadius: 2 } } }}` (LocalModeShareErrorDialog, ReadonlyLoadErrorDialog, PublicShareLoadErrorDialog, SaveErrorDialog, ShareErrorDialog, ImportErrorDialog). `packages/axoview-app/package.json` → `\"@mui/material\": \"^7.3.9\"`. Repo-wide `grep -rn 'PaperProps' packages/*/src --include=*.tsx` survives only …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":67,"adr":"0011","section":"§1 ('Every failure-of-intent surfaces an explicit Dialog') + §2 ('No raw <div> overlays, no custom focus traps')","class":"A2a","claim":"ADR 0042's DriveDisplayGate surfaces the `transient` load failure of the /display/drive/:id route (a user typed/opened a URL and the read failed on network/rate-limit/5xx) as a custom full-route overlay with a Retry button, not as a MUI Dialog — §2 explicitly accommodates that case via its 'optional secondary action ... Retry' slot. ADR 0011 carries no addendum recording that a later ADR introduced a non-Dialog failure-of-intent surface. Flagged primarily for the cross-ADR graph agent: 0011 and …[truncated]","evidence":"packages/axoview-app/src/components/DriveDisplayGate.tsx renders a custom `<Box>` overlay (`maxWidth: 440`, with the comment `// Below MUI dialogs (1300) so DriveAccessRequiredDialog and friends`) carrying `<Typography variant=\"h6\" component=\"h1\">` + `driveDisplay.transientBody` + a `driveDisplay.retryButton` ('Try again') for `driveDisplayState === 'transient'` — not a MUI Dialog, and it uses the `driveDisplay.*` i18n namespace rather than §6's `dialog.<scenario>.*`. Note the gate DOES honor …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":68,"adr":"0012","section":"Decision → Scope changes bullet 2 (\"Item-type parity\") + Interaction bullet 2 (\"Click the item → the popover pins open\")…","class":"A2a","claim":"ADR 0012 says clicking an item pins the popover and that \"node, connector, rectangle, and textbox all use the same popover\"; in the code only NODES can pin. `Pan.handleReadonlyClick` — the sole `itemControls` producer reachable in EXPLORABLE_READONLY — bails with `setItemControls(null)` for every hit whose `type !== 'ITEM'`, so clicking a connector, rectangle or textbox in view mode DISMISSES rather than pins. Hover parity does hold for all four types; pin parity does not. The ADR's manual …[truncated]","evidence":"packages/axoview-lib/src/interaction/modes/Pan.ts → `handleReadonlyClick`: `const item = getItemAtTile({ tile: currentTile, scene }); if (item?.type !== 'ITEM') { uiState.actions.setItemControls(null); return; }`. The popover is fully built for the other three — packages/axoview-lib/src/components/ViewModeInfoPopover/ViewModeInfoPopover.tsx → `const INFO_TYPES = new Set(['ITEM', 'CONNECTOR', 'RECTANGLE', 'TEXTBOX'])` gating `pinnedControls`, and ViewModeInfoPopover.helpers.ts → `deriveItemInfo` …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":69,"adr":"0012","section":"Decision → Interaction bullet 1 (\"Hover an item that has content\") and bullet 4 (\"Only items with content (non-empty nam…","class":"A2a","claim":"ADR 0012 says hover shows a preview for \"an item that has content\", where content = non-empty name, notes, OR headerLink. The code narrows the HOVER gate to notes-only: a name-only or headerLink-only item pops nothing on hover. This was an explicit owner decision dated 2026-07-01 that lives ONLY in a code comment and an e2e docblock — ADR 0012 was never extended, so the ADR still states the pre-narrowing rule.","evidence":"packages/axoview-lib/src/components/ViewModeInfoPopover/ViewModeInfoPopover.tsx → the `info` useMemo: comment `// Owner 2026-07-01: on HOVER, the popover appears ONLY when the item has notes — no empty name-only preview cluttering the canvas. A PINNED click is an explicit request for details, so it still shows name / link / notes via hasInfoPopoverContent.` implemented as `if (isPinned) { if (!hasInfoPopoverContent(name, notes, headerLink)) return null; } else if (!hasNotes) return null;`. …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":70,"adr":"0012","section":"Decision → Interaction bullet 4 (the content gate) — the PIN-side gate specifically","class":"A2a","claim":"ADR 0012 defines the content gate as \"non-empty `name`, `notes`, or `headerLink`\". The gate that actually decides whether a click pins is `Pan.nodeHasReadonlyContent`, which is a DIFFERENT set: `link || headerLink || description || notes`. `name` is not a qualifier (a name-only node can never pin, contradicting the ADR), while `link` and `description` — neither of which appears in the ADR's gate — do qualify. Two disagreeing gates exist in the code; Pan's runs first and therefore wins. …[truncated]","evidence":"packages/axoview-lib/src/interaction/modes/Pan.ts → `const nodeHasReadonlyContent = (modelItem: ModelItem | undefined): boolean => !!modelItem && (!!modelItem.link || !!modelItem.headerLink || htmlHasVisibleText(modelItem.description) || htmlHasVisibleText(modelItem.notes));` — note the absence of `name`. Compare packages/axoview-lib/src/components/ViewModeInfoPopover/ViewModeInfoPopover.helpers.ts → `hasInfoPopoverContent` = `hasName || hasNotes || !!headerLink`. Effective pin gate …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":71,"adr":"0012","section":"Decision → Positioning paragraph (\"an MUI `Popover` renders via Portal at the document root … it anchors to the item's D…","class":"A2a","claim":"The Decision section justifies the popover's pixel-stability with a mechanism that does not exist: there is no MUI `Popover`, no Portal, and no `anchorEl` in the implementation, and the popover does not anchor to \"the item's DOM node\" — canvas items have no per-item DOM node (WebGL2 is the sole bulk substrate per ADR 0038). It is an absolutely-positioned MUI `Box`/`Paper` placed imperatively in screen space, anchored to a TILE via `getTilePosition`. The 2026-06-12 placement revision directly …[truncated]","evidence":"packages/axoview-lib/src/components/ViewModeInfoPopover/ViewModeInfoPopover.tsx imports `{ Box, Paper, IconButton, Typography, Stack, Link } from '@mui/material'` — `Popover` is absent; the render root is `<Box ref={wrapperRef} data-axoview-id=\"view-mode-info-popover\" sx={{ position: 'absolute', … }}>` wrapping a `<Paper elevation={4}>`, and `applyPlacement` writes `el.style.left / el.style.top / el.style.transform` directly. Grep for `anchorEl` across the ViewModeInfoPopover/ directory → zero …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":72,"adr":"0013","section":"2026-06-18 addendum, bullet 2 — 'across all 14 locales'","class":"A2a","claim":"The addendum claims the Present rename landed 'across all 14 locales'. The repo has 13 locales, and has never had 14 in its entire git history. Of those 13, only 11 actually carry the renamed keys; the other 2 are partial locales that inherit en-US. MINOR — the decision itself (the Present vocabulary lock) genuinely shipped; only the scope number is wrong. Filing at low confidence because it is a count nit, not a misstatement of the decision.","evidence":"packages/axoview-app/src/i18n/ contains exactly 13 files (bn-BD, de-DE, en-US, es-ES, fr-FR, hi-IN, id-ID, it-IT, pl-PL, pt-BR, ru-RU, tr-TR, zh-CN .json). packages/axoview-lib/src/i18n/ contains 14 .ts files but one is `index.ts` — the likely origin of the off-by-one (a bare `ls | wc -l`). `git log --all --diff-filter=AD --name-status` over both i18n dirs shows only additions (11 locales at the packages rename commit 33d4d71, then de-DE + id-ID at 7838ff2) and zero deletions — so no 14th …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":73,"adr":"0014","section":"§Entry & placement, bullet 1 (and the Context paragraph that frames the whole ADR as filling ADR 0005's reserved slot)","class":"A2a","claim":"ADR 0014 says the pen lives in \"the top toolbar's View-modes group (ADR 0005 Group 1)\", and its Context declares \"[ADR 0005] Decision 1 already reserved a top-toolbar slot for exactly this … This is that ADR.\" The code never put it there: the pen is a floating, absolutely-positioned canvas button rendered by axoview-lib's UiOverlay, and the app toolbar has no View-modes group at all. ADR 0005's Group 1 row still reads \"`◐ View` (focus mode, annotation overlay) | Reserved slot — buttons not …[truncated]","evidence":"packages/axoview-lib/src/components/AnnotationPalette/AnnotationPalette.tsx — symbol `AnnotationPalette`: the pen is `<IconButton data-axoview-id=\"annotation-pen\" sx={{ position:'absolute', right: rightOffset, top: 12, zIndex: 20 }}>` where `rightOffset = dockOpen ? DOCK_WIDTH + EDGE_GAP : EDGE_GAP` (it dodges the 300px right dock — a canvas-overlay concern a toolbar button would never have). Mounted at packages/axoview-lib/src/components/UiOverlay/UiOverlay.tsx (`{!hideViewControls && …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":74,"adr":"0014","section":"Revision (2026-06-12), bullet 3 — \"**Esc / `V`** also returns to Select\"","class":"A2a","claim":"ADR 0014 binds exactly two keys to \"return to Select\": Esc and `V`. The code binds `v`/`V` with NO modifier guard, so **Ctrl+V / Cmd+V also silently resets the armed annotation tool to `select`** — colliding with the interaction manager's clipboard shortcut. Both handlers are registered on `window`, and `preventDefault()` in the paste path does not stop the other listener, so a user with pencil armed who presses Ctrl+V both pastes canvas content and loses their draw tool. The ADR is right here; …[truncated]","evidence":"packages/axoview-lib/src/components/AnnotationLayer/AnnotationLayer.tsx — the Esc/V `useEffect` (guarded `if (!shown || tool === 'select') return;`, so it is live in exactly the draw/eraser case): its `onKey` filters only INPUT/TEXTAREA/isContentEditable targets, then `if (e.key === 'Escape' || e.key === 'v' || e.key === 'V') { actions.setAnnotationTool('select'); }` — `grep -rn \"ctrlKey|metaKey\" AnnotationLayer.tsx` returns NOTHING. Registered via `window.addEventListener('keydown', …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":75,"adr":"0018","section":"Resolved contract decision D-12, bullet \"Second finger *during* a GRAB → abort\"","class":"A2a","claim":"ADR 0018 D-12 says: \"**Second finger *during* a GRAB** → **abort** the carry (Decision 4/7.4) — it does **not** zoom; the grab is a modal state and takes precedence.\" The code does the exact inverse on both halves: a second finger during an in-flight item drag **commits** the drag and then **starts pinch-zooming**. This clause is live, not superseded: the 2026-06-14 (B) revision supersedes only \"Decisions 3, 4 and 6\" and explicitly reaffirms D-12 (\"two fingers → pinch‑zoom + pan (unchanged, …[truncated]","evidence":"packages/axoview-lib/src/interaction/useInteractionManager.ts — `onTouchPointerDown`: `if (ts.pointers.size >= 2) { // Second finger → pinch. End an in-flight item drag first so the node commits where it is rather than chasing the pinch centroid. clearLongPress(); if (ts.phase === 'item') forwardMouse(e, 'mouseup', interactionsEl); … ts.phase = 'pinch'; return; }`. `forwardMouse(e,'mouseup',…)` routes to `DragItems.mouseup`, which commits (docs/guidelines/canvas-interaction.md §8: DRAG_ITEMS …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":76,"adr":"0018","section":"Decision 7.4 (Shared guardrails — pointercancel)","class":"A2a","claim":"Decision 7.4 says \"**`pointercancel` handling** to abort a `GRAB` (carrying) state when the OS reclaims the gesture (Decision 4's abort transition)\" — i.e. the item returns to origin. The shipped handler does the opposite: it **commits** the in-flight gesture. Decision 7 is live — the 2026-06-14 (B) revision names \"guardrails\" among the things that are \"unchanged\", and supersedes only Decisions 3, 4 and 6 — so §7.4's abort semantics are the ADR's only live statement about pointercancel, and it …[truncated]","evidence":"packages/axoview-lib/src/interaction/useInteractionManager.ts — `onTouchPointerCancel`: `// End an in-flight item gesture cleanly (commit where it is).` / `if (wasPhase === 'item') forwardMouse(e, 'mouseup', interactionsEl);` (and the 'palette' branch even treats a moved cancel over the canvas as a **drop**, forwarding mousemove/mousedown/mouseup). Corroborated by the canonical guideline docs/guidelines/canvas-interaction.md §8: \"**There is no `rollbackDragTransaction`.** … every `exit` …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":77,"adr":"0019","section":"Decision ¶2 — \"The per-node DOM `<Node>` is retained, but only for the *actively-manipulated* nodes — the single selecte…","class":"A2a","claim":"ADR 0019 defines the DOM-hybrid set as a CLOSED two-member union (\"only … ∪ …\"). The code's `hybridIds` unions THREE members — a node whose NAME label is being dragged is promoted to the DOM overlay too (the \"Track P (T6 fix)\"). The code won; the ADR was never extended. Material because the header names \"the DOM-hybrid rationale\" as one of exactly three rules \"retained and unchanged\", so a reader takes this enumeration as current-and-complete.","evidence":"packages/axoview-lib/src/components/Renderer/Renderer.tsx, symbol `hybridIds`: `if (!selectedNodeId && !draggingKey && !labelDragId) return null;` then `if (selectedNodeId) ids.add(selectedNodeId); if (draggingKey) for (…) ids.add(id); if (labelDragId) ids.add(labelDragId);` — deps `[selectedNodeId, draggingKey, labelDragId]`. The third member comes from `const labelDragId = useUiStateStore((s) => s.labelDrag?.id ?? null)`, documented in-file as \"Track P (T6 fix): a canvas node whose NAME label …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":78,"adr":"0019","section":"Implementation addendum — 2026-06-15, \"Deferred, documented gaps\" → \"**Connectors** remain on the existing DOM/SVG layer…","class":"A2a","claim":"This addendum bullet states connectors render on the DOM/SVG layer and that folding them onto the canvas is \"unmeasured scope\". Both are now false — connector bodies render on the WebGL2 `ConnectorsCanvas` via a hybrid split. The bullet's twin in known_issues.md was explicitly corrected; the ADR's was not — and known_issues.md then points the reader BACK into the stale ADR prose, so the round-trip lands on the false statement.","evidence":"packages/axoview-lib/src/components/SceneLayers/Connectors/ConnectorsCanvas.tsx — header \"ConnectorsCanvas — WebGL2 INSTANCED draw of the connector BODIES (halo + core\"; imports `createSpriteBatch` from src/webgl/glSpriteBatch, `attachContextLossRecovery`, and `walkDots, walkDashes, buildAaLineQuad, AA_FEATHER` from src/webgl/lineStyle. Renderer.tsx renders `<ConnectorsCanvas connectors={canvasConnectors} />` alongside a sparse `domConnectors` memo (comment: \"WebGL ConnectorsCanvas; only a …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":79,"adr":"0020","section":"§4 Tier ladder & north-star metric — the T4 row","class":"A2a","claim":"ADR 0020 §4 marks the tier ladder's T4 row **✅** against the targets '10,000 SSB / **5,000+** LEB60' with the unlock 'WebGL instanced sprites (SHIPPED 2026-07-08, ADR 0038); **batched per-tick rule eval**'. The code supports only half of that row: the WebGL sprite substrate shipped, but (a) LEB60 — which this same ADR defines as *concurrently simulated, collision-checked, MOVING entities* holding p95 ≤ 16.6 ms — has never been measured by anything, and (b) 'batched per-tick rule eval' does not …[truncated]","evidence":"ABSENT LEB60 harness: engine-perf.spec.ts exposes `window.__perfH = { measureSpawn, measureDrag, measurePaste, measureIdle, measureLabelDrag, measureBloat, measurePan, measureLabelHeavy, measureConnLabelHeavy, measureBgHeavy, measureFloatingLabelHeavy, resetState, calibrate }` — no symbol moves N entities under collision. `measureDrag(N, steps)` drags ONE node through occupied tiles (DRAG_STEPS = 80 rAF-paced pointermoves); `measurePan(N, steps)` moves the VIEWPORT over static entities …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":80,"adr":"0020","section":"§1 Harness (KR1) — the committed N grid","class":"A2a","claim":"§1 says the harness captures metrics 'per entity count N ∈ {25, 50, 100, 200, 500, 1000, **2000**}'. The harness's committed default grid stops at 1000 — 2000 has never been a default cell, and the certified artifact the ADR points readers at has no 2000 row. The ADR overstates the grid by one cell, and it is the cell that would evidence the §4 T2 rung (SSB 2,000).","evidence":"engine-perf.spec.ts symbol `N_SET`: `const N_SET = process.env.PERF_N ? process.env.PERF_N.split(',').map(...) : [25, 50, 100, 200, 500, 1000];` — carrying the comment 'defaults are **the charter's committed baseline grid (KR2)** and median-of-≥7', i.e. the code asserts this 6-cell set IS the committed grid the ADR is describing. perf-results/baseline.md (the certified output) has exactly six spawn rows and six drag rows — 25/50/100/200/500/1000, no 2000 — and engine-perf.spec.ts asserts that …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":81,"adr":"0020","section":"2026-07-02 addendum — the scenario table's 'What it stresses (substrate)' column","class":"A2a","claim":"The scenario table's substrate column still names **Canvas2D** for three of five knobs — `PERF_LABELHEAVY` ('Canvas2D label draw'), `PERF_BGHEAVY` ('Canvas2D fill+stroke overdraw'), `PERF_FLOATLABELS` ('Canvas2D `LabelsCanvas`') — and the closing prose calls the gate 'the measured evidence behind the **Label substrate = Canvas2D** decision'. Those surfaces are WebGL2 now. The column is written in the present tense about knobs that still run today, so a reader running `PERF_FLOATLABELS` is told …[truncated]","evidence":"packages/axoview-lib/src/components/SceneLayers/Labels/LabelsCanvas.tsx — the exact symbol the table names — is headed 'LabelsCanvas (ADR 0031) — **WebGL2 INSTANCED** draw of the floating Label layer' and imports `createSpriteBatch` from src/webgl/glSpriteBatch, `attachContextLossRecovery`, `rasterizeLabelChip`; its only 2D context is an explicitly offscreen one ('Throwaway 2D context for measureText (the visible canvas is owned by WebGL)'). Same for the rectangle surface `PERF_BGHEAVY` …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":82,"adr":"0021","section":"\"Related pre-T3 render/interaction fixes (PR #49)\" → \"Canvas label stalk + LOD + text-layout cache\" bullet","class":"A2a","claim":"ADR 0021 says NodesCanvas \"caches per-node text layout keyed by `(fontSize, name, description)`\". The code's key is `(fontSize, bold, italic, name)` — `description` is not in the key, and the string \"description\" does not appear anywhere in NodesCanvas.tsx today. The code moved; the ADR was never extended. (Low severity and arguably defensible as a frozen historical record — see refutation notes.)","evidence":"packages/axoview-lib/src/components/SceneLayers/Nodes/NodesCanvas.tsx — symbol `labelLayoutCacheRef` (`Map<string, LabelLayout>`); its `layoutKey` is built as `${fontSize}:${labelBold ? 1 : 0}:${labelItalic ? 1 : 0}:${name}`, and the `measureNodeLabel` signature takes (mctx, name, fontSize, labelBold, labelItalic, chip) — no description parameter. `grep -n \"description\" NodesCanvas.tsx` returns ZERO hits. The claim WAS true when the ADR was written: `git show …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":83,"adr":"0022","section":"§1 (mouse-model table, row \"Double left-click on item → Open the details panel (`itemControls`)\")","class":"A2a","claim":"ADR 0022 §1 says double-left-click on an item opens the details panel; for TEXTBOX the code instead selects WITHOUT the panel and drops into an on-canvas edit session. ADR 0034 §1 won this ground on 2026-07-03, but 0022 was never extended and 0034 never declared it — there is no link in either direction.","evidence":"CODE: packages/axoview-lib/src/interaction/useInteractionManager.ts, symbol `onDoubleClick` — `if (item.type === 'TEXTBOX') { uiState.actions.setItemControls({ type: 'TEXTBOX', id: item.id }, { openPanel: false }); uiState.actions.setEditingTextBoxId(item.id); return; }`, with the handler's own doc comment: \"TEXTBOX is the exception (ADR 0034): … double-clicking a text box EDITS its text in place (deck stays closed; Details is reachable via the context menu)\". Corroborated by …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":84,"adr":"0023","section":"§2 — One snap chokepoint","class":"A2a","claim":"ADR 0023 §2 says \"All drag/paste/placement position resolution goes through a single helper, `resolvePlacement(tile, offset, snap, globalSnap)`\" — drag listed FIRST. The code diverges: the drag path never calls the chokepoint. `DragItems.ts` does not import `resolvePlacement` at all and instead reimplements the ADR's snapped predicate inline as a private `isOffGrid`. The paste and fresh-placement paths DO conform; only drag was carved out, and the ADR was never extended to record the carve-out.","evidence":"packages/axoview-lib/src/interaction/modes/DragItems.ts — symbol `isOffGrid` = `(snap, globalSnap) => !((snap ?? true) && globalSnap)`, a duplicate of the `const snapped = (snap ?? true) && globalSnap` test inside packages/axoview-lib/src/utils/resolvePlacement.ts `resolvePlacement`. DragItems.ts's own comment admits it: \"Mirrors the snapped test in resolvePlacement; kept inline so the drag hot path has no extra import indirection.\" The duplicated predicate is consumed by `applyNodePreview` …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":85,"adr":"0025","section":"Addendum 2026-06-21, first clause ('a hidden `NON_INTERACTIVE` Axoview whose **Canvas2D icons** decode asynchronously' ……","class":"A2a","claim":"ADR 0025's addendum describes the export's node layer as Canvas2D and its connectors as DOM/SVG; the code says the node layer is WebGL2 and connectors are a WebGL2/DOM hybrid. The MECHANISM the addendum decides (wait for `data-all-icons-drawn`, then recapture) is fully intact and correct — only the substrate descriptors around it drifted when ADR 0038 made WebGL2 the sole substrate, and 0025 was never extended to match.","evidence":"Head-to-head contradiction between the ADR sentence and the file implementing that exact sentence: waitForIconsDrawn.ts opens 'the export snapshots the hidden Axoview's **WebGL2 node layer**' and says connectors 'draw on the **first GPU build**' — where the ADR says 'Canvas2D icons' and 'connectors, being DOM/SVG'. Producer confirms: NodesCanvas.tsx header — 'NodesCanvas — **WebGL2 INSTANCED** bulk draw of the node layer'; imports `createSpriteBatch` from src/webgl/glSpriteBatch; body comment …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":86,"adr":"0027","section":"§3 Command catalogue — Item menu, the \"type-specific\" clause","class":"A2a","claim":"ADR 0027 §3 says the item menu carries \"type-specific (node: Link…; connector: Add waypoint / line style; etc.)\". The code implements NONE of those three, and implements two type-specific entries the ADR never mentions. §3 is wrong in both directions, and the strongest instance is **\"connector: Add waypoint\"** — that command has never existed in the menu and the waypoint affordance lives in an entirely different interaction layer (Alt-click splice / anchor drag on the connector-anchor overlay). …[truncated]","evidence":"packages/axoview-lib/src/components/CanvasContextMenu/CanvasContextMenu.tsx — the complete `variant === 'item'` render array is: key=\"details\" (`handleDetails`), key=\"rename\" (`handleRename`, gated `canRename`), key=\"addLabel\" (`handleAddLabel`, gated `canAddLabel = target?.type === 'CONNECTOR'`), key=\"addNote\" (`handleAddNote`), key=\"cut\"/\"copy\"/\"paste\" (`useCopyPaste`), key=\"duplicate\" (`handleDuplicate`), key=\"forward\"/\"back\" (`nudgeZOrder`), key=\"tofront\"/\"toback\" (`setZOrderExtreme`), …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":87,"adr":"0027","section":"§3 Command catalogue — Multi menu (2026-06-20 addendum): \"Unsnap / collision placeholders (T8)\"","class":"A2a","claim":"ADR 0027 §3 describes the multi/bulk menu's off-grid rows as \"Unsnap / collision **placeholders** (T8)\" — i.e. as inert stubs pending work item T8. They are not placeholders and never shipped as placeholders: the code implements both as real transactional bulk mutations over the entire selection. A reader planning work off this ADR would conclude bulk unsnap / bulk disable-collision is unimplemented and re-do it. The identifier \"T8\" is additionally dangling — it resolves to nothing in the …[truncated]","evidence":"packages/axoview-lib/src/components/CanvasContextMenu/CanvasContextMenu.tsx — symbol `handleBulkUnsnap`: `scene.transaction(() => { filterUserFacingRefs(selectedIds).forEach((ref) => applyOffGrid(ref, { snap: false })); })`, and symbol `handleBulkDisableCollision`: `scene.transaction(() => { filterUserFacingRefs(selectedIds).forEach((ref) => applyOffGrid(ref, { collides: false })); })`. Both are wired into the `variant === 'multi'` render array (key=\"snap\" → `run(handleBulkUnsnap)`, …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":88,"adr":"0027","section":"§2 Invocation — the gesture table's \"Right-click tap on an item\" / \"Right-click tap on empty canvas\" rows","class":"A2a","claim":"ADR 0027 §2's table presents right-tap→menu as unconditional on what was tapped (item vs empty). The shipped contract has a third, undocumented axis: the current **mode**. The item and canvas menus are a CURSOR-mode-only affordance — in any tool mode (LASSO, FREEHAND_LASSO, CONNECTOR, TEXTBOX, LABEL, RECTANGLE.DRAW, PLACE_ICON) a right-tap instead ABORTS the in-flight tool and opens no menu. The code states this deliberately and pins the contrary behaviour with a unit test, so the code plainly …[truncated]","evidence":"packages/axoview-lib/src/interaction/usePanHandlers.ts, symbol `handleRightButtonUp` — after the multi-selection branch, an explicit early return with the rationale in-comment: \"In a tool mode (lasso / freehand / connector …) a right-tap with no multi-selection hit ABORTS the in-flight tool action — the long-standing 'right-click cancels the tool' behavior. The single/canvas menus are a CURSOR-mode affordance, so they do not open here.\" → `if (currentModeType !== 'CURSOR') { …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":89,"adr":"0028","section":"§2 (Target & driver) — and the matching Consequences bullet 'Preview is storage-less'","class":"A2a","claim":"ADR 0028 §2 says the integration preview is 'storage-less (session/localStorage)' and that 'server-only features (share links, server persistence) are out of scope on it and must not be filed as defects — for full view-mode/share coverage, run against a local/Docker build with ENABLE_SERVER_STORAGE=true'. For the Drive-native share path shipped by ADR 0042 (released v3.7.0) the code says the OPPOSITE: the anonymous read proxy is a Cloudflare-ONLY feature that runs ON the preview and is …[truncated]","evidence":"functions/api/[[path]].ts — `export const onRequest = handle(app)` importing `packages/axoview-worker/src/app`, so the Pages preview runs the worker, not just static assets. packages/axoview-worker/src/app.ts — `app.get('/api/public/drive/:fileId', ...)` + `drivePublicPreview: !!c.env.GOOGLE_API_KEY`. packages/axoview-backend/src/routes.js — `getConfig` returns `drivePublicPreview: false` (hardcoded; no proxy route). packages/axoview-app/src/App.tsx — `<Route path=\"/display/drive/:driveFileId\" …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":90,"adr":"0029","section":"Decision ¶1 (the pinned helper implementation) + Consequences","class":"A2a","claim":"ADR 0029 defines the shared sanitizer as exactly `DOMPurify.sanitize(content, { USE_PROFILES: { html: true } })` and is the corpus's only durable record for that helper; the shipped helper does strictly more — it registers a module-load `DOMPurify.addHook('afterSanitizeAttributes', …)` on the DOMPurify **singleton** that force-rewrites `rel=\"noopener noreferrer\"` onto every anchor with an href. This security behaviour (added 2026-07-05, after the ADR's 2026-06-21 date) is recorded in NO ADR, …[truncated]","evidence":"packages/axoview-lib/src/utils/sanitizeHtml.ts → module-scope `DOMPurify.addHook('afterSanitizeAttributes', (node) => { if (node.tagName === 'A' && (node as Element).hasAttribute('href')) { (node as Element).setAttribute('rel', 'noopener noreferrer'); } })`, source comment attributing it to \"security review 2026-07-05\". Provenance: `git log -- packages/axoview-lib/src/utils/sanitizeHtml.ts` → `ba0666a fix(security): harden self-host auth/CORS, CF Access JWT, sanitizer and zip import` …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":91,"adr":"0030","section":"§2 Amendment (2026-07-02) — \"the one place the strip acts on a mixed selection\"","class":"A2a","claim":"ADR 0030's 2026-07-02 amendment asserts the cross-type font-size stepper is \"**the one place** the strip acts on a mixed selection\". The code has a second one: text colour applies to a heterogeneous label-bearing selection through the identical target set and transaction. The exclusivity claim is false and ADR 0030 was never extended for it.","evidence":"packages/axoview-lib/src/components/TopBarStyleControls/TopBarStyleControls.tsx — alongside `crossTypeLabelIds` / `nudgeCrossTypeLabelSize` (the documented stepper) sits `const crossTypeColorOnly = !sel && !!crossTypeLabelIds;` and `const applyCrossTypeTextColor = (color: string | undefined) => { … crossTypeLabelIds.forEach(…) … }`, under the comment \"O3 (ADR 0034, resolved 2026-07-03): text colour also works on a MIXED label-bearing selection — same targets/transaction as the size stepper …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":92,"adr":"0030","section":"Implementation notes (non-binding) — the i18n bullet","class":"A2a","claim":"ADR 0030 states \"the strip currently has ~30 hardcoded English strings and no `useTranslation` — ratifying it as a primary surface makes localization a release blocker (separate slice)\". The separate slice landed: the strip is fully localized across 13 locales. A reader today is told a shipped release blocker is still outstanding.","evidence":"packages/axoview-lib/src/components/TopBarStyleControls/TopBarStyleControls.tsx — `import { useTranslation } from 'src/stores/localeStore';` with three `const { t } = useTranslation('topBarStyleControls');` call sites; ~104 `t(...)` call sites in the file, including every string the ADR's own amendments name (`t('iconSize')`, `t('changeIcon')`, `t('changeIconBulk')`, `t('iconSizeBulk')`, `t('linkToDiagram')`, `t('openLinkedDiagram')`, and the `disabledTip('changeIconDisabled')` / …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":93,"adr":"0031","section":"§Decision point 6 + the 2026-06-30 'render substrate decided by measurement' addendum + the 2026-06-30 'Confirmed by C1'…","class":"A2a","claim":"ADR 0031 binds the floating-Label render layer to Canvas2D — the addendum states 'The extracted `Label` layer (slice **C1**) renders on **Canvas2D** … This binds C1's render-layer implementation', and the Confirmed-by-C1 addendum asserts 'The shipped `LabelsCanvas` (Canvas2D billboard + a per-(text, fontSize, B/I) layout cache …)' and closes 'The substrate decision holds.' The code does WebGL2: `LabelsCanvas` is a GPU instanced draw. The ADR was never extended to record the change.","evidence":"packages/axoview-lib/src/components/SceneLayers/Labels/LabelsCanvas.tsx — symbol `LabelsCanvas`: imports `createSpriteBatch`/`SpriteBatch` from `src/webgl/glSpriteBatch`, `attachContextLossRecovery` from `src/webgl/contextLoss`, `rasterizeLabelChip`/`CHIP_SUPERSAMPLE` from `src/webgl/itemRaster`; internal fns `buildInstances(b: SpriteBatch)` (b.beginInstances/b.putCanvas/b.addSprite/b.commitInstances) and `drawGLBatch(b)` (b.render(...)). Its own header comment reads 'LabelsCanvas (ADR 0031) — …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":94,"adr":"0033","section":"Context ¶ — \"All four are `.optional()` with `Required<Omit<…>>` compile-time defaults, so the divergence is safe\"","class":"A2a","claim":"ADR 0033 says all FOUR conventions are guarded by `Required<Omit<…>>` compile-time defaults; the code has such a guard for only THREE. The connector additional-label row (`labels[].bold/italic/strikethrough`) has no defaults object at all, so nothing forces exhaustiveness when a field is added to `connectorLabelSchema`.","evidence":"packages/axoview-lib/src/config.ts: repo-wide `grep -rn 'Required<'` over lib+app src returns exactly six guards — `VIEW_DEFAULTS`, `VIEW_ITEM_DEFAULTS`, `CONNECTOR_DEFAULTS`, `TEXTBOX_DEFAULTS`, `LABEL_DEFAULTS`, `RECTANGLE_DEFAULTS` (plus an unrelated `Required<ThemeOptions>` in styles/theme.ts). There is no `Required<Omit<ConnectorLabel, …>>`; the type `ConnectorLabel` (packages/axoview-lib/src/types/model.ts, `z.infer<typeof connectorLabelSchema>`) has no DEFAULTS constant. FALSIFIABLE …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":95,"adr":"0035","section":"§2 amendment (2026-07-06, granular consent) — 'Identity works either way, but Drive UI gates on this flag instead of let…","class":"A2a","claim":"ADR 0035's granular-consent amendment says a grant without drive.file still yields a working identity session that merely hides Drive UI behind driveScopeGranted. The code does the opposite: it treats identity-without-Drive as a HARD STOP — it discards the token, forgets the account, parks the store in an undocumented DRIVE_ACCESS_REQUIRED state, and surfaces a blocking (no-backdrop, no-Escape) re-consent dialog. Neither DRIVE_ACCESS_REQUIRED nor grantDriveAccess()/markDriveScopeMissing() …[truncated]","evidence":"packages/axoview-app/src/stores/authStore.ts — _onToken(): `if (!driveScopeGranted) { clearProfileHint(); set({ status: 'DRIVE_ACCESS_REQUIRED', accessToken: null, expiresAt: null, driveScopeGranted: false, ... }) }` with the comment 'Hard stop (ADR 0035 §6): drive.file is the ENTIRE point of signing in ... Identity without Drive is never a usable session'; getValidToken() returns null for DRIVE_ACCESS_REQUIRED; exported type AuthStatus includes 'DRIVE_ACCESS_REQUIRED'; symbols …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":96,"adr":"0036","section":"§7 (Errors, retries — online-only v1), bullet 1","class":"A2a","claim":"ADR 0036 §7 decides that after backoff is exhausted the failure surfaces as \"a sticky error notification with a **Retry** action\". The code pushes a sticky error notification with NO action, and the only Retry affordance lives on a different surface (StatusCluster) where, after an autosave failure, clicking it is a no-op.","evidence":"(a) NO ACTION ON THE NOTIFICATION: packages/axoview-app/src/providers/DiagramLifecycleProvider.tsx — the `useAutoSave({... onError })` handler is `onError: () => { notificationStore.push({ severity: 'error', message: t('alert.saveFailed', 'Auto-save failed — check your connection.') }) }`. No `action` field. It IS sticky (stores/notificationStore.ts `autoDismissDefaults` maps `error: undefined`), so only the Retry half diverges. The store fully supports it — `NotificationAction { label, onClick …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":97,"adr":"0036","section":"§1 (Provider shape and Drive API mapping), opening paragraph + the 2026-07-07 Amendment claim (2)","class":"A2a","claim":"§1 asserts \"all list queries filter on it [appProperties {axoview:'true'}] plus `trashed = false`\", and the 2026-07-07 amendment claims fix (2) brought folder-scoped list queries into line with that claim. `GoogleDriveProvider.findManifestId` is a folder-scoped list query that still carries no appProperties filter.","evidence":"packages/axoview-app/src/services/storage/providers/GoogleDriveProvider.ts — `private async findManifestId(root: string)` builds `const q = \\`'${root}' in parents and name='${MANIFEST_NAME}' and trashed=false\\`` and passes it to `listFiles`. It has the `trashed=false` half but not `APP_MARKER_Q`, unlike its siblings `listDiagrams` and `listFolders`, which both interpolate `${APP_MARKER_Q}` into every branch of their query. It is called by both `getTreeManifest()` and `saveTreeManifest()`. …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":98,"adr":"0037","section":"§2 (Decision 2 — 'The active provider follows the open diagram'), the branch enumeration clause","class":"A2a","claim":"ADR 0037 §2 says: 'Every existing mode branch — `remoteStorageActive`, autosave vs manual save, the Session chip, unsaved-changes guards, delete-dialog copy — thereby keys off the OPEN diagram with no per-branch rewrite.' The code does NOT do this for the delete-dialog copy: it got a per-branch rewrite and keys off the deleted ITEM's place, not the open diagram. Four of the five enumerated branches check out; this one is false. The core decision itself (§2's first sentence) is fully IMPLEMENTED …[truncated]","evidence":"packages/axoview-app/src/components/fileExplorer/FileExplorer.tsx — the delete-confirm `<Dialog>` body IIFE: `const place = deleteConfirm?.placeId === 'google-drive' ? 'Drive' : 'Session'` → `t(\\`fileExplorer.deleteBody${shape}${place}\\`)`. Its own code comment contradicts the ADR verbatim: 'Places model: the fate follows the ITEM's place, not a global mode.' Backing symbols: `interface DeleteConfirm { …; placeId: PlaceId }`; `handleDeleteFolder`/`handleDeleteDiagram` set `placeId: …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":99,"adr":"0041","section":"Decision 6 (`sitemap.xml`) + the \"Depends on ADR 0040\" ripple bullet","class":"A2a","claim":"ADR 0041 Decision 6 says the sitemap lists \"`/`, `/privacy`, `/terms`, + **the editor entry** per ADR 0040\", and its ripple bullet makes the trigger explicit: \"If 0040's URL layout changes (editor at `/app`), **the sitemap gains the editor entry** but the canonical *root* stays the landing.\" That trigger fired — ADR 0040 R1 shipped the editor at `/app` on 2026-07-13 — but the shipped sitemap has no editor entry, and the code is RIGHT to omit it: the app shell is `noindex`, and listing a noindex …[truncated]","evidence":"packages/axoview-app/public/sitemap.xml — `<urlset>` contains exactly three `<loc>` elements (https://axoview.app/, /privacy, /terms); no /app entry. packages/axoview-app/app-shell.html — `<meta name=\"robots\" content=\"noindex\" />` with the comment \"this shell is intentionally noindex — it carries no SEO/social metadata\". packages/axoview-app/src/appBase.ts — `export const APP_BASENAME = `${publicUrl}/app`` proves the editor lives at /app (the ripple's trigger condition). git: `git log -p …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":100,"adr":"0042","section":"§3 (Link semantics — live document) + Implementation notes (i18n)","class":"A2a","claim":"ADR 0042 §3 states \"Popover copy **must** say 'view the latest version' so the difference is explicit\" and the Consequences repeat it (\"popover copy carries the disclosure\"). The Drive share surface renders NO live-vs-snapshot disclosure at all. The string was written and shipped, then silently dropped by a later UI restructure; the ADR was never extended, and the i18n key is now orphaned in all 13 locales.","evidence":"Key exists: packages/axoview-app/src/i18n/en-US.json → `share.drive.liveHint` = \"Anyone the file is shared with sees the latest version — the link opens the live diagram, not a snapshot.\" (present in all 13 files: bn-BD, de-DE, en-US, es-ES, fr-FR, hi-IN, id-ID, it-IT, pl-PL, pt-BR, ru-RU, tr-TR, zh-CN). Zero code references: `grep -rn liveHint packages/ ` matches ONLY the i18n JSONs (src + build copies) — no .ts/.tsx hit. Render sites checked exhaustively: AppToolbar.tsx Drive branch …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":101,"adr":"0042","section":"Consequences → \"Positive\", bullet 2 (\"on both deploy targets\")","class":"A2a","claim":"The Consequences claim \"Anonymous preview with zero server storage — the Drive-native analogue of the rejected server-snapshot option, **on both deploy targets**\" is false: §8 made the read proxy a Cloudflare-only feature, so anonymous preview does not exist on the Express/Docker target at all. §8 says so explicitly, but the Consequences bullet was never updated, so a reader of the Consequences section is told the opposite of what ships.","evidence":"Code: packages/axoview-backend/src/routes.js `getConfig` hardcodes `drivePublicPreview: false` with the comment \"The Express/Docker target has NO anonymous read proxy (that lives only in the Cloudflare worker — ADR 0042 §8)\"; `grep -rn \"public/drive\\|publicDrive\\|driveProxy\" packages/axoview-backend/src/` returns ZERO hits (the route exists only as `app.get('/api/public/drive/:fileId')` in packages/axoview-worker/src/app.ts); packages/axoview-app/src/services/drive/drivePublicRead.ts …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":102,"adr":"0042","section":"§2, \"Required mutual-exclusion guard\" paragraph","class":"A2a","claim":"§2 prescribes a specific mechanism in MUST terms — the owner-readonly loader effect \"is guarded only by `isPublicShareUrl`/`isReadonlyUrl`\" and \"MUST gain a `driveFileId` early-return (mirroring its existing `if (isPublicShareUrl) return` line)\". The shipped effect has no `driveFileId` early-return and no `isPublicShareUrl` return; it was re-keyed onto its own route param instead. The ADR's mandated OUTCOME (no co-fire on `/display/drive/*`) is fully achieved — only its stated mechanism and its …[truncated]","evidence":"packages/axoview-app/src/providers/DiagramLifecycleProvider.tsx, the owner-readonly loader effect (deps `[readonlyDiagramId, storage]`): the entire guard is `if (!readonlyDiagramId || !storage) return;`, with the comment \"react-router populates `readonlyDiagramId` ONLY on `/display/:readonlyDiagramId`, so it is falsy on the public-share … and Drive … routes. This single guard replaces the per-sibling co-fire exclusions (ADR 0042 §2 mutual exclusion — a fourth readonly route needs zero new …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":103,"adr":"0001","section":"§ Filename convention — single-diagram export","class":"A2b","claim":"ADR 0001 specifies `axoview-diagram-<diagramName>-<timestamp>.json` for single-diagram export. Nothing has ever emitted that name; the single-diagram exporter emits `<diagramName>-<YYYY-MM-DD>.json` — no `axoview-diagram-` prefix and a date-only stamp rather than a timestamp.","evidence":"packages/axoview-app/src/providers/DiagramLifecycleProvider.tsx — `exportDiagram` builds `const filename = `${diagramName || 'diagram'}-${new Date().toISOString().split('T')[0]}.json`` and assigns it to `a.download`. The sibling conventions ARE implemented, in `projectZipFilename` (projectZip.ts): the folder branch and the `axoview-project-${ts}.zip` default. `git log -S` on that filename expression returns only 33d4d71 (the mechanical packages/fossflow-* -> packages/axoview-* folder rename) …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":104,"adr":"0018","section":"Decision 5 + Implementation notes — `TAP_TIME_MS`","class":"A2b","claim":"Decision 5 defines a tap as a press that lifts \"within `TAP_SLOP_PX` of its `pointerdown` point (default ~8-10 screen px) **and** within `TAP_TIME_MS`\", and the Implementation notes require that \"Thresholds (`TAP_SLOP_PX`, `TAP_TIME_MS`) belong in a config module, not inline constants.\" Only the spatial half shipped. `TAP_TIME_MS` does not exist under any spelling; the classifier is purely spatial, so the ADR's conjunctive tap definition is not the shipped rule. Decision 5 is live (revision B: …[truncated]","evidence":"packages/axoview-lib/src/config/tapGesture.ts exports only `TAP_SLOP_PX = 8` and `exceedsTapSlop(from, to, slop)` — a pure distance check (`dx*dx + dy*dy > slop*slop`) with no time term — and its own header comment restates the rule without the time clause: \"A press that lifts within TAP_SLOP_PX is a tap; beyond it, a drag/pan.\" The two time constants that DO exist are different primitives and are inline, not in the config module: packages/axoview-lib/src/interaction/useInteractionManager.ts …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":105,"adr":"0026","section":"§2 (Decision 2, second sentence)","class":"A2b","claim":"ADR 0026 §2 decides \"Counter-scale handles to screen px per [ux-principles §8.8] so they don't shrink with zoom.\" The code never counter-scales the transform handles: they are rendered inside a zoom-scaled SceneLayer at fixed pixel sizes, so they scale 1:1 with zoom — the exact failure §8.8 exists to prevent. Nothing in the ADR (which has zero addenda) reverses this decision.","evidence":"packages/axoview-lib/src/components/TransformControlsManager/TransformAnchor.tsx — `TransformAnchor` computes `const w = isEdge ? EDGE_LENGTH : TRANSFORM_ANCHOR_SIZE` / `const h = isEdge ? EDGE_THICKNESS : TRANSFORM_ANCHOR_SIZE` (constants: `EDGE_LENGTH`=42, `EDGE_THICKNESS`=15, `TRANSFORM_ANCHOR_SIZE`=30 in src/config.ts) and sets its only transform as `const boxTransform = isEdge ? `rotate(${barAngleDeg ?? 0}deg)` : undefined` — rotation only, never `scale(1/zoom)`. The component never reads …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":106,"adr":"0040","section":"Implementation notes, final bullet: \"Once the routing map is finalized, record it as a dated **addendum to [ADR 0009]** …","class":"A2b","claim":"ADR 0040 designates ADR 0009 as the owner of the URL namespace + Cloudflare routing and directs that the finalized R1 routing map be recorded there as a dated addendum. The map was finalized and shipped on 2026-07-13 (commit f3a2ce2) and 0040's own top callout declares it \"implemented\" and \"DONE\" — but no such addendum was ever written. ADR 0009 therefore still describes a URL namespace that no longer exists: it presents `/display/p/<shareUuid>` and `/display/<diagramId>` as live root routes, …[truncated]","evidence":"ABSENCE side — docs/adr/0009-deployment-topology.md: `grep -in '/app\\b|app\\.html|landing|0040|_redirects|301|marketing|crawl'` returns only three unrelated hits, all pointing at the file `packages/axoview-worker/src/app.ts` (lines 13, 34, 172). Zero addenda dated after 2026-05-22 (its only addendum is §3 \"**Addendum (2026-05-22, productization-audit C.2 row B-1):**\"). Its §5 heading is verbatim \"### 5. Authoritative wrangler.toml, mandatory `_routes.json`, and the asset pipeline\" — no …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":107,"adr":"0014","section":"§Acceptance criteria bullet 1 (\"…opens a draggable floating palette…\") — and, non-bindingly, §Implementation notes bulle…","class":"A2c","claim":"The 2026-06-12 revision killed the draggable palette and scoped its supersession to \"the detail **above**\" — but the Acceptance criteria section sits BELOW the revision and still reads \"**Manual:** The pen toolbar entry opens a draggable floating palette in both edit and preview.\" That criterion asserts behaviour the same ADR deliberately removed and the shipped code deliberately does not have, so anyone executing 0014's acceptance criteria today fails a test the ADR itself already retired. …[truncated]","evidence":"packages/axoview-lib/src/components/AnnotationPalette/AnnotationPalette.tsx — symbol `AnnotationPalette` renders the palette as a MUI `<Paper elevation={6} data-axoview-id=\"annotation-palette\" sx={{ position:'absolute', right: rightOffset, top: 60, zIndex: 20, width: 48 }}>`. It is NOT a `UiElement` (the component imports Box/Divider/IconButton/Paper/Stack/Tooltip from @mui/material and never imports `src/components/UiElement/UiElement`), and it carries no drag affordance — its only pointer …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":108,"adr":"0018","section":"Decision 2 — the `pointerType` → gesture-model table (touch / pen rows)","class":"A2c","claim":"The 2026-06-14 (B) revision explicitly preserves Decision 2 (\"the foundation (Pointer Events, `pointerType` branch, …) is unchanged\") while superseding Decision 4 — but Decision 2's table body is written entirely in terms of Decision 4. Its `touch` row reads \"**Tap-to-select / tap-to-place** state machine (Decision 4)\" and its `pen` row \"Same as `touch`\", and the prose below calls the branch \"The new state machine\". So the one section a reader consults to learn what each pointerType does still …[truncated]","evidence":"The branch MECHANISM is implemented — packages/axoview-lib/src/interaction/useInteractionManager.ts `onPointerDown`: `const kind = (e.pointerType || 'mouse') as PointerKind; pointerTypeRef.current = kind; if (kind !== 'mouse') { … onTouchPointerDown(e) … }` — but what it branches INTO is direct manipulation, not tap-to-place: `onTouchPointerDown` sets phase 'item' + `forwardMouse(e,'mousedown',…)` on a draggable target, else phase 'pan-pending'. No tap-to-place path exists; repo-wide grep for …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":109,"adr":"0034","section":"Consequences → \"Negative / risks\", third bullet (\"The editor keeps Quill's `getSemanticHTML` space→`&nbsp;` behavior (pr…","class":"A2c","claim":"ADR 0034's Consequences section records the Quill `getSemanticHTML` space→`&nbsp;` behaviour as a LIVE, accepted, deferred risk — explicitly \"not fixed here\" and \"masked by auto-grow\". The ADR's own later 2026-07-04 addendum root-caused that exact behaviour (\"Resting render honored neither wrap nor width — root cause: Quill's `getSemanticHTML()` serializes EVERY space as `&nbsp;`\") and FIXED it with `normalizeQuillHtmlSpaces`, applied on both commit and load. Two sections of one Accepted ADR …[truncated]","evidence":"Refuted-and-fixed by: packages/axoview-lib/src/components/SceneLayers/TextBoxes/TextBoxInlineEditor.tsx — the commit path is `sanitizeHtml(normalizeQuillHtmlSpaces(quill.getSemanticHTML()))` (two call sites, the change-commit and the finish-commit); packages/axoview-lib/src/hooks/useInitialDataManager.ts calls `normalizeQuillHtmlSpaces(folded.content)` in the pre-parse normalization so legacy content is normalized on load too. Symbol: `normalizeQuillHtmlSpaces` in …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":110,"adr":"0036","section":"Consequences → \"Negative / risks\", bullet 3 (the Google Picker bullet)","class":"A2c","claim":"ADR 0036's Consequences section states the Google Picker is \"scoped to the custom-folder path only\" — but §2's own dated addendum (2026-07-05 \"v1 deviation\") removed the Picker from the custom-folder path entirely, and the code confirms the custom-folder path is a typed name. Two sections of the same ADR describe opposite designs and only one is marked.","evidence":"ADR text, Consequences: \"The Google Picker adds a second Google script (`apis.google.com/js/api.js` — already CSP-allowed) and its own API-key-less picker config quirks; **scoped to the custom-folder path only**.\" Contradicted by the same ADR's §2.2: \"**v1 deviation (2026-07-05):** the custom path is a **typed folder name**, not the Google Picker … and is deferred; a typed name satisfies 'custom path' without that surface. `configureRoot(name)` implements it.\" Code confirms the deviation won: …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":111,"adr":"0038","section":"§Deferred — the section heading \"## Deferred (recorded, not yet shipped)\" and its preamble","class":"A2c","claim":"The §Deferred heading asserts \"not yet shipped\" and its preamble says \"These were scoped in the productization audit and deliberately deferred; each is a follow-up, not a silent gap\" — but 6 of the section's 8 bullets are now marked IMPLEMENTED / DONE / SHIPPED / ACCEPTED / FIXED in-line and are verifiably in the code. Only 2 items (adaptive/lazy atlas + sampler2DArray; and, per §Consequences, export-Renderer context teardown) are actually still deferred. The heading is a factual statement that …[truncated]","evidence":"Heading/preamble: docs/adr/0038-webgl-instanced-render-substrate.md §Deferred. The six shipped bullets each trace to a live symbol: (1) context-loss → packages/axoview-lib/src/webgl/contextLoss.ts symbol `attachContextLossRecovery`, imported by all four bulk canvases; (2) line-styles → packages/axoview-lib/src/webgl/lineStyle.ts symbols `walkDots`/`walkDashes` + `widthScale` in ConnectorsCanvas.tsx; (3) premultiplied alpha → `premultipliedAlpha: true` in `createSpriteBatch` (glSpriteBatch.ts); …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":112,"adr":"0003","section":"Implementation notes — \"'Differs in any user-visible field' compares: name, url, collection, category\"","class":"A2d","claim":"ADR 0003 names `category` as an icon comparison field. No `category` field has ever existed on the Icon schema; the code compares `isIsometric` instead.","evidence":"packages/axoview-lib/src/schemas/icons.ts `iconSchema` = `z.object({ id, name, url, collection?, isIsometric?, scale? })` — no `category`. packages/axoview-lib/src/utils/leanSave.ts `ICON_COMPARE_FIELDS: (keyof Icon)[] = ['name', 'url', 'collection', 'isIsometric']`. `Icon` is `z.infer<typeof iconSchema>` (packages/axoview-lib/src/types/model.ts), so a literal `'category'` in `ICON_COMPARE_FIELDS` would not typecheck — the ADR's field list is unimplementable as written.","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":113,"adr":"0008","section":"§4 (Provider-id discriminant — label clause)","class":"A2d","claim":"§4 rules that \"Each provider id has a human label resolved via `providerIdToLabel` ([FileExplorer.tsx:82-85])\". The symbol `providerIdToLabel` does not exist anywhere in the repo, and the cited line range now contains unrelated code — the ADR describes a resolution mechanism that is not there.","evidence":"`grep -rni 'providerIdToLabel'` over packages/ → ZERO hits (the only occurrence of the string in the whole repo is inside ADR 0008 itself). The cited anchor no longer resolves: packages/axoview-app/src/components/fileExplorer/FileExplorer.tsx lines 82-85 today sit inside a tree-node render callback (`node.handleClick` / `node.tree.edit(node.id)`), not a label map. The behaviour moved layers rather than vanishing: labels now flow as a prop — FileExplorer.tsx …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":114,"adr":"0008","section":"§2 (Locked vocabulary — Popover examples)","class":"A2d","claim":"§2's Popover row cites `QuickAddNodePopover` as a canonical example of the locked vocabulary. That component was deleted on 2026-06-10; the vocabulary table now teaches the Popover contract by pointing at a surface that does not exist.","evidence":"No `QuickAddNodePopover` component exists: `find packages/*/src -iname '*quickadd*'` returns only packages/axoview-lib/src/__perf_refactor_regression__/quickAdd.groupButton.test.ts. It DID exist when the ADR was written — `git ls-tree -r --name-only 7838ff2 | grep -i quickadd` → packages/axoview-lib/src/components/QuickAddNodePopover/QuickAddNodePopover.tsx — and was deleted in eeec0a6 (2026-06-10, \"v1.1 wrap … dead-code cleanup (#24)\"), confirmed via `git log --diff-filter=D --name-only -- …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":115,"adr":"0009","section":"§4c — 'The `npm run dev:server` script invokes the Express backend with `ENABLE_SERVER_STORAGE=true`'","class":"A2d","claim":"ADR 0009 names a script that does not exist anywhere in the repo, and misdescribes what the real script does.","evidence":"Root `package.json` `scripts` contains `dev`, `dev:win`, `dev:lib`, `dev:backend` — there is no `dev:server`. The real one, `\"dev:backend\": \"npm run dev --workspace=packages/axoview-backend\"`, chains to `packages/axoview-backend/package.json` `\"dev\": \"nodemon server.js\"`, which sets **no** env var — so it does not enable server storage either. `docs/deployment.md` documents the actually-working invocation as `ENABLE_SERVER_STORAGE=true STORAGE_PATH=./diagrams npm run dev:backend`: the operator …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":116,"adr":"0010","section":"§4 Session isolation — the HTTP Basic Auth / nginx bullet (+ the nginx.conf row in \"Files affected\")","class":"A2d","claim":"ADR 0010 §4 states \"HTTP Basic Auth (self-host) covers everything that isn't the public-namespace cutout — that's the nginx config in [nginx.conf:4-5]\" and that \"the current nginx omits the nested `location` block that exempts `/api/public/*`, which is the bug C.2 closes\"; \"Files affected\" repeats the nginx.conf#L4 nested-location row. No such surface exists: nginx.conf has no Basic Auth and no /api/public location, and self-host auth is not nginx's job at all — it is Express `AUTH_MODE`. The …[truncated]","evidence":"nginx.conf — `grep -rn \"auth_basic|htpasswd|Basic Auth\"` over *.conf/*.js/*.ts/Dockerfile/*.sh (excl. node_modules) returns ZERO hits; `grep -n public nginx.conf` matches only two unrelated comments. The real mechanism: packages/axoview-backend/server.js — `AUTH_MODE` (`none`/`shared-token`) + `constantTimeEquals` in the `app.use` auth middleware, with the cutout in `isPublicRoute`/`PUBLIC_SNAPSHOT_RE`. Git proves it was never true: `git log -S auth_basic -- nginx.conf` shows auth_basic ADDED …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":117,"adr":"0019","section":"Addendum **2026-06-21 (UX re-test addendum)**, signal (A1) — \"Text drawn to the canvas — node names *and rich-descriptio…","class":"A2d","claim":"ADR 0019 says the canvas draws \"rich-description captions\" and decodes their HTML. The code draws no description/caption at all — ADR 0032 retired the on-canvas rich caption outright on 2026-06-29. The decode rule survives for the node name/label only. This is material (not merely historical) because the 2026-07-08 header AFFIRMATIVELY re-certifies this exact rule as one of three \"still-valid rules … retained and unchanged\" — an affirmation made 9 days AFTER ADR 0032 had already killed half of …[truncated]","evidence":"PHANTOM: `grep -n \"description|caption\" NodesCanvas.tsx` → zero hits; `grep -i \"description|caption|subtitle\" packages/axoview-lib/src/webgl/*.ts` → zero hits; `rasterizeNodeChip(name, chipW, chipH, s, ss)` in src/webgl/itemRaster.ts takes a `name` and no description. WHAT SURVIVES: NodesCanvas.tsx calls `decodeHtmlEntities(modelItem.label ?? modelItem.name)` — name/label only. WHAT KILLED IT: `git log -S\"getDescriptionText\"` → removed in `894cb3b` (\"Node: drop the competing rich caption …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":118,"adr":"0023","section":"Implementation notes (non-binding) — \"Context-menu entries via the existing `ContextMenu` component\"","class":"A2d","claim":"The Implementation notes tell an implementer to hang the entries off \"the existing `ContextMenu` component\". No component by that name exists in the repo — the only one is `CanvasContextMenu`, which ADR 0027 created FOR this purpose. The note also self-contradicts the same ADR's §3, which states twenty lines earlier that \"there is **no** context-menu component today, so #20 has no home until ADR 0027 ships\". Both cannot be true; §3 + ADR 0027 are the accurate pair, so the note is the phantom.","evidence":"`find packages/axoview-lib/src -iname \"*ContextMenu*\" -not -path \"*__tests__*\"` returns only packages/axoview-lib/src/components/CanvasContextMenu/ and .../CanvasContextMenu/CanvasContextMenu.tsx — symbol `CanvasContextMenu`. There is no `ContextMenu` component. The entries in question actually live in CanvasContextMenu.tsx as `handleToggleSnap` / `handleToggleCollision` / `applyOffGrid`. Corroborated from the other side: docs/adr/0027-canvas-context-menu.md line 14 — \"[ADR 0023] (#20) routes …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":119,"adr":"0024","section":"Addendum 2026-06-20 §2 — \"Repositioning only on canvas; sizing stays numeric\"","class":"A2d","claim":"ADR 0024's 2026-06-20 addendum §2 states \"Precise positioning lives in the Style panel's existing **Label height** slider, now **signed** (its old `min 60` couldn't express below-node).\" That slider does not exist in the code. It was deleted with its host file `NodeStyleTab.tsx` by commit 18ea5af and was never replaced, so the on-canvas drag is now the ONLY way to set `labelHeight` — the ADR's stated numeric/precise half of the contract is a phantom surface. Neither ADR 0024 nor ADR 0030 …[truncated]","evidence":"PRESENT AT ADR-IMPL TIME: `git show b67fad4:packages/axoview-lib/src/components/ItemControls/NodeControls/NodeStyleTab/NodeStyleTab.tsx` contains the exact slider the addendum describes — comment \"Label position — signed offset (ADR 0024): negative drops the label below the node\" over `<Section title={t('labelHeight')}><Slider marks step={20} min={-200} max={280} value={node.labelHeight ?? 80} onChange={(_, v) => onViewItemUpdated({ labelHeight: v as number })} />`. DELETED BY: commit 18ea5af …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":120,"adr":"0035","section":"Consequences (bullet 2) + Acceptance criteria (manual verification) — the \"Reconnect\" chip","class":"A2d","claim":"ADR 0035 says 'No true background refresh and no cross-reload persistence — the \"Reconnect\" chip is a deliberate UX cost, not a bug' (Consequences) and 'reload with Drive active shows the Reconnect chip' (Acceptance criteria → Manual verification). No such chip exists in the code: the ADR's own §3 amendment retired it ('the \"Reconnect\" chip is retired') and the §5 amendment removed it ('the standalone chip is gone'), replacing it with an amber Badge dot on the AuthControl avatar. Both …[truncated]","evidence":"packages/axoview-app/src/components/AuthControl.tsx — the needsReconnect affordance is `<Badge variant=\"dot\" color=\"warning\" ... invisible={!needsReconnect} data-axoview-id={needsReconnect ? 'auth-needs-reconnect' : undefined}>` wrapping the Avatar; the only MUI `Chip` in the file is SessionStatusRow's 'Session' place chip, unrelated to auth. Cross-reload persistence: packages/axoview-app/src/stores/authStore.ts — PROFILE_HINT_KEY = 'axoview-google-profile', `user: loadProfileHint()` in the …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":121,"adr":"0039","section":"§1 (Decision), opening parenthetical","class":"A2d","claim":"ADR 0039 §1 says the picker body is \"rendered inside the existing StripButton popover **and the detail-panel controls**\". The detail-panel render site is a phantom: `ColorPickerBody` has exactly ONE importer in the entire repo — TopBarStyleControls.tsx. No ItemControls deck renders any colour control, and none ever did. The clause also contradicts §5 of the same ADR (which enumerates all 7 call sites, every one of them a strip control) and ADR 0030, which ADR 0039's own header cites as \"the …[truncated]","evidence":"Sole importer: `packages/axoview-lib/src/components/TopBarStyleControls/TopBarStyleControls.tsx` line 59 `import { ColorPickerBody } from '../ColorSelector/ColorPickerBody'` — repo-wide grep for `ColorPickerBody` across packages/*/src returns only that file, ColorPickerBody.tsx itself, and its test. The ItemControls tree has NO colour control: `RectangleControls.tsx` (symbol `RectangleControls`) carries the comment \"all visual styling lives on the top-bar style strip, so the deck has no text …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":122,"adr":"0041","section":"The \"Implementation status (2026-07-10)\" header callout — the phrase \"the current canonical root under 0040's shipped R2…","class":"A2d","claim":"ADR 0041's status callout tells every reader the site is running \"0040's **shipped R2 interim**\". R2 is a phantom: ADR 0040 records it as \"**R2 (rejected)** — content-in-shell at `/` … Shipped briefly (2026-07-10) as a safe interim, **then removed when R1 landed**\", and R1 (landing at `/`, editor at `/app`) is what actually ships. The two paired ADRs therefore state contradictory records of the same deployment. Worse, the ADR file was CREATED by the very commit that removed R2, so the claim was …[truncated]","evidence":"packages/axoview-app/public/index.html — head comment: \"The marketing landing = the canonical, indexed root (**ADR 0040 R1**). The editor SPA lives at /app (app-shell.html).\" packages/axoview-app/src/appBase.ts — `export const APP_BASENAME = `${publicUrl}/app``, with the comment \"R1 (ADR 0040): the marketing landing owns the site root (`/`); the editor lives under `/app`\". packages/axoview-app/public/_redirects — header \"Cloudflare Pages routing — **R1 (ADR 0040)**: marketing landing at /, …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":123,"adr":"0042","section":"§4 (parenthetical) + Consequences → \"Negative / risks\", bullet 2","class":"A2d","claim":"Two sites outside §1 still describe the `gapi.drive.share.ShareClient` widget and its mandatory drive.google.com/`webViewLink` fallback as live parts of the design, though the 2026-07-14 revision deleted that mechanism. §4 says the new service is \"(`driveSharing`: **ShareClient launch**, `permissions.list` summary, preview-URL builder)\" and the Consequences assert \"ShareClient depends on third-party cookies + a live Google browser session — **the drive.google.com fallback is mandatory, not …[truncated]","evidence":"packages/axoview-app/src/services/drive/driveSharing.ts exports exactly: `drivePreviewUrl`, `getFileShareMeta`, `listPermissions`, `getAccessSummary`, `getAccessOverview`, `setAnyoneWithLink`, `addPersonPermission`, `removePermission` — no ShareClient loader, no launcher; its header comment records \"The legacy client-side `ShareClient` widget was dropped\". packages/axoview-app/src/services/drive/gapiLoader.ts types the loader as `loadGapiModule(name: 'picker')` — 'drive-share' is not even …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":124,"adr":"0005","section":"§Context 1 (MainMenu.tsx link) + §5b (\"mqa-results.md #27\")","class":"A3a","claim":"Two of the ADR's references point at files that no longer exist on disk: §Context 1's markdown link to `MainMenu.tsx`, and §5b's citation \"mqa-results.md #27\".","evidence":"(1) §Context 1 links to `../../packages/axoview-lib/src/components/MainMenu/MainMenu.tsx` — neither the file nor its parent directory exists (`test -d`/`test -f` both negative), and `grep -rn \"MainMenu\"` over packages/ returns zero hits. Removal is recorded by §5's 2026-05-23 amendment, so the ADR is not lying — the hyperlink is simply dead. (2) §5b ends \"mqa-results.md #27\" — `find . -name \"mqa-results*\"` (excluding node_modules) returns nothing; `git log --all --diff-filter=A -- …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":125,"adr":"0006","section":"§7 (line 72)","class":"A3a","claim":"ADR 0006 §7 sources its four refinements from a tactical doc — 'Four refinements from the canvas-UX overhaul (`docs/tactical/canvas-ux-overhaul.md`)' — that has NEVER existed at that path in this repository, at any commit, on any ref. The addendum's provenance is unciteable.","evidence":"`git log --all --oneline -- 'docs/tactical/canvas-ux-overhaul.md'` returns 0 commits. `git log --all --pretty=format: --name-only --diff-filter=A -- 'docs/tactical/*'` enumerates all 26 tactical docs ever added (incl. retired ones like perf-charter.md, productization-audit.md, mqa-design-shakeout.md) — canvas-ux-overhaul.md is absent. An ls-tree scan across history finds no blob by that name. `grep -rn canvas-ux-overhaul docs/` hits only docs/adr/0006 line 72 and docs/adr/0013 line 46 (both …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":126,"adr":"0006","section":"Context (line 17) + See also (line 131)","class":"A3a","claim":"ADR 0006 cites `docs/tactical/mqa-design-shakeout.md` twice — once as the record of the three-architecture trade-off that justifies the whole design ('The plan considered three architectures'), and once as the 'MQA tactical plan' See-also link holding the #8/#9 user-facing requirements. The file was deleted and its content was not migrated anywhere, so both citations are dead and the ADR's stated rationale source is unrecoverable from the working tree.","evidence":"`test -e docs/tactical/mqa-design-shakeout.md` → ABSENT; docs/tactical/ contains only README.md and adr-code-audit.md. Deleted in commit 53e16a3 ('feat(ux): startup splash + parallel storage probes + 800ms timeouts') per `git log --diff-filter=D --all`. `find docs -iname '*mqa*'` returns nothing — it did not move into docs/guidelines/ or docs/reviews/ during the 2026-07-15 sweep. `grep -rln mqa-design-shakeout docs/` hits ONLY docs/adr/0006 itself. Line 131 is a real markdown link `[MQA …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":127,"adr":"0008","section":"Context, §3, §6, \"Files affected\", \"See also\" (14 links)","class":"A3a","claim":"ADR 0008 links to `docs/tactical/productization-audit.md` 14 times — as the cited driving evidence for Decisions 1, 3, 4, 6 and 7 — but that file was deleted from the repo on 2026-05-23, i.e. within days of the ADR being written. Every evidentiary citation in an ADR whose stated principle is \"Every rule below cites the finding that drove it\" now resolves to nothing.","evidence":"`grep -c 'tactical/productization-audit.md' docs/adr/0008-naming-convention.md` → 14. `ls docs/tactical/productization-audit.md` → No such file. Deletion is old and unrelated to the current working-tree sweep: `git log --diff-filter=D --name-only -- docs/tactical/productization-audit.md` → 6d33f1b \"chore(v1.1): dead-code wave — Track 0 (#3)\", 2026-05-23. `git status --porcelain -- docs/tactical/` confirms the staged 2026-07-15 sweep deleted only canvas-interaction-baseline.md, …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":128,"adr":"0008","section":"\"See also\" (docs/workflow.md row)","class":"A3a","claim":"ADR 0008's \"See also\" asserts \"[docs/workflow.md](../workflow.md) — references Decision 7 (skill naming) in its 'Process debt' section.\" workflow.md contains no reference to ADR 0008, Decision 7, or skill naming — the claimed back-reference does not exist, making the cross-link one-way.","evidence":"`grep -n '0008' docs/workflow.md` → zero hits; `grep -n -i 'naming convention|skill naming|adr 0008|decision 7' docs/workflow.md` → zero hits. The section itself DOES exist (docs/workflow.md line 233, \"## Process debt — deferred skills\") and tables the nine deferred candidates, but it attributes them to \"The productization audit (§A.9.4; wrapped — see git history)\" — never to ADR 0008. Contrast with the ADR's other stated cross-reference obligation, which DID land correctly and proves the …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":129,"adr":"0008","section":"§1 rename table + \"Files affected by adopting this ADR\"","class":"A3a","claim":"Eight markdown links in the §1 \"Current\" column and the \"Files affected\" list point at the four PRE-rename paths (components/ExportDialog.tsx, fileExplorer/ExportDialog.tsx, src/StorageManager.tsx, components/SessionModeBanner.tsx). All four were renamed by this very ADR, so every one of those links 404s on disk.","evidence":"None of the four link targets exist: a repo-wide `find` for ExportDialog|StorageManager.tsx|SessionModeBanner across .ts/.tsx returns only the post-rename files (ExportSingleDiagramDialog.tsx, fileExplorer/ExportProjectZipDialog.tsx, LocalStorageInspector.tsx, LocalModeBanner.tsx) plus the retained services/storage/StorageManager.ts class. `grep -rn 'SessionModeBanner'` across packages/ → zero hits (only LocalModeBanner in App.tsx and the component file). Dead anchors: ADR lines 32-35 …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":130,"adr":"0009","section":"Header `Supersedes:` line, Context bullet 1 (Theme 7), 'Distribution model' paragraph, and all three 'See also' bullets","class":"A3a","claim":"ADR 0009 contains six markdown links to `../tactical/productization-audit.md`; the file does not exist. The `**Supersedes:**` header is the worst hit — 0009's sole provenance link (the classification table justifying every decision in it) is dead.","evidence":"`ls docs/tactical/productization-audit.md` → 'No such file or directory'; `docs/tactical/` contains only `README.md` and `adr-code-audit.md`. `git log --oneline --diff-filter=D -- docs/tactical/productization-audit.md` → deleted in `6d33f1b` ('chore(v1.1): dead-code wave — Track 0', PR #3). The six occurrences: the `**Supersedes:**` header line (anchor `#a61-flare_planmd-section-classification`), the Context 'Theme 7 of the Phase A synthesis' bullet (anchor `#theme-7`), the 'Distribution model' …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":131,"adr":"0010","section":"\"Files affected by adopting this ADR\" + Consequences → Negative/open (atomicity bullet)","class":"A3a","claim":"The \"Files affected\" section lists five pending edits and closes with \"All edits land in the C.2 cleanup tactical\"; Consequences states \"**The atomicity contract (decision 3)** requires a one-file change in fs.js that this ADR does not perform. Until that change lands (C.2), the contract is partially aspirational.\" Every one of those edits has landed (except the nginx row, which is obsolete per the §4 finding), and the \"C.2 cleanup tactical\" no longer exists. A reader today is told fs.js is …[truncated]","evidence":"All four live rows are DONE: packages/axoview-backend/src/adapters/fs.js `createFsAdapter().put` does tmp+rename (in-code comment: \"Atomicity contract (ADR 0010 Decision 3)\"); packages/axoview-backend/server.js `app.get('/healthz')` + `probeStorage` exist; Dockerfile has `HEALTHCHECK … /healthz` (comment: \"Container health probe (ADR 0010 Decision 8)\"); compose.yml and compose.dev.yml both carry `healthcheck: test: [\"CMD\",\"wget\",\"-qO-\",\"http://localhost:3001/healthz\"]`. The C.2 tactical lived …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":132,"adr":"0011","section":"Context, §1 forbidden-patterns, Consequences/Positive, Files-affected, See-also (7 links)","class":"A3a","claim":"ADR 0011 links to `docs/tactical/productization-audit.md` 7 times; the file does not exist. It was deleted in commit 6d33f1b ('chore(v1.1): dead-code wave — Track 0'). The B-9a inventory (2 conformant dialogs + 20 silent-failure surfaces S1–S20 + 5 intentional-silent I1–I5) and the B-9b retrofit row are the ADR's entire evidence base and its named vehicle for the deferred work in Consequences/Negative — all of it is now unreachable except through git history.","evidence":"`git ls-files | grep -i productization` → empty; `find . -iname '*productization*'` → no file. Deletion commit: `git log --diff-filter=D -- docs/tactical/productization-audit.md` → 6d33f1b. Live link count: `grep -c productization-audit docs/adr/0011-error-ux-contract.md` → 7. Corpus-wide, not 0011-only: ADRs 0008/0009/0010, docs/guidelines/testing.md, docs/manual-test-baseline.md, docs/workflow.md all still link it too.","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":133,"adr":"0018","section":"\"Resolved contract decisions (2026-06-14)\" preamble — tactical-plan link","class":"A3a","claim":"The ADR states \"the execution sequence is in the [tactical plan](../tactical/touch-pen-gesture-contract.md)\" — a dead link. docs/tactical/touch-pen-gesture-contract.md does not exist; it was deleted in commit 982cfd6 (\"docs: touch/pen wrap bookkeeping (ADR 0018 addendum + PLAN accuracy) (#46)\"), i.e. the wrap commit for this very ADR. The link has therefore been broken since the feature wrapped in June, not by the 2026-07-15 docs sweep.","evidence":"`ls docs/tactical/touch-pen-gesture-contract.md` → \"No such file or directory\"; `git log --oneline --diff-filter=D -- docs/tactical/touch-pen-gesture-contract.md` → `982cfd6 docs: touch/pen wrap bookkeeping (ADR 0018 addendum + PLAN accuracy) (#46)`. docs/tactical/ now contains only README.md and adr-code-audit.md. Note this is the ADR's ONLY dead link: `../guidelines/canvas-interaction.md` (cited 3×, for §3 and §8.1) resolves and both anchors are real (\"## 3. The load-bearing layout\", \"### 8.1 …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":134,"adr":"0019","section":"Context §, evidence links: `[cpuprofile-spawn-1000](../../perf-results/cpuprofile-spawn-1000.md)` and `[t2-design.md](..…","class":"A3a","claim":"ADR 0019 cites two perf-results files as the evidentiary basis for its RED-gate decision (the CPU profile attributing the spawn freeze, and the Iter 8/9 FINDINGs). Both files were DELETED and neither resolves on disk. The ADR's \"Evidence detail:\" pointer is a dead end — the reader cannot reach the evidence for the decision.","evidence":"`git ls-files perf-results/` returns exactly two files: baseline.md, decision-log.md. `git show --name-status 49c55b6` lists `D perf-results/cpuprofile-spawn-1000.md` and `D perf-results/t2-design.md`. AGGRAVATING: the SAME commit `49c55b6` also shows `M docs/adr/0019-canvas2d-node-render-layer.md` — the author edited this ADR and deleted its cited evidence in one commit. The deletion is by policy, not accident: ADR 0020's \"**Perf-results retention policy**\" declares DURABLE = decision-log.md + …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":135,"adr":"0020","section":"2026-07-02 addendum, prose ¶1 — link to the E-slice integration plan","class":"A3a","claim":"The 2026-07-02 addendum opens 'Productizing the labels/text-styling UX ([integration plan](../tactical/integration-productization-plan.md), slice E) …'. That file does not exist; the link is dead.","evidence":"docs/tactical/ contains ONLY README.md and adr-code-audit.md. `find . -iname '*productization-plan*' -not -path '*/node_modules/*'` → zero hits. Deleted at commit ce8f8f5 'docs(notes): end-of-session sync + wrap the labels/text-styling tactical'. The repo's own remediation pattern for this exact file is already applied elsewhere and was simply not applied here: PLAN.md line 908 reads 'The full deferred catalogue lives in the retired …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":136,"adr":"0020","section":"2026-07-02 addendum, closing prose — link to perf-results/e-slice-gate.md","class":"A3a","claim":"The 2026-07-02 addendum ends 'full numbers in [`perf-results/e-slice-gate.md`](../../perf-results/e-slice-gate.md)'. That file does not exist — and it was deleted BY THIS ADR'S OWN 2026-07-08 addendum, in the same commit that added it. The ADR contradicts itself within one file: line 119 links to the report as live, line 188 names it among the retired files.","evidence":"perf-results/ contains only baseline.md, decision-log.md, raw/. `find . -iname '*e-slice*' -not -path '*/node_modules/*'` → zero hits. Deleted at commit 49c55b6 'docs(adr): record WebGL2 substrate (ADR 0038) + retire perf-results scratch' — the same commit that wrote the 2026-07-08 addendum whose retention policy reads 'REGENERABLE (prune when the tier/gate closes; git history is the archive): per-scenario `*.md` reports … Applying this, 18 pre-T4 POC/scratch files (**incl. `e-slice-gate.md`** …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":137,"adr":"0020","section":"Acceptance criteria, bullet 2 — 'the `docs/testing.md` section'","class":"A3a","claim":"The second acceptance criterion lists the durable artifacts that must survive the T2 wrap as 'this ADR, `decision-log.md`, `baseline.md`, the harness code, the `docs/testing.md` section'. That path no longer exists — the file is docs/guidelines/testing.md. The same staged sweep fixed this exact path in the Implementation notes and missed it here, so the ADR now names the artifact two different ways.","evidence":"Path resolution: `docs/testing.md` → DEAD; `docs/guidelines/testing.md` → OK (git status shows the rename `docs/testing.md -> docs/guidelines/testing.md`, staged). Half-applied fix, visible in the staged diff for this ADR: `- [docs/testing.md](../testing.md)` → `+ [docs/guidelines/testing.md](../guidelines/testing.md)` at the Implementation-notes bullet, while the acceptance-criteria occurrence was not touched. The referenced section itself is alive and correct at its new home — …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":138,"adr":"0022","section":"Context — `[PanSettings](../../packages/axoview-lib/src/components/PanSettings/PanSettings.tsx)`","class":"A3a","claim":"The Context section hyperlinks PanSettings at a path this very ADR decided to delete; the link is dead on disk today.","evidence":"DOC: docs/adr/0022-canvas-pointer-interaction-model.md Context — \"[`PanSettings`](../../packages/axoview-lib/src/components/PanSettings/PanSettings.tsx) (Settings → \\\"Canvas\\\" tab)\". DISK: `ls packages/axoview-lib/src/components/PanSettings` → \"No such file or directory\"; the directory does not exist. The deletion is this ADR's own §6 (\"Delete `PanSettings` and the Settings → 'Canvas' tab pan section\"), corroborated by packages/axoview-lib/src/config/persistedSettings.ts (\"a …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":139,"adr":"0031","section":"Addendum 2026-06-30 (both the substrate table intro and the 'Confirmed by C1' paragraph) — evidence links","class":"A3a","claim":"ADR 0031's substrate addendum backs its binding decision with three links to perf-results scratch files; two of the three no longer exist on disk. The addendum cites `perf-results/e-slice-gate.md` twice ('full table in …' and '§2b') and `perf-results/pan.md` once. Both files were deleted; only `perf-results/baseline.md` survives. The load-bearing evidence for the ADR's only measured, binding decision is unreachable.","evidence":"On-disk check: `perf-results/` contains ONLY `baseline.md`, `decision-log.md`, and `raw/`. `perf-results/e-slice-gate.md` → MISSING; `perf-results/pan.md` → MISSING; `perf-results/baseline.md` → EXISTS (link resolves). Deletion commit: `git log --oneline --diff-filter=D -- perf-results/e-slice-gate.md perf-results/pan.md` → `49c55b6 docs(adr): record WebGL2 substrate (ADR 0038) + retire perf-results scratch`. The retirement is acknowledged in …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":140,"adr":"0034","section":"Context (\"Three S1 defects verified in the current code (see catalog)\", \"full record in the session artifact and `memory…","class":"A3a","claim":"ADR 0034 defers a known limitation to, and sources its Context from, a \"catalog\" that does not exist anywhere in the repository — nor has it EVER existed in the repository's git history. The identifier `I-21` appears in exactly one place on disk: line 158 of ADR 0034 itself. A reader told a defect is \"tracked as catalog item I-21\" has no way to find I-21, so the tracking is fictional and the deferral is unauditable. The same bullet's sibling Context citation, …[truncated]","evidence":"`grep -rn \"I-21\" . --include=*.md --include=*.ts --include=*.tsx` (excluding node_modules/dist/coverage) returns exactly ONE hit: docs/adr/0034-inline-canvas-text-editing-and-dual-scope-strip-formatting.md:158. `git log --all --oneline -S \"I-21\" -- '*.md'` returns only 18ea5af and 54e2eeb — both commits whose ONLY I-21-bearing file is ADR 0034 itself (verified with `git grep -l \"I-21\" <sha> -- '*.md'`), i.e. the catalog was never committed. `find . -iname \"*richtext-popup*\" -not -path …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":141,"adr":"0006","section":"§7 (LayersPanel citations)","class":"A3b","claim":"ADR 0007 §7's inline `file:line` anchors into LayersPanel.tsx have drifted and now point at unrelated code. The ADR says bulk layer assignment happens 'not just the dragged item at [LayersPanel.tsx:348]' — line 348 today is an unrelated ref assignment, and the actual `assignLayerToItems` call is ~89 lines away at 437.","evidence":"docs/adr/0006 line 76 links LayersPanel.tsx#L348 for the bulk `assignLayerToItems` claim; `grep -n assignLayerToItems packages/axoview-lib/src/components/LayersPanel/LayersPanel.tsx` resolves it to line 437 (`assignLayerToItems(target, refs);`), while line 348 is `itemClickImpl.current = { tryShiftRangeSelect, applyCtrlToggleSelect, mode, uiStateActions }` — the identity-stable-handler ref plumbing, unrelated to layer assignment. Same line 76 links LayersPanel.tsx#L122 for the …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":142,"adr":"0007","section":"Context §1 item 3 — \"`__perf_refactor_regression__/` (18 spec files + a README)\"","class":"A3b","claim":"ADR 0007's Context describes the behavioural-contract suite as \"18 spec files + a README\". The folder contains 47 spec files today, and — checked against the ADR's own authoring commit — it already contained 40 spec files on 2026-06-10 when the sentence was written. The count is not stale-by-drift; it was never accurate, and it now understates the suite by 2.6×. A reader sizing the guarded surface from this ADR gets a materially wrong picture, which also under-sells how far the folder has grown …[truncated]","evidence":"`packages/axoview-lib/src/__perf_refactor_regression__/` — `ls .../*.ts*` returns 47 test files today (README.md and the `fixtures/` dir excluded by the glob). At the ADR's only commit, `git ls-tree --name-only e1cd620 packages/axoview-lib/src/__perf_refactor_regression__/` returns 42 entries = 40 test files + `README.md` + `fixtures/`. Files added since include `annotationOpenReset.contract.test.ts`, `lasso.bulkPerf.test.ts`, `paste.bulkPerf.test.tsx`, `rectangleDrawTransform.modes.test.ts`, …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":143,"adr":"0008","section":"§4 and §8 (file:line citations)","class":"A3b","claim":"Both surviving `file:line` citations in the Decision section point at the wrong code today. §4 cites `types.ts:26` for the provider union (it now lives at line 66) and §8 cites `AppToolbar.tsx:179` for `serverStorageAvailable` (that line is now an unrelated share-menu handler).","evidence":"§4's `[packages/axoview-app/src/services/storage/types.ts:26](…#L26)`: line 26 today falls in the `PersistedDiagramBlob` field block (`created?: string` / `lastModified?: string` region); the cited union actually lives at types.ts:66 — `export interface StorageProvider { id: 'local' | 'google-drive'; … }`. §8's `[AppToolbar.tsx:179](…#L179)`: line 179 today is `const handleShareMenuOpen = (e: React.MouseEvent<HTMLElement>) => {` (a Drive-mode caret-menu handler); `serverStorageAvailable` …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":144,"adr":"0009","section":"Context bullet 2, §1 table (`/api/diagrams/*` row), §5, and the 'Files affected' list","class":"A3b","claim":"Four `file:line` citations no longer point at what they cite; one of them (`app.ts:43-45`, used twice as the evidence anchor for the 503 short-circuit) now lands inside an unrelated handler, so a reader who follows it is actively misled.","evidence":"(1) `app.ts:43-45` / `#L43` — cited in Context bullet 2 ('verified at [app.ts:43-45]') and in the §1 table's `/api/diagrams/*` row as the 503 short-circuit. `packages/axoview-worker/src/app.ts` line 43 today is `googleClientId: c.env.GOOGLE_CLIENT_ID || null,` — inside the `/api/config` handler. The real short-circuit is the `app.all('/api/*')` catch-all at the END of the file. (2) 'Files affected' → `packages/axoview-worker/src/app.ts#L29 — delete /api/storage/status handler`: line 29 is …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":145,"adr":"0010","section":"Context (fs.js:45), §4 (server.js:46-79, nginx.conf:4-5), §5 (fs.js:80-84), Files affected (fs.js#L45, nginx.conf#L4)","class":"A3b","claim":"Five of the ADR's six pinned `file:line` citations no longer resolve to the thing they cite. Per this audit's own rule 2 (\"every file:line anchor in this repo's retired tactical docs had drifted within a month\"), these are exactly that failure mode — and they are load-bearing, since §5's citation is offered as \"the reference\" implementation of the reserved-key list.","evidence":"Verified each anchor against the working tree today. DRIFTED: fs.js:45 (cited twice — Context for \"uses `fs.writeFile` directly\", and Files affected for \"tmp-file + rename for `put`\") is `const name = path.basename(filePath);`, not a write call. fs.js:80-84 (cited as \"the reference\" for the reserved-key exclusion) is `if (e.code === 'ENOENT') return [];` inside `list()`'s catch — the real exclusion is the `entry.name === 'folders.json' || …` block in `listDiagramMeta`. server.js:46-79 (cited …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":146,"adr":"0011","section":"§1 carve-out, §5 (precedent citation), §7, Files-affected — all `file:line` anchors","class":"A3b","claim":"6 of the ADR's 7 `file:line` citations no longer resolve to the thing cited; they now land on unrelated code. The underlying claims are all still TRUE — only the anchors rotted — so this is rot, not a truth defect. Exactly the failure mode the audit protocol warns about ('every file:line anchor in this repo's retired tactical docs had drifted within a month').","evidence":"Verified today against the working tree: (1) `DiagramLifecycleProvider.tsx#L83-L84` cited as `readonlyLoadFailed`+`clearReadonlyLoadFailed` → L83-84 is now a load-state type union; the real symbols are at L105-106 (interface) / L251-252 (useState+useCallback). (2) `App.tsx#L377-L383` cited as the ReadonlyLoadErrorDialog mount → now a file-explorer `<Box>`; the real mount is ~L536-543. (3) `DiagramLifecycleProvider.tsx#L417-L422` cited as the public-share toast → now a `fileExplorerOpen` …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":147,"adr":"0018","section":"Context §bullets 1/2/4 + Decision 5 + Implementation notes — `file:line` anchors","class":"A3b","claim":"Five of the ADR's seven line-pinned citations no longer resolve to the thing they name, and two of them now resolve to code that means the OPPOSITE of the label. `Renderer.tsx#L221-L231` is cited twice as \"the inner `canvas-interactions` Box\" (the thing you must NOT bind to) but today L221-231 contains `uiStateActions.setRendererEl(containerRef.current)` — the container, i.e. the thing you MUST bind to. `Renderer.tsx#L167-L181` is cited in the Implementation notes as the container (\"Pointer …[truncated]","evidence":"Verified each anchor today against the working tree. packages/axoview-lib/src/components/Renderer/Renderer.tsx: `canvas-interactions` lives at the `data-axoview-id=\"canvas-interactions\"` Box (~L547, file is 610 lines); the container Box (`containerRef`, `data-testid=\"axoview-canvas\"`) starts ~L479; `setRendererEl` is at L222. packages/axoview-lib/src/interaction/modes/Cursor.ts: the drag-start threshold is now `const hasDragged = exceedsTapSlop(...)` inside `mousemove` (~L579), `hasMovedTile` …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":148,"adr":"0022","section":"Body — two sections numbered \"4\"","class":"A3b","claim":"ADR 0022 contains two different sections numbered 4: Decision \"### 4. The details panel survives an in-panel text-drag (#6)\" and, inside the 2026-06-25 addendum, \"### 4. Inline-rename commit contract (canvas contentEditable editors)\". Code and sibling docs already cite \"ADR 0022 §4\" for BOTH, so the anchor resolves to the wrong section roughly half the time.","evidence":"DOC: docs/adr/0022-canvas-pointer-interaction-model.md — heading \"### 4. The details panel survives an in-panel text-drag (#6)\" in the Decision section, and heading \"### 4. Inline-rename commit contract (canvas contentEditable editors)\" under \"## Addendum — 2026-06-25\". CODE CITING §4 = text-drag: packages/axoview-lib/src/interaction/modes/Cursor.ts, `mouseup` — \"MQA #16 / ADR 0022 §4 (#6): the press did not land on the canvas…\". CODE CITING §4 = inline-rename: …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":149,"adr":"0022","section":"Implementation notes + Context — all six `file:line` citations","class":"A3b","claim":"Every file:line anchor in ADR 0022 has drifted; not one resolves to the thing it names. Three are in Context (describing the pre-decision state) and three are in Implementation notes (naming the post-decision landing sites).","evidence":"Verified against the working tree today (2026-07-15) with sed; Cursor.ts is 669 lines and useInteractionManager.ts is 1588, so every anchor still resolves to SOME line — just the wrong one. (1) \"`resolveClickSelection` ([Cursor.ts:465])\" → `resolveClickSelection` is defined at Cursor.ts:535; line 465 sits inside `toggleConnectorGroupSelection`. (2) \"[Cursor.ts:575]\" (the selectedIds→itemControls auto-mirror) → 575 is inside `mousemove`'s drag-slop detection. (3) \"the existing MQA-#16 guard at …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":150,"adr":"0022","section":"Addendum 2026-06-21, bullet (a) — \"This strikes the earlier 'double-click → QuickAdd' promise in §3\"","class":"A3b","claim":"The addendum strikes the QuickAdd promise but cites the wrong section: the promise lives in the §1 mouse-model table (line 33), and §3 contains no QuickAdd text. Consequence — §1's row \"Double-click empty canvas | QuickAdd popover (unchanged)\" still stands unmarked, and the code no-ops there.","evidence":"DOC: docs/adr/0022-canvas-pointer-interaction-model.md line 33 (inside the §1 table) is the ONLY statement of the promise: \"| Double-click empty canvas | QuickAdd popover (unchanged). |\". Line 84 (the addendum) says \"…strikes the earlier 'double-click → QuickAdd' promise in §3\". `grep -in \"quickadd\"` on the file returns exactly those two lines — §3 has zero. §3 verbatim concerns only the panel: \"`selectedIds.length === 1` drives highlight + action bar only. The details panel (`itemControls` …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":151,"adr":"0023","section":"§1 — Additive data model, bullet 3: \"Zod `modelItem` schema extends with these optionals\"","class":"A3b","claim":"§1 names the wrong Zod symbol. `modelItemSchema` is a real, distinct schema in this codebase (the model-level item: name/label/description/notes/icon) and it does NOT carry offset/snap/collides — correctly, since those are placement concerns. The fields actually landed on `viewItemSchema` (plus `rectangleSchema` / `textBoxSchema`). A wrong-symbol citation in the Decision section could send a future implementer or agent to edit the wrong schema.","evidence":"packages/axoview-lib/src/schemas/modelItems.ts — symbol `modelItemSchema`: fields are id/name/label/description/notes/headerLink/link/icon; `grep -n \"offset|snap|collides\" packages/axoview-lib/src/schemas/modelItems.ts` returns NOTHING. The trio lives on packages/axoview-lib/src/schemas/views.ts `viewItemSchema`, packages/axoview-lib/src/schemas/rectangle.ts `rectangleSchema`, packages/axoview-lib/src/schemas/textBox.ts `textBoxSchema` — `grep -rln \"collides\" …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":152,"adr":"0025","section":"Context, bullet #9 — the `[svgOptimizer.ts:386](../../packages/axoview-lib/src/utils/svgOptimizer.ts#L386)` link","class":"A3b","claim":"ADR 0025 cites `btoa(unescape(encodeURIComponent(...)))` as living at svgOptimizer.ts line 386 and links to `#L386`; today line 386 is the `export async function optimizeSvgDataUrl(svgDataUrl: string)` declaration, and the cited expression exists nowhere in the file as live code — it was removed by the very fix this ADR ordered. A reader clicking the anchor lands on an unrelated symbol.","evidence":"packages/axoview-lib/src/utils/svgOptimizer.ts (421 lines). Line 386 = `export async function optimizeSvgDataUrl(svgDataUrl: string): Promise<string> {` (verified by sed 380–392 TODAY, per the audit's 'verify the line resolves today' rule). The only occurrence of the cited pattern is line 343, inside a comment on `utf8ToBase64`: 'The previous `btoa(unescape(encodeURIComponent(s)))` path threw' — i.e. historical prose, not the sink. The live encoder is `utf8ToBase64` (declared line 349, …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":153,"adr":"0027","section":"Context § — `[useInteractionManager.ts:960](../../packages/axoview-lib/src/interaction/useInteractionManager.ts#L960)`","class":"A3b","claim":"The Context section anchors its \"right-click today opens the details panel + NodeActionBar (`onContextMenu`)\" citation at `useInteractionManager.ts:960`. That line number no longer resolves to anything related: line 960 today is pinch-zoom clamp arithmetic. `onContextMenu` is defined ~177 lines away. The deep-link lands a reader in unrelated code.","evidence":"packages/axoview-lib/src/interaction/useInteractionManager.ts — line 960 today reads `const newZoom = Math.max(` inside the two-finger pinch-zoom block (surrounded by `ts.pinchLastDistance`, `MIN_ZOOM`/`MAX_ZOOM`, and the comment \"Anchor the world point under the OLD centroid to the NEW centroid at the new zoom\"). The cited symbol `onContextMenu` is declared at line 783 (`const onContextMenu = useCallback(`) and registered at line 1552 (`window.addEventListener('contextmenu', onContextMenu)`) — …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":154,"adr":"0028","section":"Context ¶2, lesson 1 (the two file:line citations)","class":"A3b","claim":"Both `file:line` anchors carrying the ADR's central 'the method works' evidence have drifted and no longer resolve to the cited code. `NodesCanvas.tsx#L127` is cited for 'a caption rendering literal &nbsp;'; line 127 today is `img.naturalWidth > 0 ? (img.naturalHeight / img.naturalWidth) * w : w` inside the `iconHeight` helper — unrelated. `PlaceIcon.ts#L31` is cited for 'click-to-arm icon placement landing under the panel'; line 31 today is the closing brace of the `mousedown` handler. These …[truncated]","evidence":"packages/axoview-lib/src/components/SceneLayers/Nodes/NodesCanvas.tsx (810 lines) — L127 = `iconHeight`; the symbol that actually owns the cited behaviour is `decodeHtmlEntities`, imported from `src/utils/htmlToPlainText`, whose header comment self-identifies: 'caption showed a literal `&nbsp;` on the default node layer (ADR 0019 / 0028)'. packages/axoview-lib/src/interaction/modes/PlaceIcon.ts (118 lines) — L31 = `}`; the symbol carrying the cited fix is the `mouseup` handler at L32, commented …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":155,"adr":"0030","section":"Context — the ux-principles line citations (\"lines 293, 295\", \"line 300\", \"line 191\")","class":"A3b","claim":"Three of ADR 0030's Context citations pin line numbers in ux-principles.md that no longer resolve. Two land on blank lines, one lands on the wrong heading. Verified against the working tree today, 2026-07-15.","evidence":"docs/adr/0030-docked-style-controls-strip.md Context cites \"[ux-principles §5.1](../guidelines/ux-principles.md) (lines 293, 295) and [§5.2](../guidelines/ux-principles.md) (line 300)\" and \"a §2.4 blockquote (line 191)\". Resolved today against docs/guidelines/ux-principles.md: lines 191, 293 and 295 are all **empty**; line 300 is `## 5. Item type parity` (the parent heading, not §5.2). The real anchors have drifted downward — `### 2.4 The context menu is the *sole* per-item command surface` is …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":156,"adr":"0032","section":"Amendment (2026-06-30) → New decision §3","class":"A3b","claim":"ADR 0032 §3 enumerates the render sites as \"Canvas2D `NodesCanvas`, the DOM `Node` overlay, the `NodeLabelHitLayer` chip-measure\". `NodesCanvas` is no longer a Canvas2D layer — WebGL2 is the sole bulk render substrate and the Canvas2D fallback was removed (ADR 0038, PR #63). The decision itself is fully intact (all three sites exist and all three read `label ?? name`); only the substrate adjective has drifted.","evidence":"packages/axoview-lib/src/components/SceneLayers/Nodes/NodesCanvas.tsx — the file's own header comment reads \"NodesCanvas — WebGL2 INSTANCED bulk draw of the node layer … WebGL2 is required — it is the sole render substrate (Phase C). A browser without it is gated upstream by the Renderer's WebGLUnsupportedScreen\"; its imports are `createSpriteBatch` from src/webgl/glSpriteBatch, `attachContextLossRecovery` from src/webgl/contextLoss, `rasterizeNodeChip` from src/webgl/itemRaster. The §3 …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":157,"adr":"0033","section":"Context table, row 1 — the `views.ts:17–19` citation","class":"A3b","claim":"ADR 0033's Context table cites [`views.ts:17–19`](../../packages/axoview-lib/src/schemas/views.ts) as the location of `labelBold` / `labelItalic` / `labelStrikethrough`. In the working tree TODAY, views.ts lines 17–19 are `labelColor` / `labelBold` / `labelItalic` — the cited trio actually sits at lines 18–20. The anchor drifted by exactly one line.","evidence":"packages/axoview-lib/src/schemas/views.ts, symbol `viewItemSchema`. `grep -n` today: 17=`labelColor: z.string().optional(),` 18=`labelBold:` 19=`labelItalic:` 20=`labelStrikethrough:`. `sed -n '17,19p'` returns labelColor/labelBold/labelItalic — NOT the cited trio. Drift is provable, not assumed: at the ADR's own authoring commit `cd26122`, `git show cd26122:packages/axoview-lib/src/schemas/views.ts | sed -n '14,22p'` puts labelBold/labelItalic/labelStrikethrough at exactly 17/18/19 — the …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":158,"adr":"0033","section":"Context table, row 4 — \"Text box / label | `isBold` / `isItalic` / `isStrikethrough` | textBox.ts\"","class":"A3b","claim":"The Context table routes a reader to `textBox.ts` for `isStrikethrough`. That field is no longer in textBox.ts — it moved to `label.ts` when ADR 0031 extracted the floating Label. textBoxSchema today has isBold/isItalic/isUnderline and NO strikethrough, so the row's link no longer points at the cited thing.","evidence":"packages/axoview-lib/src/schemas/textBox.ts, symbol `textBoxSchema`: `grep -n 'isBold|isItalic|isStrikethrough|isUnderline'` returns only 44=isBold, 45=isItalic, 46=isUnderline — no isStrikethrough. The field lives in packages/axoview-lib/src/schemas/label.ts, symbol `labelSchema` (isStrikethrough at line 34). Move is git-provable: `git log -S'isStrikethrough' -- packages/axoview-lib/src/schemas/textBox.ts` returns `3255bf9 feat(label): extract floating Label into a first-class entity (ADR …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":159,"adr":"0035","section":"Context, bullet 1 — the `/api/config` link `../../packages/axoview-worker/src/app.ts#L38`","class":"A3b","claim":"ADR 0035's Context links '/api/config' to packages/axoview-worker/src/app.ts#L38. Line 38 today is `app.use('/api/*', authMiddleware());` — the auth middleware registration, not the config route. The `app.get('/api/config', ...)` handler the sentence is about starts at line 40. The anchor drifted by two lines; the linked claim itself (that /api/config returns googleClientId and driveScopes) is still TRUE, so this is pure citation rot, not a truth defect.","evidence":"packages/axoview-worker/src/app.ts — verified today by numbering the file: L37 `);`, L38 `app.use('/api/*', authMiddleware());`, L39 blank, L40 `app.get('/api/config', (c) =>`, L43 `googleClientId: c.env.GOOGLE_CLIENT_ID || null`. Symbol `app.get('/api/config')` resolves at L40, not L38.","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":160,"adr":"0039","section":"Context (¶ on the data model) — `resolveHex` citation","class":"A3b","claim":"ADR 0039 cites `resolveHex` at [`TopBarStyleControls.tsx#L810`]. The symbol is LIVE and retained by this ADR's own §2 (\"`resolveHex` stays\"), but it sits at line 675 today; L810 resolves to a bare `});`. Unlike the ADR's other drifted anchors, this one points at a still-current symbol the ADR discusses in the present tense, so a reader following it lands on nothing.","evidence":"`packages/axoview-lib/src/components/TopBarStyleControls/TopBarStyleControls.tsx` — `resolveHex` is declared at line 675: `const resolveHex = (presetId?: string, customColor?: string) => customColor || colors.find((c) => c.id === presetId)?.value;`. `sed -n '810p'` on that file today returns ` });`. Secondary, weaker: the ADR's `StripButton` anchor `#L182` is 3 lines off (`const StripButton = ({` is at line 179; L182 is the `icon,` prop) — still inside the symbol, so arguably harmless. …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":161,"adr":"0040","section":"Context (index.html:81), § Ripple / consequences (App.tsx:95, App.tsx:51), Implementation notes (rsbuild.config.ts:22-27…","class":"A3b","claim":"ADR 0040 pins five `file:line` citations with GitHub-style `#L` anchors. Four no longer resolve to the thing they cite — the R1 work this ADR authorized is itself what moved the code out from under them. The worst is the Context anchor: `[index.html:81](…/public/index.html#L81)` is cited as evidence that the file \"ships only a decorative splash and a `<noscript>`\", but R1 turned that exact file into the marketing landing and relocated the splash to app-shell.html. A reader who follows the link …[truncated]","evidence":"Each anchor resolved against the working tree TODAY (rule 2 compliance): (1) `index.html:81` cited as \"ships only a decorative splash and a `<noscript>`\" → packages/axoview-app/public/index.html line 81 is `.hero-cta { display: flex; flex-wrap: wrap; gap: 14px; align-items: center; }`. The described content is now in packages/axoview-app/app-shell.html (symbols `#ax-splash` / `.ax-splash-inner` / `<noscript>You need to enable JavaScript to run the Axoview editor.</noscript>`) — a different …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":162,"adr":"0042","section":"Body citations: DiagramLifecycleProvider.tsx#L462-L513, #L202, #L518-L575; AppToolbar.tsx#L336, #L72-L128, #L400-L463","class":"A3b","claim":"Six of the ADR's eleven `file:line` deep-links no longer resolve to the thing they name — every anchor into DiagramLifecycleProvider.tsx and AppToolbar.tsx drifted (mostly because the ADR's own implementing commit inserted code above them). A reader following them lands on unrelated code.","evidence":"Verified today against the working tree. (1) \"the public-snapshot loader ([DiagramLifecycleProvider.tsx:462-513](…#L462-L513))\" → L462-513 is the `loadLastOpenedData`/`applyLoadedDiagram` region; the public-snapshot loader effect (`if (!isPublicShareUrl || !shareUuid) return;`) is at ~L538-580. (2) \"`isReadonlyUrl` ([DiagramLifecycleProvider.tsx:202](…#L202))\" → L202 is the `createContext` call; the `const isReadonlyUrl =` symbol is at ~L237. (3) \"the existing owner-readonly loader effect …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":163,"adr":"0002","section":"Acceptance criteria, bullet 3 (\"Unit test: ... assert the loaded version wins\")","class":"A3c","claim":"ADR 0002 lists as a required acceptance criterion a unit test asserting that a model icon sharing an id with a bundled fixture but carrying different metadata wins the merge. That test does not exist, and the test file explicitly and deliberately declines to write it — the rationale for the deliberate gap lives only in a code comment, never in the ADR.","evidence":"packages/axoview-lib/src/utils/__tests__/leanSave.test.ts — inside `describe('mergeBundledFixtures (ADR 0002)')` the third test is replaced by a comment: \"The 'override wins over bundled fixture' case is intentionally not tested here. Per ADR-0002 (2026-05-01), `bundledFixtures` is empty by design — the consuming app (PWA) injects @isoflow/isopacks instead. With no bundled defaults, the override path is unreachable in production. If bundled fixtures are ever re-added, re-introduce a test that …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":164,"adr":"0002","section":"Acceptance criteria, bullet 4 (\"Integration test: export-then-import a diagram with a custom icon; assert the side dock …","class":"A3c","claim":"ADR 0002 names an integration test that export-then-imports a diagram with a custom icon and asserts the side dock shows both the custom icon and all defaults. No test at the integration/e2e layer does this, and the 'all defaults' half of the assertion has been vacuous since bundledFixtures became empty on 2026-05-01.","evidence":"packages/axoview-e2e/tests/import-export-json.spec.ts — `test.describe('Import / Export JSON — J7 + J8')` contains only 'J7: imports a JSON diagram from the empty-state Import button' and 'J8: exports the current diagram as JSON; file downloads round-trip'; J8 places stock icons via `placeIcon` and asserts structural keys only, with an inline comment conceding 'The bundled-fixtures array is currently empty (see lib/fixtures/icons.ts), so `icons` survives intact'. …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":165,"adr":"0003","section":"Acceptance criteria — the three strip unit tests","class":"A3c","claim":"ADR 0003's strip unit-test criteria are satisfied by tests that cannot fail. Because `bundledFixtures` is `[]`, the named tests exist but assert nothing about the behaviour the criteria describe — and the override test is structurally broken (it spreads `undefined`).","evidence":"packages/axoview-lib/src/utils/__tests__/leanSave.test.ts: (1) 'drops icons that are pure duplicates of bundled fixtures' constructs `baseModel(bundledFixtures.map((i) => ({ ...i })))`, which is `baseModel([])`, then asserts `stripped.icons` `toEqual([])` — vacuously true for any implementation. (2) 'preserves overridden defaults (same id, changed name)' does `const overridden = { ...bundledFixtures[0], name: 'User-renamed' }`; `bundledFixtures[0]` is `undefined`, so `overridden` is `{ name: …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":166,"adr":"0003","section":"Acceptance criteria — \"Round-trip test: session-save then session-load produces a model whose merged `icons` array is el…","class":"A3c","claim":"ADR 0003 names a session-storage round-trip test as an acceptance criterion. No such test exists — nothing anywhere exercises session-save followed by session-load and compares the merged icons array.","evidence":"The nearest artifact, packages/axoview-lib/src/utils/__tests__/leanSave.test.ts `describe('round-trip: strip then merge')`, is a pure-function composition (`mergeBundledFixtures(stripDefaultIcons(original))`) with no sessionStorage and no provider involved — and it is itself near-vacuous (with `bundledFixtures` = `[]` it proves only that `customIcon` survives two no-ops). packages/axoview-app/src/services/storage/__tests__/LocalStorageProvider.test.ts drives `saveDiagram` through a mocked …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":167,"adr":"0005","section":"§Acceptance criteria → \"Unit / component\" (all three bullets)","class":"A3c","claim":"All three of §Acceptance criteria's \"Unit / component\" bullets name tests that do not exist: (1) \"`SettingsDialog` test asserts About + Diagnostics tabs render\" — there is no SettingsDialog unit test at all, and the only test covering those tabs asserts the OPPOSITE (no Diagnostics tab); (2) \"`AppToolbar` snapshot/render test in both modes asserts the four-group structure\" — no AppToolbar unit/component test exists anywhere; (3) \"Storage-gauge test continues to pass (no behavior change)\" — no …[truncated]","evidence":"(1) packages/axoview-lib/src/components/SettingsDialog/ contains exactly two files — SettingsDialog.tsx and AboutTab.tsx, no __tests__ dir. The only asserting surface is packages/axoview-e2e/tests/dialogs.spec.ts (J16), which is an e2e not a unit test and whose tab-list assertion is \"the regression guard against accidental re-add\" of the Diagnostics tab. (2) `grep -rln \"AppToolbar\"` over packages/axoview-app/src + packages/axoview-lib/src returns only source files (App.tsx, AppToolbar.tsx, …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":168,"adr":"0018","section":"Acceptance criteria (both the 2nd unit test and the touch manual-verification bullet)","class":"A3c","claim":"The Acceptance criteria section was never revised after the 2026-06-14 (B) revision killed Decision 4, so it still gates the ADR on the superseded tap-to-place model. It names \"**Unit test:** the SELECT → GRAB → PLACE state machine moves a node to the tapped tile and a `pointercancel` mid-`GRAB` leaves the node at its origin\" — a test that does not exist and cannot, since neither the state machine nor the abort semantics shipped. Worse, the manual-verification bullet instructs a verifier to …[truncated]","evidence":"ABSENT test: the only ADR-0018 unit test is packages/axoview-lib/src/__perf_refactor_regression__/touchGesture.test.ts, whose docstring concedes the change — \"Direct manipulation (Option A) decides by what is under the finger at pointerdown and forwards the gesture to the existing mouse modes, so the rest of the touch machine is DOM/pointer-capture dependent and covered by the e2e touch project.\" CONTRADICTED manual step: packages/axoview-lib/src/interaction/useInteractionManager.ts …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":169,"adr":"0022","section":"Acceptance criteria — \"e2e (new `details-interaction.spec`)\"","class":"A3c","claim":"ADR 0022's Acceptance criteria name a new e2e spec `details-interaction.spec` covering five behaviours; no such spec exists anywhere in the repo, and two of its five behaviours (double-click opens the panel; name-field text-drag crossing the panel keeps it open) have no e2e coverage at all.","evidence":"ABSENT: `ls packages/axoview-e2e/tests/` (74 specs) has no `details-interaction*`; repo-wide `grep -rni \"details-interaction|detailsInteraction|details_interaction|details interaction\"` returns exactly ONE hit — docs/adr/0022-canvas-pointer-interaction-model.md itself. Where the behaviours DID land: right-drag-pan + right-tap-menu → UNIT, packages/axoview-lib/src/interaction/__tests__/usePanHandlers.test.ts (\"right-drag beyond threshold → enters PAN mode\", \"right-tap on empty canvas (CURSOR) …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":170,"adr":"0026","section":"Acceptance criteria — e2e bullet","class":"A3c","claim":"ADR 0026's e2e acceptance criterion says \"**e2e (extend `rectangle-ops`):** edge handle resizes one dimension in iso **and** 2D; corner handles still work.\" The edge-handle e2e tests were never added to `rectangle-ops.spec.ts` — they live in `rectangle-move-resize.spec.ts`. The BEHAVIOUR is fully covered (including the iso-and-2D matrix), so this is a misdirected pointer, not missing coverage: anyone verifying the criterion at the named file finds nothing.","evidence":"packages/axoview-e2e/tests/rectangle-ops.spec.ts — the file exists but contains exactly one describe, `Rectangle ops — Track 5h`, with the single test `5h rectangle: Delete on a selected rectangle removes it; other rectangles survive`; grepping it for `edge|anchor|TOP|2D|isometric|canvas-transform-anchor` returns ZERO hits. The actual coverage is packages/axoview-e2e/tests/rectangle-move-resize.spec.ts — describe `Rectangle edge-midpoint resize — ADR 0026 (T7)` (its doc comment literally …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":171,"adr":"0027","section":"Acceptance criteria — \"**e2e:** … 'Details…' opens the same panel as double-click\" and \"'Unsnap' + 'Disable collision' p…","class":"A3c","claim":"Two of the six e2e acceptance criteria have no corresponding test at ANY level. (a) \"'Details…' opens the same panel as double-click\" — no test drives the menu's Details… entry, let alone asserts parity with the double-click path. (b) \"'Disable collision' present\" — no test asserts the Disable-collision entry exists or clicks it; only its sibling Unsnap is covered. There is also no unit test file for the component at all, so nothing catches the gap at a lower tier. (The other four criteria ARE …[truncated]","evidence":"Zero component tests: `find packages/axoview-lib/src/components/CanvasContextMenu -type f` returns exactly one file — `CanvasContextMenu.tsx`. No `__tests__/` sibling, in contrast to e.g. packages/axoview-lib/src/components/NodeActionBar/__tests__/NodeActionBar.helpers.test.ts. E2E gap (a): grepping the e2e suite for `Details…|Details\\.\\.\\.|getByRole\\('menuitem'` across packages/axoview-e2e/tests/ yields exactly ONE menuitem interaction in the whole suite — …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":172,"adr":"0032","section":"Acceptance criteria — \"Unit:\" bullet, final clause (and the supporting rationale in Decision §3 / the `useInitialDataMan…","class":"A3c","claim":"ADR 0032's Unit acceptance criterion says `description` is \"absent from the working model post-fold but **preserved on export round-trip**\". No test asserts the second clause anywhere in the repo, and for the editor's own JSON-export path the claim is false: the fold deletes `description` before the store is hydrated, and every editor serializer reads the store, so a node `description` cannot survive an open→export round-trip.","evidence":"Mechanism: `foldNodeDescription` (packages/axoview-lib/src/utils/foldNodeDescription.ts) ends with `delete next.description`, and it runs at the load chokepoint BEFORE validation/hydration — `rawData.items = asArray(rawData.items).map(foldNodeDescription).map(seedNodeLabel)` in packages/axoview-lib/src/hooks/useInitialDataManager.ts. The editor export then reads the folded store: `exportAsJSON(buildSaveData() as Model)` in packages/axoview-app/src/providers/DiagramLifecycleProvider.tsx, and …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
{"row":173,"adr":"0036","section":"Acceptance criteria — bullet 1 (\"Unit test: full provider suite with mocked fetch\")","class":"A3c","claim":"ADR 0036's first acceptance criterion enumerates sub-criteria \"rename/move/trash/untrash\", \"root-folder discovery (marker found / not found / stale cache)\" and \"503 ×3 → error notification with Retry\"; the provider suite that satisfies this criterion contains NO test for untrash (restoreDiagram), NO test for Drive renameDiagram/renameFolder, NO stale-cache discovery test, and its give-up test asserts no notification at all.","evidence":"packages/axoview-app/src/services/storage/__tests__/GoogleDriveProvider.test.ts — full test roster is: listDiagrams mapping, loadDiagram, saveDiagram, createDiagram, `deleteDiagram trashes (never hard-deletes)`, `moveItem swaps parents`, `401 forces SESSION_EXPIRED and throws`, `retries a transient 503 then succeeds`, `gives up after exhausting retries`, permanent-403, rate-limit-403, `root discovery: adopts an existing marker folder`, `root discovery: no marker → not configured`, nextPageToken …[truncated]","proposedFix":"*(see the run journal)*","verdict":"UNVERIFIED"}
