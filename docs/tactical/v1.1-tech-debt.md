# Tactical — v1.1 Tech-Debt Cleanup (post-M10 hygiene)

> **Read first:**
> - [`docs/workflow.md`](../workflow.md) — canonical session cadence + skill decision table.
> - [`docs/technical-review-2026-05.md`](../technical-review-2026-05.md) — the frozen post-v1.0.0 review artifact; THIS tactical's source-of-truth for what needs cleanup.
> - [`docs/architecture.md`](../architecture.md) + [`docs/deployment.md`](../deployment.md) + [`docs/ux-principles.md`](../ux-principles.md) — living references.
> - ADRs touched by this cleanup: [0005 toolbar/dock](../adr/0005-toolbar-and-dock-layout-contract.md) (MainMenu deletion), [0008 naming](../adr/0008-naming-convention.md) (CoordsUtils + Sidebars), [0009 deployment](../adr/0009-deployment-topology.md) (env-var truth + dual wrangler), [0010 session backend](../adr/0010-session-backend-contract.md) (single-tenant docs callout), [0011 error UX](../adr/0011-error-ux-contract.md) (B-9b silent-failure catalogue).
> - The retired productization-audit tactical's locked decisions list (commit `fdfa3be` wrap; consult via git if needed).
>
> **Status:** Scaffolded 2026-05-23 · Owner: Igor · Last updated 2026-05-23 · Track 4 partially complete (CodeQL toggle + branch protection on master done 2026-05-23; item 4 single-tenant callout folded into Track 1 PR); **Track 0a-0h shipped 2026-05-23 on `master`** as PR #3 squash `6d33f1b`; **Track 1 shipped 2026-05-23 on `integration`** (7 commits; B1 `1933091` → deployment callout `f2b279a`); Track 2 awaits Decision Point 2.
>
> This is a **short-lived working doc.** Delete it after the cleanup tracks complete and tests pass. The durable record is the commit history + any ADRs spawned during Track 2 decisions. New tacticals spawned from this one (Tracks 5/6/7) link back here via "Read first".

---

## Session startup checklist

1. Read this file fully.
2. Read [`docs/technical-review-2026-05.md`](../technical-review-2026-05.md) §7.8 (cross-package observations) + §11 (open known issues) + §8d (test:source ratio) — these are the catalogue this cleanup executes against.
3. Skim the linked ADRs only as needed per track.
4. Use `TodoWrite` to track sub-tasks within the active track.
5. Mark `[x]` as work completes.
6. On completion of a track, post-flight `npm run build` + `npm test` + `npx eslint .` must all be green before commit.

---

## Goal

End state: a codebase that's **sparkling clean** post-v1.0 — confirmed dead code deleted, audit claims made true (or honestly corrected), productization decisions resolved, external GitHub Settings actions completed. The artifact at [`docs/technical-review-2026-05.md`](../technical-review-2026-05.md) names the gaps; this tactical closes them.

**Why now:** the productization audit shipped v1.0.0 with a substantial deferred-debt tail. The "deferred-by-design" framing was honest but easy to lose the cumulative weight of. The technical review artifact made the weight visible; this tactical executes the cleanup before the debt metastasizes into v1.1+ work.

**Explicit non-goals:**
- New features. v1.1 may bring features; THIS tactical does not.
- Architectural refactors of large surfaces (Track 6 catalogues them; defers them).
- Closing the server-runtime test gap (Track 5 catalogues it; spawns its own tactical).
- Closing operational/security gaps that need real-user signal first (Express structured logging, etc.).

---

## Scope

### In scope

- All Track 0 dead-code deletions (one PR, multi-commit).
- All Track 1 code-truth fixes (nginx headers, /api/storage/status doc strip, dead env vars, ErrorBoundary URL, isPublicRoute alignment).
- Track 2 productization decisions executed once user answers (4 decisions; see Decision Points below).
- Track 3 artifact-corrections appendix (preserves freeze convention).
- Track 4 user-only external actions (CodeQL toggle + branch protection + repo metadata + single-tenant docs callout).
- Tracks 5/6/7 are **catalogued here** so nothing is lost; **execution lives in spawned tacticals** when each is authorized.

### Out of scope

- Server-runtime test foundation (Track 5a) — large workstream, own tactical.
- Refactor of any single file > 300 LOC (Track 6) — own tactical per refactor.
- New ADR-worthy workstreams (Track 7 — XSS surface, a11y baseline, telemetry/privacy, migration policy, SBOM/SRI, etc.).
- B-9b silent-failure retrofit (S2-S20) — opportunistic per-PR per audit closeout; not a sweep here.
- Lib's published-shape-but-private posture cleanup — only the dead Next.js docs scaffold deletion is in Track 0a; deeper cleanup is Track 7.

---

## Locked decisions

| # | Decision | Why |
|---|---|---|
| 1 | The technical-review-2026-05.md artifact is **frozen**. Corrections land via Track 3 appendix, not in-place edits. | Preserves the snapshot's reviewer-visible chronology + the audit-wrap convention that the artifact was the durable record. |
| 2 | Execution order is **Track 4 → 0 → 1 → 2 → 3**, then 5/6/7 as separate tacticals when authorized. | Track 4 (branch protection) must guard subsequent merges; Track 0 is dead-code (most reversible); Track 1 makes audit claims true (no behaviour change); Track 2 sequences user decisions one at a time; Track 3 is doc-only. |
| 3 | Track 0 ships as **one PR with multi-commit clusters** (0a, 0b, 0c, ...). | Per-cluster commits = clean reverts if any cluster surprises in CI. |
| 4 | New tacticals (Tracks 5/6/7) are spawned only when the user explicitly authorizes them. | Avoids scope creep; cleanup wave stays bounded. |
| 5 | The B-9b silent-failure retrofit (S2-S20) stays **deferred-by-design** per the audit closeout. NOT bundled into this cleanup. | The audit's "opportunistic per-PR" framing was correct; a sweep would re-litigate per-row scenarios that benefit from being touched alongside their owning feature work. |
| 6 | Sibling tacticals `e2e-suite-rewrite.md` + `git-automation-hardening.md` get deleted in Track 0e per convention memory's lifecycle rule (delete after merge + tests pass). | Convention adherence; their durable record is the commits they shipped. |

---

## Decision points pending user input

These block execution starts. Answer inline below; commits then proceed.

### Decision Point 1 — `pl-PL.json` orphan locale (blocks Track 0 start)

[`packages/axoview-app/src/i18n/pl-PL.json`](../../packages/axoview-app/src/i18n/pl-PL.json) exists but is **not registered in `supportedLanguages`** — i.e., users cannot select Polish. Likely a half-finished contributor PR that never wired in.

**Options:**
- **(a) Wire it in** — add Polish to `supportedLanguages`; users get a (possibly partial-coverage) Polish UI. Risk: if coverage is sparse, joins de-DE + id-ID as a known-partial-coverage locale in [`known_issues.md`](../../known_issues.md).
- **(b) Delete it** — remove the file; codebase stops carrying an unreachable artifact.
- **(c) Defer** — leave the file in place + add a `_README.md` next to it explaining the orphan status; revisit when a Polish-speaking contributor surfaces.

**User answer:** **(a) Wire it in** — locked 2026-05-23. Add `pl-PL` to `supportedLanguages`; verify coverage level; if partial, add to `known_issues.md` alongside de-DE + id-ID.

### Decision Point 2 — Track 2 sequencing (4 decisions, each independent)

Each can be answered now or just-in-time when its commit comes up. Recommendation: pre-answer all four so Track 2 ships as one focused session.

- **B4 — `npm install --production` at container boot.** `docker-entrypoint.sh` runs `npm install --production` at startup → container boot is **network-required + non-reproducible**.
  - **(a) Move backend deps into image build** with `npm ci` (requires generating + committing `packages/axoview-backend/package-lock.json`).
  - **(b) Keep runtime install** — accept the network dependency + non-reproducibility.
  - **User answer: (a)** locked 2026-05-24 — move to image build; commit backend lockfile.

- **B5 — `ENABLE_SERVER_STORAGE=true` Dockerfile default.** Audit ADR 0009 §5d claims default is `false` (intentional implicit-off). Dockerfile actually ships `true`.
  - **(a) Flip Dockerfile default to `false`** — match audit claim; deployers explicitly opt in.
  - **(b) Amend audit appendix (Track 3)** to say "Docker default is on" — match reality.
  - **User answer: (b)** locked 2026-05-24 — Track 3 §12 appendix entry; NO Dockerfile change in Track 2.

- **B3 — Backend `package.json` `"private": true` + NPM_TOKEN.** `axoview-backend/package.json` lacks `private: true`; `NPM_TOKEN` still flows in `release.yml` even though Locked Decision #11 explicitly dropped npm publish.
  - **(a) Add `private: true` + drop NPM_TOKEN from `release.yml`.** Conforms to #11.
  - **(b) Leave both** — harmless residue; only matters if backend is ever published (it shouldn't be).
  - **User answer: (a)** locked 2026-05-24 — add private + drop NPM_TOKEN.

- **M3 — CI Node matrix vs `engines`.** `test.yml` matrix-tests Node 20/22/24 against `engines: ">=22"`. The matrix lies about minimum.
  - **(a) Drop Node 20 from matrix** — match `engines`.
  - **(b) Widen `engines` to `>=20`** — match matrix.
  - **User answer: (a)** locked 2026-05-24 — drop Node 20 from matrix.

- **B6 (NEW, added 2026-05-24) — Compose project name + service rename.** Docker Compose's default project name is the lowercased CWD basename. Current local CWD is `c:\myTemp\FossFLOW` → project group renders as `fossflow`, image as `fossflow-axoview`. User decision: clean it up at the config layer.
  - **User answer: option 2** locked 2026-05-24 — add `name: axoview` top-level to `compose.yml` + `compose.dev.yml`; rename service `axoview` → `app`. Image becomes `axoview-app`.
  - **Impact**: NO Cloudflare (uses git+wrangler, not compose); NO GitHub Actions (no workflow invokes compose; the old `docker.yml` was deleted in Track 0). Minor: anyone using `docker compose exec axoview <cmd>` patterns must update to `app`. Grep `fossflow-axoview` + `compose.*axoview` + `service.*axoview` in docs/scripts before commit.

### Decision Point 3 — Tier 3 polish (does NOT block; influences Track 0g / 1 / 2 scope expansion)

Tier 3 items from my earlier proposal (lint warnings → 0, naming residue cleanup, Express structured logging, preview-deploy warning, lib published-posture cleanup).

- **(a) In-scope for v1.1** — bundle into the relevant tracks (warnings into a new commit on Track 1; naming into Track 0g; etc.).
- **(b) Defer to v1.2** — spawn separate tacticals later.
- **Recommendation: (b)** — keep this cleanup focused. Tier 3 is genuinely defer-without-harm.

**User answer:** _<TBD; default = (b) defer_

---

## Track overview

```
EXECUTION TRACKS (this tactical owns):
   Track 4 — User-only external actions (you, 5 min)          ← FIRST
   Track 0 — Dead-code deletion wave (one PR, ~10 commits)
   Track 1 — Code fixes for audit-claim truth (one PR, ~3 commits)
   Track 2 — Productization decisions (4 sub-PRs, sequenced)
   Track 3 — Artifact-corrections appendix (one doc commit)
       ↓
FUTURE WORKSTREAMS (spawn own tacticals when authorized):
   Track 5 — Test coverage workstream (5a server-runtime PRIORITY, 5b/5c/5d follow)
   Track 6 — Refactor candidates for files > 300 LOC
   Track 7 — New audit workstreams (XSS, a11y, telemetry, migration, SBOM, etc.)
```

---

## Recommended execution sequence

| # | Track | Why this position | Sessions |
|---|---|---|---|
| 1 | **Track 4** (your 5 min) | Branch protection must guard subsequent merges; CodeQL toggle means subsequent PRs exercise it | 0.1 |
| 2 | **Track 0** (single PR, 7-10 commits) | Most reversible work; biggest LOC-removal payoff; **needs Decision Point 1 (pl-PL) first** | 1-2 |
| 3 | **Track 1** (single PR, 3-4 commits) | Makes shipped audit claims true; no behaviour change | 1 |
| 4 | **Track 2** (single PR, 4 commits sequenced) | One decision at a time → execute → next; **needs Decision Point 2 answers** | 1-2 |
| 5 | **Track 3** (single doc commit) | Append-only correction to frozen artifact | 0.3 |
| 6 | **Tracks 5/6/7** | Each spawns its own tactical when user authorizes | varies |

**Total this tactical: ~4-6 sessions** (excluding spawned tacticals).

---

## Per-track detail

### Track 0 — Dead-code deletion wave

One PR (`v1.1/dead-code-wave` branch or similar). One commit per cluster for clean reverts. Risk: low (every row has anchor + cascade pre-identified).

#### 0a — `axoview-lib/docs/` Nextra scaffold (14 files)

- [x] Delete entire [`packages/axoview-lib/docs/`](../../packages/axoview-lib/docs/) directory (separate `package.json` + own `package-lock.json` + 13 `.mdx`/`_meta.json`/`tsconfig.json`/`.gitignore` files).
- [x] Verify zero importers via `grep -rn "axoview-lib/docs"` across rest of repo.
- [x] `installation.mdx` actively misleads (`npm publish` flow contradicts Locked Decision #11) — deletion resolves the misdirection.

Commit: `chore(deletion): remove dead axoview-lib/docs Nextra scaffold (per audit N2)` — landed `b24a8ce`.

#### 0b — Burger-menu retirement cluster (locked per audit C.2)

- [x] Delete [`packages/axoview-lib/src/components/MainMenu/MainMenu.tsx`](../../packages/axoview-lib/src/components/MainMenu/MainMenu.tsx) (260 LOC).
- [x] Delete [`packages/axoview-lib/src/components/MainMenu/MenuItem.tsx`](../../packages/axoview-lib/src/components/MainMenu/MenuItem.tsx) (21 LOC).
- [x] Delete [`packages/axoview-lib/src/components/MainMenu/`](../../packages/axoview-lib/src/components/MainMenu/) parent folder.
- [x] Delete [`packages/axoview-lib/src/components/ConfirmDiscardDialog/ConfirmDiscardDialog.tsx`](../../packages/axoview-lib/src/components/ConfirmDiscardDialog/ConfirmDiscardDialog.tsx) (58 LOC).
- [x] Delete [`packages/axoview-lib/src/components/ConfirmDiscardDialog/`](../../packages/axoview-lib/src/components/ConfirmDiscardDialog/) parent folder.
- [x] Verify zero importers via `grep -rn "MainMenu\|MenuItem\|ConfirmDiscardDialog"` (cascades from MainMenu mount sites; locked decision verified the cascade in audit C.2).
- [x] Verify ADR 0005's "burger menu removed" claim still holds — **surprise**: deletion exceeds ADR 0005's stated scope (ADR said lib component "remains for other consumers"; audit C.2 overrides). Track 1 doc-truth fix needed.

Commit: `chore(deletion): remove burger-menu cascade per audit C.2` — landed `3590aa3`.

#### 0c — Adjacent latent dead code (verify-grep-then-act per file)

- [x] [`packages/axoview-app/src/reportWebVitals.ts`](../../packages/axoview-app/src/reportWebVitals.ts) (15 LOC) — file + call deleted (no handler ever passed; whole module was a no-op).
- [x] [`packages/axoview-app/src/serviceWorkerRegistration.ts`](../../packages/axoview-app/src/serviceWorkerRegistration.ts) — slimmed from 117 LOC to a 16-line unregister-only helper.
- [x] `packages/axoview-app/src/diagramUtils.ts` — `validateDiagramData` confirmed dead via grep + knip; removed.
- [x] `packages/axoview-app/scripts/generateMaterialIconPack.ts` — `.ts` twin deleted (the `.js` is what prebuild/prestart + the unit test actually use).

Commit: `chore(deletion): remove adjacent dead code (per audit anomalies)` — landed `16dabb0`. **Drift surfaced**: `web-vitals` dep became unused once `reportWebVitals` was removed; Track 1 follow-up cleanup.

#### 0d — Dead E2E scaffolding

- [x] `packages/axoview-e2e/fixtures/canvas.fixture.ts` — deleted (no spec imported `canvasTest`).
- [x] `packages/axoview-e2e/fixtures/index.ts` — deleted (unused barrel).
- [x] `packages/axoview-e2e/helpers/mouse.ts` — deleted (no spec or POM imported it).
- [x] `packages/axoview-e2e/tsconfig.json` path aliases (`@fixtures/*`, `@helpers/*`, `@pom/*`) + the dependent `baseUrl` — removed.

Commit: `chore(deletion): remove dead e2e fixtures + helpers + unused tsconfig aliases` — landed `19182b1`.

#### 0e — Wrap-pending tacticals (per docs convention lifecycle)

- [x] Delete `docs/tactical/e2e-suite-rewrite.md` — done.
- [x] Delete `docs/tactical/git-automation-hardening.md` — done.
- [x] productization-audit tactical confirmed already deleted (wrapped in `fdfa3be`).
- [x] Memory pointer `project_docs_convention.md` updated to drop both from "active" + record under "Retired 2026-05-23".

Commit: `chore(docs): wrap e2e-suite-rewrite + git-automation-hardening tacticals per lifecycle convention` — landed `aab1a4c`.

#### 0f — Vestigial / suspicious (verify-then-act)

Each item needs a grep-and-read before commit. If suspicion holds, delete/refactor. If false alarm, document inline why it stays.

- [~] `ContextMenuManager.tsx` (7 LOC noop) — **SKIPPED** (cascade too large). Surprise: same folder also has `ContextMenu.tsx` (knip-flagged) which is a real renderer; `useInteractionManager` actively calls `setContextMenu()` on right-click. Removing the noop alone would leave dead state with no consumer. Whole context-menu surface is a half-implemented feature — Track 7 candidate.
- [~] `renderProbe.ts` — **SKIPPED** (load-bearing). Used by `Nodes`/`Node`/`Connectors`/`Connector`; documented in `docs/perf-troubleshooting.md`; explicitly retained in `known_issues.md:62`. Not a duplicate of `utils/trace.ts`.
- [x] `packages/axoview-lib/tsconfig.dev.json` — deleted (no tooling consumes it).
- [x] `packages/axoview-app/src/layout/FileExplorerLayout.tsx` — inlined into App.tsx as a literal Box+Box pair; empty `layout/` folder pruned.
- [x] `EmptyStateScreen.tsx` `GRID_VARIANT` — `'iso'` locked; dead `'2d'` branch + the A/B constant removed.
- [~] `useRuntimeConfig.serverStorage` field — **SKIPPED** (audit anomaly #31 was wrong). Field IS read by `AppStorageContext.tsx:47` which calls `manager.setServerStorage(config.serverStorage)`.

Commit: `chore(deletion): vestigial cleanup pass (tsconfig.dev, FileExplorerLayout, GRID_VARIANT)` — landed `ec950dd`.

#### 0g — Naming residue (ADR 0008 cleanup cascade)

- [x] `CoordsUtils.ts` → `coordsUtils.ts` (class symbol unchanged). All path-string importers updated.
- [x] `SizeUtils.ts` → `sizeUtils.ts` (class symbol unchanged). All path-string importers updated.
- [x] `svgOptimizer.test.ts` moved into `utils/__tests__/`; relative import `./svgOptimizer` → `../svgOptimizer`.
- [x] `components/Sidebars/RightSidebar.tsx` → `components/RightSidebar.tsx`; empty `Sidebars/` folder gone.

Commit: `refactor: adr 0008 naming residue cleanup (coords/size utils + sidebars + svgOptimizer test)` — landed `e575057`. **Minor scare**: moving the test file broke its `./svgOptimizer` import on the first build; fixed in the same commit before staging.

#### 0h — Orphan locale (DECISION 1 LOCKED: option a — wire in)

Per DP1 locked 2026-05-23: wire `pl-PL` into `supportedLanguages`; verify coverage; add to `known_issues.md` if partial.

- [x] Added `pl-PL` (label "Polski") to `supportedLanguages` in `packages/axoview-app/src/i18n.ts`.
- [x] Measured coverage: **pl-PL 78/93 = 83.9%** vs en-US baseline. (15 missing app keys; 1 extra stale key.) Reference baselines: de-DE 46.2%, id-ID 46.2%.
- [x] Coverage ≥ 80% → shipped as fully-supported; **no `known_issues.md` entry created**.
- [x] Lib-side `packages/axoview-lib/src/i18n/pl-PL.ts` already exists → lib strings render in Polish, no English fallback warning.
- [x] `i18n.localeCompleteness.test.ts` (lib) still green (all 93 suites pass; pl-PL is app-only so the lib test isn't affected).

Commit: `feat(i18n): wire pl-PL into supportedLanguages (DP1 option a)` — landed `8636489`.

---

### Track 1 — Code fixes that make audit claims true

**Shipped 2026-05-23 on `integration`** — 7 commits (B1 `1933091` → deployment callout `f2b279a`). All audit-truth rows resolved + 3 follow-ups from Track 0 surfaces + 1 Track 4 fold-in.

| ID | What | File | Status |
|---|---|---|---|
| B1 | nginx serves zero security headers — audit §6d claims parity with `_headers` | [`nginx.conf`](../../nginx.conf) | **[x]** Added `add_header` directives for CSP / X-Content-Type-Options / X-Frame-Options / Referrer-Policy / Permissions-Policy + `Cache-Control: no-store` on `/api/`. HSTS intentionally omitted (parity with `_headers`; host-driven). Re-emitted inside `/api/` block per nginx no-inherit rule. — `1933091` |
| M1 | `/api/storage/status` still referenced in operator docs (deleted per ADR 0009 D2) | [`docs/architecture.md`](../architecture.md), [`docs/deployment.md`](../deployment.md) | **[x]** — `21ef6d3` |
| M4 | Dead `HTTP_AUTH_USER`/`HTTP_AUTH_PASSWORD` env vars (Locked Decision #13 removed Basic Auth) | [`compose.yml`](../../compose.yml) | **[x]** — `21ef6d3` |
| URL | Hardcoded `github.com/stan-smith/Axoview/issues/new` in ErrorBoundary (post-rename residue) | [`packages/axoview-app/src/components/ErrorBoundary.tsx`](../../packages/axoview-app/src/components/ErrorBoundary.tsx) | **[x]** — `21ef6d3` |
| M2 | `isPublicRoute` divergence: Express accepts any method; Worker accepts GET only | [`packages/axoview-backend/server.js`](../../packages/axoview-backend/server.js) vs [`packages/axoview-worker/src/auth.ts`](../../packages/axoview-worker/src/auth.ts) | **[x]** — Tightened Express to GET-only on both `/api/config` and `/api/public/diagrams/` per ADR 0010 D6. Pre-edit grep confirmed no caller used non-GET against either. — `506194b` |
| ADR-0005 | Track 0b deletion exceeded ADR 0005's stated scope (lib's MainMenu was framed as "remains for other consumers" but was fully deleted). architecture.md MainMenu references also stale. | [`docs/adr/0005-toolbar-and-dock-layout-contract.md`](../adr/0005-toolbar-and-dock-layout-contract.md), [`docs/architecture.md`](../architecture.md) | **[x]** — ADR 0005 §5 gained Amendment 2026-05-23 (ADR 0003 addendum style); architecture.md UI Components table, EDITABLE-mode row, UiState fields, UiState actions, useDirtyTracker section, interaction-manager surprise entry all cleaned. — `6dfd9b4` |
| WEB-VITALS | `web-vitals` dep orphaned after Track 0c `reportWebVitals` deletion | [`packages/axoview-app/package.json`](../../packages/axoview-app/package.json) | **[x]** — `npm uninstall web-vitals --workspace=packages/axoview-app`. Knip unused-deps drops 3 → 2. — `cea53da` |
| I18N-1 | 9 `LocaleProps.mainMenu.*` keys × 13 lib locales = 117 dead i18n entries after MainMenu cascade deletion | `packages/axoview-lib/src/i18n/*.ts` + `types/axoviewProps.ts` + `__perf_refactor_regression__/i18n.localeCompleteness.test.ts` | **[x]** — Python regex strip preserving UTF-8 / LF / no-BOM per file. (First attempt via PowerShell introduced BOM + mojibake on em-dashes / ☰ glyphs; reverted via `git reset --hard HEAD~1` and redone clean.) 143 deletions across 13 locales + 12 deletions across types/test, zero insertions. Lib jest `i18n.localeCompleteness` 13/13 still passes. — `6aed572` |
| TRK4-4 | Single-tenant-lock operator callout (folded in from Track 4 item 4 per ADR 0010 D4) | [`docs/deployment.md`](../deployment.md) | **[x]** — `f2b279a` |

---

### Track 2 — Productization decisions (DP2 locked 2026-05-24)

**Shipped 2026-05-24 on `integration`** — 4 code commits + this bookkeeping. DP2 answers: B4(a), B5(b), B3(a), M3(a), B6 option 2.

| ID | Decision (locked) | Status |
|---|---|---|
| B4 | (a) `npm ci` at build time + commit backend `package-lock.json` | **[x]** — `6067b81` |
| B5 | (b) Amend audit appendix; NO Dockerfile change here | **Queued for Track 3** — §12 appendix entry will read: "ADR 0009 D4a 'ENABLE_SERVER_STORAGE=false default' is aspirational; Dockerfile ships `true`. Per DP2-B5(b): correct in appendix, not in Dockerfile." |
| B3 | (a) backend `private: true` + drop NPM_TOKEN from release.yml | **[x]** — `3bde7a8` |
| M3 | (a) Drop Node 20 from CI matrix | **[x]** — `894858a` |
| B6 | option 2 — `name: axoview` + service `axoview` → `app` | **[x]** — `0a316ad` |

Commits (per decision, as landed):
1. `feat(docker): backend deps via npm ci at build time + commit backend package-lock.json` — `6067b81`
2. `chore(backend+ci): backend private:true + drop dead NPM_TOKEN from release.yml` — `3bde7a8`
3. `ci: drop Node 20 from test matrix to match engines: ">=22"` — `894858a`
4. `chore(docker): compose project name axoview + service rename axoview → app` — `0a316ad`

---

### Track 3 — Artifact-corrections appendix (frozen-artifact convention)

Single commit. Appends `§12 — Corrections (2026-05-23)` to [`docs/technical-review-2026-05.md`](../technical-review-2026-05.md). Preserves the freeze convention (no in-place edits) while making the corrections visible to future reviewers.

Cover:
- **B2** — Worker `app.ts` doesn't actually import `routes.js` at runtime (only `tsconfig.json` includes it for typecheck); §3b/§7.4 "single source of truth" wording is aspirational.
- **B5** (added 2026-05-24 per DP2-B5(b)) — ADR 0009 §5d claims `ENABLE_SERVER_STORAGE` defaults to `false` (intentional implicit-off). Dockerfile actually ships `true`. Per locked decision: appendix correction, NOT a Dockerfile flip. Document that the Docker default is `ENABLE_SERVER_STORAGE=true` and explain the rationale (Docker users overwhelmingly want server-backed storage; the implicit-off default is correct for the non-Docker SaaS path but inverted for the container).
- **M6** — Hono `secureHeaders()` ≠ `_headers` set; "CI verifies neither layer drifts" is unsupported (no such CI step exists).
- **M7** — Coverage 10% gate applies only to `axoview-lib/jest.config.js` (~63% of LOC); app/backend/worker uncovered.
- **Anomaly #31** — audit flagged `useRuntimeConfig.serverStorage` field as dead, but Track 0f verify-grep confirmed it IS read by `AppStorageContext.tsx:47` which calls `manager.setServerStorage(config.serverStorage)`. The audit row was wrong; correct in the appendix.
- `axoview-lib/package.json` "published-shape but private" posture flag (audit row 810) — explicitly name as reviewer flag for Track 7.
- Auto-generated artifacts not to red-pen (§7.8 bullet 10) — surface in appendix for reviewer convenience.
- **Status header update**: "Status: artifact frozen 2026-05-23 (Sessions A + B + C complete). **Corrections appendix added <DATE>. Reviewer-ready.**"

Commit: `docs(review): add §12 corrections appendix (B2 + B5 + M6 + M7 + published-posture + reviewer notes)`.

---

### Track 4 — User-only external actions

| # | Action | Where | Severity | Status |
|---|---|---|---|---|
| 1 | Enable CodeQL repo toggle | GitHub Settings → Code security and analysis → CodeQL | low (workflow exists; just dormant) | **[x] done 2026-05-23** |
| 2 | Branch protection on master: require Run Tests + E2E + CodeQL + PR review | GitHub Settings → Branches | med (semantic-release fires on master push; no PR-gate today) | **[x] done 2026-05-23** |
| 3 | Repo metadata: Description + Topics (`isometric`, `react`, `diagram-editor`, `cloudflare-pages`, `docker`) + Homepage (`https://axoview.pages.dev/`) | GitHub Settings → General | low (discoverability) | [ ] pending — non-blocking |
| 4 | Single-tenant lock callout in [`docs/deployment.md`](../deployment.md) | docs/deployment.md | med (operator surprise risk; see ADR 0010 D4) | **[x] done 2026-05-23** in Track 1 PR — commit `f2b279a` |

**Track 4 status as of 2026-05-23:** the two security-relevant items (CodeQL toggle + branch protection) are done; item 4 (single-tenant callout) is shipped in the Track 1 PR. Item 3 (repo metadata) remains as the only pending row — your UI action whenever convenient.

---

### Track 5 — Test-coverage workstream (own tactical when authorized)

**The biggest invisible debt.** Captured here so it's not lost; **execution lives in a spawned tactical** (`docs/tactical/v1.1-test-coverage.md` or similar).

#### 5a — Backend + Worker zero-jest gap — §11 HIGH severity

- [`packages/axoview-backend/src/routes.js`](../../packages/axoview-backend/src/routes.js) (325 LOC — most-load-bearing surface in repo, zero tests).
- [`packages/axoview-backend/src/adapters/fs.js`](../../packages/axoview-backend/src/adapters/fs.js) (95 LOC — only server-backed adapter, untested).
- [`packages/axoview-worker/src/app.ts`](../../packages/axoview-worker/src/app.ts) + [`auth.ts`](../../packages/axoview-worker/src/auth.ts) (no jest config at all).
- **Right shape**: contract-test harness with separate Express + Hono adapters in the test harness.
- **Why this matters**: the orchestrator's B2 finding (Worker is hardcoded 503) flows from this gap — no tests forced the divergence to be visible.

#### 5b — `axoview-app` 15% test:source — §8d

Untested large surfaces: `App.tsx` (373 LOC), `AppToolbar.tsx` (333 LOC), `FileExplorer.tsx` (667 LOC), `DiagramLifecycleProvider.tsx` (~1,330 LOC). Plus dozens of "No unit test" rows on `LocalModeBanner`, dialogs, hooks.

#### 5c — `axoview-lib` integration-only large surfaces — §7.1

`Axoview.tsx` (326 LOC), `LayersPanel.tsx` (535 LOC), `ConnectorControls.tsx` (557 LOC), `useSceneActions.ts` (789 LOC), `useInteractionManager.ts` (741 LOC), `Cursor.ts` (458 LOC), `DragItems.ts` (310 LOC), `NodePanel.tsx` (377 LOC), `Renderer.tsx` (223 LOC), `Node.tsx` (350 LOC), `Connector.tsx` (281 LOC), `ViewTabs.tsx` (291 LOC), `uiStateStore.tsx` (290 LOC), and all `ItemControls/*Controls.tsx`.

#### 5d — Knip B-10 triage — §8f, §11

15 unused files / 23 unused exports / 22 unused types / 3 unused deps / 8 unused devDeps. Needs separation pass between genuine dead and false-positives (lib public API) before promoting Knip to hard-fail.

**Authorization gate**: spawn the orchestrator with this scope when user is ready: *"Spawn the orchestrator subagent for v1.1 test-coverage. Source: this tactical's Track 5. Tier 1 = 5a (server-runtime contract tests, HIGH severity per §11). Tier 2 = 5b (app surfaces). Tier 3 = 5c (lib large surfaces) + 5d (knip triage + hard-fail promotion). Produce tactical doc + milestone path + first session prompt."*

---

### Track 6 — Refactor candidates (defer; flag for future)

Audit-named "refactor candidate" rows. **Don't touch as part of this cleanup**; capture for a future tactical that decides refactor priority.

| File | LOC | Suggested split |
|---|---|---|
| [`packages/axoview-app/src/providers/DiagramLifecycleProvider.tsx`](../../packages/axoview-app/src/providers/DiagramLifecycleProvider.tsx) | ~1,330 | Per-lifecycle-phase |
| [`packages/axoview-lib/src/hooks/useSceneActions.ts`](../../packages/axoview-lib/src/hooks/useSceneActions.ts) | 789 | Reducer-domain boundaries |
| [`packages/axoview-lib/src/interaction/useInteractionManager.ts`](../../packages/axoview-lib/src/interaction/useInteractionManager.ts) | 741 | Per-mode handlers |
| [`packages/axoview-lib/src/components/ExportImageDialog/ExportImageDialog.tsx`](../../packages/axoview-lib/src/components/ExportImageDialog/ExportImageDialog.tsx) | 760 | Component split |
| [`packages/axoview-app/src/components/fileExplorer/FileExplorer.tsx`](../../packages/axoview-app/src/components/fileExplorer/FileExplorer.tsx) | 667 | Per-action |
| [`packages/axoview-app/src/components/DiagnosticsOverlay.tsx`](../../packages/axoview-app/src/components/DiagnosticsOverlay.tsx) | 563 | Telemetry vs UI |
| [`packages/axoview-lib/src/components/LayersPanel/LayersPanel.tsx`](../../packages/axoview-lib/src/components/LayersPanel/LayersPanel.tsx) | 535 | Per-row-type |
| [`packages/axoview-lib/src/components/ItemControls/ConnectorControls/ConnectorControls.tsx`](../../packages/axoview-lib/src/components/ItemControls/ConnectorControls/ConnectorControls.tsx) | 557 | Per-property-group |
| [`packages/axoview-lib/src/components/LeftDock/ElementsPanel.tsx`](../../packages/axoview-lib/src/components/LeftDock/ElementsPanel.tsx) | 440 | — |
| [`packages/axoview-app/src/components/fileExplorer/ImportDialog.tsx`](../../packages/axoview-app/src/components/fileExplorer/ImportDialog.tsx) | 312 | — |

**Authorization gate**: spawn orchestrator with refactor priority when user wants to start. Recommendation: **defer until at least Track 5a (server-runtime tests) lands** — refactoring untested surfaces is the worst combination.

---

### Track 7 — New audit workstreams (scoping conversations)

Gaps the original audit didn't cover. Each is its own tactical when authorized.

| Workstream | Origin | Suggested vehicle |
|---|---|---|
| **M5 XSS surface review** — `dangerouslySetInnerHTML` in TextBox.tsx / useTextBoxProps.ts / config.ts + CSP `'unsafe-inline'` in style-src | Orchestrator finding + §6d, §11 | Polish ADR + tactical pairing Quill review with CSP tightening |
| **M8a Accessibility baseline** — zero ARIA / WCAG / keyboard-trap audit; §11 has no a11y row | Orchestrator finding | Standalone tactical |
| **M8b Telemetry / privacy disclosure** — CSP allows `connect-src` to `googleapis.com` pre-Drive; no privacy policy; PWA manifest has no data-handling description | Orchestrator finding | Privacy ADR |
| **M8c v1.0.0 → v1.x migration policy** — §10b prompt 5 asks the reviewer; semantic-release shipping minors against undocumented data-shape | Orchestrator finding | Versioning ADR |
| **M8d SBOM + SRI** — out of scope today (§6g); Google OAuth scripts whitelisted in CSP have no integrity hashes | Orchestrator + §6g | Supply-chain ADR |
| **B-9b silent-failure retrofit** (S2-S20) | Audit register, §4f, §11 | **Opportunistic per-PR; NOT a sweep** (per Locked Decision #5 here) |
| **Express morgan / structured logging** | ADR 0009 D6 + §6h + §11 | "Awaits real user complaint" — defer until grounded |
| **`*.pages.dev` preview-deploy warning when AUTH_MODE=none** | §6h + §11 | Deploy-time warning or preview-deploy block |
| **Worker `wrangler.toml` consolidation** | §5f + §7.8 + §11 | Symlink or generator script |
| **CSP `'unsafe-inline'` style-src tightening** | §6d + §11 | Needs MUI emotion nonce/hash work |
| **ADR 0007 (trace harness) backfill** | §9.1 row 0007 + §11 | Authorial gap; could backfill from commits if useful |
| **i18n triplication coverage-matrix CI linter** | §7.8 bullet 1 + §10a prompt 7 | Linter for lib × app coverage matrix |
| **`axoview-lib` public-API surface narrowing** | §7.1 + §10a #8 | Public-API audit if/when npm-publish need surfaces |
| **MQA #21 folders-collision repro** | §6e, known_issues.md | Defer |
| **Connector drag sustained-GC cliff** | §11 (~50s marathon drags) | Defer |
| **MQA diag exporter element counts read 0** | §11 + DiagnosticsOverlay.tsx | Defer |
| **Context-menu surface decision** — `ContextMenu.tsx` renderer + `ContextMenuManager.tsx` (7 LOC noop) + `useInteractionManager.setContextMenu()` call site form a half-implemented surface. Track 0f skipped these because removing the noop alone would leave dead state with no consumer. Decide: implement (wire the renderer to render at the anchor) OR remove entirely (state + setter + Manager + renderer). | Track 0f finding (`ec950dd`) | ADR or feature tactical |
| **CodeQL config drift** — `.github/workflows/codeql.yml` config + branch protection toggle live, but no audit pass to verify config matches what the repo actually exercises (languages list, query suites, scheduled runs vs PR-only). | Track 4 ramp-up + cleanup PR observation | Light audit / spawned tactical |
| **`uiOverlay.editorModes.test.ts` ZOOM_CONTROLS drift** — test-mirror's local `EDITOR_MODE_MAPPING` does not match prod `EDITOR_MODE_MAPPING` for ZOOM_CONTROLS visibility. Pre-existing; surfaced during Track 0b cleanup. Two-mirror divergence pattern; correct the test mirror. | Track 0b finding | Single-commit fix or fold into next test-cleanup wave |

**Authorization gate**: each row spawns a separate tactical via orchestrator when user prioritizes it.

---

## Findings register

Append findings here as tracks execute. Each row gets a one-line note + commit hash.

### Track 0 findings

Shipped 2026-05-23 across 9 commits (setup + 0a-0h) on `integration`. All gates green (`npm run build`, 1007 tests passing, `npx eslint .` 0 errors).

**Knip baseline delta** (`npx knip --reporter compact`):

| Metric | Pre-Track-0 | Post-Track-0 | Delta |
|---|---|---|---|
| Unused files | 16 | 13 | **−3** |
| Unused exports | 23 | 22 | −1 |
| Unused exported types | 23 | 22 | −1 |
| Unused dependencies | 2 | 3 | **+1** (`web-vitals` newly orphaned by 0c — flag for Track 1) |
| Other categories | unchanged | unchanged | 0 |

The −3 in unused files (4 deletions / 4 inlines on the lib + app side; some balanced by new files NOT introduced) is below the ~7-file deletion target because (a) cluster 0d files were already counted as in-package unused files baseline = lib-side counts; (b) cluster 0f kept 3 of 6 items per verify-grep surprise.

**Per-cluster summary:**

- **0a** (`b24a8ce`) — 14 files / 7021 LOC of dead Nextra scaffold gone. Clean — zero importers.
- **0b** (`3590aa3`) — Burger-menu cascade landed: 3 component files + 8 state/type/config retrofits + 1 test mirror update. **Surprise**: deletion exceeds ADR 0005's "lib component remains for other consumers" wording → **Track 1 doc-truth fix needed for ADR 0005 + `architecture.md` MainMenu references**. Also noted: `LocaleProps.mainMenu` i18n keys (9 keys × 14 locales) remain in place — future i18n cleanup wave. Test-mirror pre-existing `ZOOM_CONTROLS` drift in `uiOverlay.editorModes.test.ts` flagged (not in scope).
- **0c** (`16dabb0`) — 4 items: reportWebVitals + half of serviceWorkerRegistration + validateDiagramData + generateMaterialIconPack.ts twin. −282 LOC net. **Drift**: `web-vitals` dep is now unused (no callers) → Track 1 cleanup candidate.
- **0d** (`19182b1`) — 4 dead e2e items removed. −114 LOC. Clean.
- **0e** (`aab1a4c`) — 2 wrap-pending tacticals deleted (−636 LOC of doc). Memory pointer (`project_docs_convention.md`) updated outside the repo. Clean.
- **0f** (`ec950dd`) — 3 of 6 bullets executed; 3 skipped after verify-grep. Skipped: ContextMenuManager (cascade implication = whole context-menu surface is half-implemented — Track 7 candidate), renderProbe (load-bearing, retained per `known_issues.md:62`), `useRuntimeConfig.serverStorage` field (audit anomaly #31 incorrect; field IS read by `AppStorageContext.tsx:47`). Pattern: ~50% of "vestigial-or-suspicious" rows revealed false-positive on verify-grep — the verify-grep discipline paid for itself.
- **0g** (`e575057`) — 4 renames (2 file-case, 1 folder-promote, 1 test-relocate). **Minor scare**: relocating `svgOptimizer.test.ts` broke its relative `./svgOptimizer` import on first post-flight; fixed in same commit pre-stage. All other path-string importers updated cleanly.
- **0h** (`8636489`) — pl-PL wired into `supportedLanguages` at 83.9% coverage (clears 80% threshold). Fully-supported; no `known_issues.md` entry. Lib-side `pl-PL.ts` already present.

**Follow-ups for Track 1 (doc-truth fixes):**

1. ADR 0005 wording about "lib's MainMenu remains for other consumers" is now stale (0b exceeded its scope).
2. `architecture.md` references to MainMenu / ConfirmDiscardDialog / mainMenuOptions / isMainMenuOpen / `Sidebars/` folder all stale post-Track-0.
3. `web-vitals` dep can be removed from `packages/axoview-app/package.json` (0c side effect).
4. `technical-review-2026-05.md §7.8` rows for the deleted files should be acknowledged in the Track 3 corrections appendix.

**Follow-ups for Track 7 (new audit workstreams):**

1. Context-menu surface decision: implement (wire `ContextMenu.tsx` to render at the anchor) OR remove entirely (state plumbing + setContextMenu in useInteractionManager / usePanHandlers + ContextMenu/ folder).
2. `LocaleProps.mainMenu` i18n cleanup (9 dead keys × 14 locales).
3. `uiOverlay.editorModes.test.ts` pre-existing ZOOM_CONTROLS drift — local mirror does not match prod EDITOR_MODE_MAPPING.

### Track 1 findings

Shipped 2026-05-23 on `integration` across 7 commits (`1933091` → `f2b279a`). No code behaviour change for users (per Track 1 contract); doc-truth + config-truth + nginx security parity + Express auth alignment + Track 0 follow-ups (web-vitals dep, ADR 0005 addendum, i18n cleanup) + Track 4 fold-in (single-tenant callout).

**Knip baseline delta** (`npx knip --reporter compact`):

| Metric | Post-Track-0 | Post-Track-1 | Delta |
|---|---|---|---|
| Unused files | 13 | 13 | 0 |
| Unused exports | 22 | 22 | 0 |
| Unused exported types | 22 | 22 | 0 |
| Unused dependencies | 3 | 2 | **−1** (`web-vitals` removed in `cea53da`) |
| Unused devDependencies | (n/r) | 3 | first-report — pre-existing |

**Per-commit summary:**

- **B1** (`1933091`) — nginx headers parity. Clean — `_headers` set transcribed verbatim into both `server` and `location /api/` blocks (nginx doesn't inherit `add_header` when a location sets its own). HSTS intentionally omitted to match `_headers` (host-driven). User to manual-smoke via `curl -I http://localhost:3000/` against Docker.
- **M1 + M4 + ErrorBoundary URL** (`21ef6d3`) — doc-truth + config-truth + post-rename URL. Bundled per Track 1 plan. The `/api/storage/status` strip touched 4 sites in deployment.md (intro bullet, C5 curl, C5 prose, D-section bullet) and 3 sites in architecture.md (registration bullet, route block, auth-bypass note). Strictly-listed scope honored: `docs/perf-troubleshooting.md`, `docs/testing.md`, `docs/manual-test-baseline.md`, `docs/technical-review-2026-05.md`, and ADR 0009 references intentionally untouched (historical/test docs vs the frozen artifact). Track 3 corrections appendix is the right venue for the technical-review row.
- **M2** (`506194b`) — Express `isPublicRoute` was already GET-only for `/api/public/diagrams/` (line 60); the actual divergence was `/api/config` (Express any-method; Worker GET-only). Tightened to GET-only for parity. Express only registers `app.get('/api/config')` so no caller is affected — the bypass surface was wider than the route surface.
- **ADR 0005 + architecture.md cascade** (`6dfd9b4`) — ADR 0005 gained an Amendment 2026-05-23 block at the top of §5 (mirroring ADR 0003's 2026-05-02 pattern). architecture.md scrubbed at 7 sites (UI Components table, EDITABLE-mode row, UiState fields x2, UiState actions, useDirtyTracker section, interaction-manager surprise entry). Title-drift detection paragraph reframed as a defensive guard since the lib menu that originally triggered it is gone.
- **web-vitals removal** (`cea53da`) — knip baseline drops 3 → 2 unused-deps as expected. The stale `packages/axoview-app/package-lock.json` (vestigial — root monorepo lock is authoritative) still references `web-vitals@^2.1.4`; left in place as out-of-scope pre-existing residue.
- **i18n mainMenu cleanup** (`6aed572`) — first attempt via PowerShell `Set-Content -Encoding utf8` added a UTF-8 BOM and mojibakeified em-dashes (`—` → `â€”`) and Unicode glyphs (`☰` → `â˜°`) on the locale files. Caught by `git show --stat HEAD` showing nonsense insertions/deletions ratio. **`git reset --hard HEAD~1` to revert**, then redone via a small Python script that detects per-file BOM + line-ending and writes back binary-faithful. Final diff: clean 11-line-block deletion per locale + 11 deletions on the type + 1 on the test = 143 + 12 = 155 deletions across 15 files, zero insertions.
- **single-tenant callout** (`f2b279a`) — Track 4 item 4 fold-in. Admonition block placed immediately after the intro paragraph + target table, before the routes/auth detail. Names the operator-surprise risk explicitly: `AUTH_MODE=shared-token` is not a team password.

**Issues to flag for user:**

1. **Manual smoke for B1** — `curl -I http://localhost:3000/` in Docker mode to confirm CSP / X-Content-Type-Options / X-Frame-Options / Referrer-Policy / Permissions-Policy all present + match `_headers`. Not a CI gate.
2. **CI gates for Track 1 PR** — Run Tests + E2E Tests + CodeQL all need to green on the integration→master PR.
3. **PowerShell encoding-hazard lesson** — never use `Set-Content -Encoding utf8` on files with non-ASCII content in PowerShell 5.1; it BOMs and mojibakes. Use Python (`open(f, 'wb')` with explicit BOM detection) or Edit per-file. Captured in `feedback_intent_verification.md` candidate.
4. **Track 0 SOP-shift** — post-squash-merge sync is now the discipline. After any PR squash-merges to master, run `git checkout integration && git fetch origin && git reset --hard origin/master && git push --force-with-lease origin integration` to keep integration as a clean fast-forward base for the next PR. Avoids the modify/delete dance.

### Track 2 findings

Shipped 2026-05-24 on `integration` across 4 code commits (`6067b81` → `0a316ad`) + this bookkeeping. DP2 answers all (a)/option-2 except B5 (deferred to Track 3 §12 appendix). No CI changes pending — all gates remain live.

**Per-commit summary:**

- **B4** (`6067b81`) — `packages/axoview-backend/package-lock.json` generated via `npm install --package-lock-only --workspaces=false` (97 packages, isolated from the workspace hoist). Dockerfile gains `COPY packages/axoview-backend/package*.json` + `RUN cd packages/axoview-backend && npm ci --omit=dev --workspaces=false` between root `npm install` and `COPY . .`. `.dockerignore` already excludes host `node_modules`, so the build-stage `npm ci` survives the source overlay; runtime stage's `COPY --from=build /app/packages/axoview-backend` carries node_modules across. `docker-entrypoint.sh` drops `npm install --production`. Container boot is now reproducible + offline-safe. **Pre-commit lockfile-regen check** post-B3 confirmed `private: true` does not perturb the lockfile (no diff). **Manual smoke required**: user to confirm `docker compose build --no-cache` succeeds + `docker compose up` (no `--build`) boots without network for dep install.
- **B3** (`3bde7a8`) — `axoview-backend/package.json` gains `"private": true`; `release.yml` env block drops `NPM_TOKEN`. Pre-edit `grep -rn "NPM_TOKEN" .github/` returned only the one line; post-edit returns empty. Conforms to Locked Decision #11 (npm publish dropped from pipeline).
- **M3** (`894858a`) — `test.yml` matrix `[20.x, 22.x, 24.x]` → `[22.x, 24.x]`. Single-line change. Matches root `engines.node >= 22.0.0`.
- **B6** (`0a316ad`) — `compose.yml` + `compose.dev.yml` gain `name: axoview` top-level + rename service `axoview` → `app`. Pre-edit grep sweep: `fossflow-axoview` only appears in this tactical doc; `compose.*axoview` matches frozen `technical-review-2026-05.md` (do not touch per LD #1) + this tactical + an MQA legacy-format reference (`fossflow-project` zip format — unrelated semantic); `service.*axoview` similarly bounded. No docs/scripts use `docker compose exec axoview <cmd>` patterns, so no doc edits needed. After this commit, the build image is `axoview-app` (down from `fossflow-axoview`). **Note**: `compose.dev.yml` still carries `HTTP_AUTH_USER` / `HTTP_AUTH_PASSWORD` dead env vars — Track 1 M4 cleaned the prod compose.yml; the dev variant was pre-existing oversight, out-of-scope here. Flag for a separate one-line follow-up if useful.

**Issues to flag for user:**

1. **Manual smoke for B4** — `docker compose build --no-cache` (one-time clean build) → `docker compose up` (no `--build`; proves cached image boots) → observe logs show NO `npm install` lines. Optional: report image size delta vs pre-B4 (expected: slightly larger due to baked-in `node_modules`, trade for offline-safe boot). **Confirm**: the build image is named `axoview-app` (B6 effect) and container starts cleanly without network access during startup.
2. **Image-name verification (B6)** — after `docker compose build --no-cache`, `docker images | grep axoview` should show `axoview-app`, not `fossflow-axoview`. Old `fossflow-*` images can be `docker image rm`'d for cleanup (user decision; non-blocking).
3. **CI gates for Track 2 PR** — Run Tests (now Node 22/24 only — verify the matrix shrink takes effect) + E2E Tests + CodeQL all need to green on the integration → master PR. `release.yml` change is post-merge (only fires on `workflow_run` for the Run Tests workflow on master); validate by observing the post-merge auto-release run succeeds without `NPM_TOKEN`.
4. **B5 queued for Track 3** — Dockerfile `ENABLE_SERVER_STORAGE=true` default remains as-is; correction lands in `technical-review-2026-05.md` §12 appendix per DP2-B5(b). Track 3's commit message expanded to include B5 in the cover list.
5. **`compose.dev.yml` HTTP_AUTH residue** — pre-existing dead env vars survived M4 cleanup (which only touched `compose.yml`). Not in B6 scope; one-line follow-up candidate for Track 3 or a tidy-up commit.

### Track 3 findings
> *To be filled as Track 3 commit lands.*

### Track 4 findings
> *User-action confirmations; checkbox marks above suffice.*

---

## Wrap-up

When every Track 0-4 checkbox is `[x]` + Tracks 5/6/7 are either spawned (own tacticals) or explicitly deferred:

1. Confirm CI all green on master + Cloudflare deploy live.
2. Add a single line under `PLAN.md` Phase 2D section:
   ```
   - v1.1 tech-debt cleanup complete <DATE> — see commit history + docs/technical-review-2026-05.md §12 corrections appendix. Sibling tacticals e2e-suite-rewrite.md + git-automation-hardening.md retired per convention.
   ```
3. Delete this tactical doc. Track 5/6/7 spawned tacticals (if any) are their own lifecycle.
4. Refresh memory: drop the v1.1-tech-debt active pointer; add corrections-appendix reference to the productization snapshot pointer if still helpful.

---

## Notes for Claude

- **One PR per track.** Track 0 = one PR with multi-commit clusters. Track 1 = one PR. Track 2 = one PR with sequenced commits per decision. Track 3 = one doc commit. Don't bundle across tracks; the clarity-per-revert is the point.
- **Decision Points must be answered first.** Track 0 needs DP1 (pl-PL); Track 2 needs DP2 (4 sub-decisions). Don't start a track without its blocking answer.
- **Verify-then-act** is the discipline for every dead-code deletion. Even "locked for deletion" items get a grep-and-read pass before the rm. False positives are cheaper to skip than false negatives are to revert.
- **CI gates already exist.** ESLint + commitlint + build-output verification + Worker bundle-size + knip soft-fail + coverage gate are all live. Don't disable; if a deletion triggers a CI failure, that's signal (e.g., knip surfacing a downstream consumer the grep missed).
- **B-9b stays deferred** per Locked Decision #5. If a Track 0 deletion happens to remove a silent-failure path, that's a bonus, not a sweep target.
- **Track 7 items don't get touched here.** Each is a future tactical when user authorizes. Capturing them in this doc is the contract.
- **Don't auto-bump the artifact's last-updated date** when shipping Track 3's appendix — the freeze convention is meaningful. Only the §12 appendix and the status header get edited; the Sessions A/B/C content stays as-shipped.
- **Post-squash-merge sync is SOP.** Track 0 shipped 2026-05-23 as PR #3 squash `6d33f1b` to master + `ed555ad` auto-release. Track 1 PR follows the same pattern: after the squash-merge to master, run `git checkout integration && git fetch origin && git reset --hard origin/master && git push --force-with-lease origin integration`. This keeps integration as a clean fast-forward base for the next PR and avoids the modify/delete conflict dance. Bake into routine.
- **PowerShell encoding hazard.** `Set-Content -Encoding utf8` in PowerShell 5.1 writes a UTF-8 BOM and mojibakeifies non-ASCII characters. **Never use it on i18n / Unicode-containing files.** Use Python (`open(f, 'wb')` with manual BOM detection) or per-file `Edit` calls. The i18n commit on 2026-05-23 caught this via a nonsense `git show --stat` insertions/deletions ratio; reverted via `git reset --hard HEAD~1` and redone cleanly.
- **Track 0 post-merge observation (semantic-release auto-bump).** PR #3 merge fired semantic-release auto-bump to v2.0.0 (commit `ed555ad`) — the MainMenu lib-API deletion carried a `BREAKING CHANGE` footer. No external impact (lib is monorepo-only per Locked Decision #11), but this is a precedent: any future Track 0/1 commit touching the lib's public surface with `BREAKING CHANGE` will trigger a major bump. Flag in PR description if intentional, or fold into Track 7 lib-public-API-narrowing scope if not.
