# v1.1 Sonar-driven refactor priorities — tactical

**Status:** Scaffolded 2026-05-25. **DPs locked 2026-06-09; M1 unblocked.** M0 bookkeeping commit landed: DPs locked (row #7), Tier 1 inventory re-verified against worktree Sonar CSV, worktree Sonar files consumed + deleted per DP5.
**Authorized:** 2026-05-25 (PLAN.md "Catalogued next-workstreams"; supersedes Track 6 of retired `v1.1-tech-debt.md`).
**Spawns from:** PLAN.md "Catalogued next-workstreams" → "v1.1 Sonar-driven refactor priorities" entry (line ~913).
**Source data:** Local Sonar scan 2026-05-26 (499 issues triaged; ~40 actionable). Inventory files (`sonar-issues.csv`, `sonar-issues.json`, `sonar-project.properties`) currently untracked in worktree at repo root — consume + delete per DP5.

---

## Read first (in order)

1. [docs/workflow.md](../workflow.md) — canonical session cadence + skill decision table.
2. This file — locked decisions, scope, milestones, sub-tasks per batch.
3. [PLAN.md](../../PLAN.md) — Phase Status Dashboard + catalogued workstream entry for context; **do not edit until wrap**.
4. [docs/adr/0006-canvas-selection-contract.md](../adr/0006-canvas-selection-contract.md) — canvas selection invariants. Touched by Cursor / Connector / DragItems / usePanHandlers / useInteractionManager refactors. Behavior MUST be preserved exactly.
5. [docs/adr/0005-toolbar-and-dock-layout-contract.md](../adr/0005-toolbar-and-dock-layout-contract.md) — toolbar / dock layout. Touched by LayersPanel / NodeActionBar / NodePanel refactors.
6. [docs/adr/0011-error-ux-contract.md](../adr/0011-error-ux-contract.md) — failure-of-intent contract. Relevant when Tier 2 S2486 empty-catch sites are in scope (they often hide failure-of-intent).
7. [docs/adr/0010-session-backend-contract.md](../adr/0010-session-backend-contract.md) — backend contract. Touched by `axoview-backend/src/routes.js:251` refactor.
8. [docs/testing.md](../testing.md) — regression suite (~1029 lib + 86 app + e2e). Refactors are guarded by tests — do not skip the green-CI gate.
9. Sonar inventory (worktree, untracked) — `sonar-issues.csv`, `sonar-issues.json`, `sonar-project.properties`. **Consume + delete per DP5.** Do NOT commit.
10. The 26 source files listed in the "Tier 1 — full S3776 inventory" table below — read only the file(s) for the current batch.

---

## Locked decisions

| # | Decision | Rationale |
|---|---|---|
| 1 | **Each refactor commit MUST preserve behavior exactly.** Tests (lib 1029, app 86, e2e suite) are the safety net. If a refactor surfaces an apparent bug, file as a finding in the register below — do NOT fix during the refactor commit. | Refactoring + bugfix in the same commit makes the diff unreviewable and breaks the bisect surface. |
| 2 | **Tier 1 (S3776) scope = the 31 S3776 hits enumerated below.** The triage summary said "~40" because it bundled S3776 (31) with S2004 (9, function-nesting > 4). S2004 hits overlap heavily with S3776; they will be addressed opportunistically while the S3776 function they live inside is refactored, NOT as standalone work. | Source: Sonar CSV inventory 2026-05-26; eliminates the "~40" double-counting ambiguity in the catalogue entry. |
| 3 | **Two real bugs from the Sonar scan are NOT in this tactical's scope.** (a) `axoview-e2e/pom/CanvasPOM.ts:88` async-Promise-executor — already fixed in PR #9. (b) `axoview-app/.../FileExplorer.tsx:63` a11y keyboard listener — in scope under Track 7 M8a. (c) `axoview-e2e/fixtures/app.fixture.ts:42` `use()` pattern-match false positive — disregarded per catalogue entry. | Per PLAN.md catalogue entry. |
| 4 | **Branch state on entry to first execution session is `integration` = `origin/master` at `257b85d`.** Three untracked Sonar files (`sonar-issues.csv`, `sonar-issues.json`, `sonar-project.properties`) are present in worktree — they are this initiative's source material; consume + delete per DP5 in the M0 commit. | Inherited working-tree state from PLAN.md Sonar catalogue. |
| 5 | **No tech-debt-for-future-features clause.** If a refactor surfaces an obvious decomposition opportunity that would benefit a deferred feature (e.g., GDrive provider extraction from `LocalStorageProvider`), STOP at the S3776-elimination boundary and file as a finding. Future features get their own ADR + tactical when wanted. | Per orchestrator methodology "no tech debt for future features". |
| 6 | **CI must be green at the end of every batch PR.** If a refactor breaks tests, the fix is to revert the refactor or fix the test — never silence/skip the test. | Refactoring-with-broken-tests is the canonical Hyrum's-Law trap. |
| 7 | **DP1–DP5 locked 2026-06-09 (M0 re-consultation), all = orchestrator's recommended defaults.** DP1=(b) Tier 1 + Tier 2 bugs (the 18-site empty-catch / dead-store / array-index-key surface); DP2=(b) one PR per file (multi-S3776 files batched into a single PR); DP3=(b) smallest-first warm-up, finish at the cx-131 capstone; DP4=(b) Sonar-profile noise reduction out of scope (user clones the profile in the SonarQube admin UI); DP5=(a) consume + delete the worktree Sonar files in M0. | DP1: Tier-2-bug surface is small, mechanical, and holds real defects; full style sweep deferred to its own tactical. DP2: file-scoped PRs are the cleanest review + bisect unit. DP3: build refactor discipline on cheap leaves before the hardest function. DP4: the profile lives on the SonarQube server, not in-repo. DP5: the Tier tables are the durable record; CSV/JSON snapshots drift the moment Sonar re-runs. |

(append-only; supersession is a new row that cites the old one)

---

## Decision Points — locked

DP1–DP5 locked 2026-06-09 — see Locked Decisions row #7. The orchestrator's recommended defaults were accepted unchanged (DP1=b, DP2=b, DP3=b, DP4=b, DP5=a). The original question/recommendation text is preserved in this file's git history (pre-`chore(tactical): v1.1-sonar-refactor DPs locked` commit) for traceability.

---

## Scope

In scope (assuming recommended DP1=b, DP2=b, DP3=b, DP4=b, DP5=a — adjust if user overrides):

- **Tier 1 — S3776 cognitive-complexity refactors.** 31 functions across 26 files. Each refactor: decompose to ≤ 15 cognitive complexity; preserve behavior; tests must stay green.
- **Tier 2 (bugs subset) — S2486 + S1854 + S6479.** 18 sites across ~14 files. Each fix: investigate (an empty catch may hide a real error path; a dead store may signal an abandoned branch); fix or document; tests must stay green.
- **M0 inventory commit.** Consume the worktree Sonar files; delete them; this tactical IS the durable record.

Out of scope:

- Tier 2 style work (S1874 deprecated APIs, S3358 nested ternaries, S6759 props readonly, S7924 a11y contrast). Defer to a follow-up tactical only if the user explicitly wants it.
- Tier 3 (~330 S77xx modernization nags). Handled out-of-band in SonarQube profile config per DP4.
- The two real bugs already routed elsewhere (CanvasPOM PR #9; FileExplorer Track 7 M8a). See LD #3.
- Any product-feature work (deferred per PLAN.md "Deferred features" register).
- Re-architecture of high-LOC files beyond the S3776-elimination boundary (LD #5).

---

## Milestones

The 8-milestone path below reflects DP1=b + DP3=b (smallest-first warm-up, finish at `useInteractionManager.ts`). If the user overrides DP3, regenerate the M2–M6 sequencing accordingly.

| M | Name | Batches | Files | Functions | Gate |
|---|---|---|---|---|---|
| M0 | DP1–DP5 answered; tactical updated with locked answers; inventory commit consumes + deletes worktree Sonar files. | 1 commit | (none — deletes 3 files) | — | User responds in re-consultation; tactical updated; commit lands on `integration`. |
| M1 | Tier 2 bugs sweep — investigate + fix the 18 empty-catch / dead-store / array-index-key sites. **Front-loaded** because these are bugs (LD #3 risk class) and unblock the otherwise-mechanical work. | 1 PR | 14 files (see Tier 2 table) | 18 sites | PR opens; CI green; findings register documents any investigation that found a real bug. |
| M2 | Tier 1 warm-up batch — single-S3776 low-complexity files (cx 16–19). | 4–6 PRs (one per file) | `Pan.ts`, `LayerRow.tsx`, `DragItems.ts`, `NodePanel.tsx`, `isoMath.ts`, `svgOptimizer.ts`, `ConnectorLabel.tsx`, `useCopyPaste.ts`, `auth.ts`, `App.tsx`, `routes.js`, `projectZip.ts`, `ConnectorAnchorOverlay.tsx` (the 13 single-hit files with cx ≤ 22). | 13 functions | Each PR: CI green; behavior preserved per smoke checklist below. |
| M3 | Tier 1 mid-complexity multi-S3776 files — pair 1. | 2 PRs (one per file) | `usePanHandlers.ts` (2 fns, cx 17+18), `LayersPanel.tsx` (2 fns, cx 20+23). | 4 functions | Each PR: CI green; behavior preserved. |
| M4 | Tier 1 mid-complexity multi-S3776 files — pair 2. | 2 PRs | `ExportImageDialog.tsx` (2 fns, cx 23+28), `DiagramLifecycleProvider.tsx` (2 fns, cx 18+32). | 4 functions | Each PR: CI green; behavior preserved; manual smoke (DiagramLifecycleProvider touches save/load lifecycle). |
| M5 | Tier 1 high-complexity single-S3776 files. | 3 PRs | `findNearestUnoccupiedTile.ts` (cx 32), `NodeActionBar.tsx` (cx 31), `Connector.ts` (cx 33), `iconPackManager.ts` (cx 32), `LocalStorageProvider.ts` (cx 23 — see re-verification note; mid- not high-complexity now). | 5 functions | Each PR: CI green; behavior preserved; manual smoke. |
| M6 | Tier 1 highest-complexity multi-S3776 file. | 1 PR | `Cursor.ts` (3 fns, cx 22+62+32) + `DiagnosticsOverlay.tsx` (2 fns, cx 22+53). May split into 2 PRs if review surface too large. | 5 functions | Each PR: CI green; behavior preserved; manual smoke (Cursor.ts is canvas-selection-hot per ADR 0006). |
| M7 | Tier 1 capstone — `useInteractionManager.ts` (single function, cognitive complexity **131**, 2h1min effort). Likely a 1-PR session entirely on this one function. | 1 PR | `useInteractionManager.ts` | 1 function | PR: CI green; behavior preserved; manual smoke (every canvas interaction path); orchestrator re-consult before merge. |
| M8 | Wrap — append PLAN.md one-line entry under the catalogued workstream; retire this tactical; durable record lives in PR commit history + Tier tables in this doc's git history. | 1 commit | (deletes this file) | — | User authorizes wrap. |

---

## Tier 1 — full S3776 inventory (32 functions, 25 files)

Sorted by milestone bucket (DP3=b warm-up order). Each row: file:line, complexity score, effort estimate (Sonar's), milestone bucket. Line numbers re-verified 2026-06-09 against the worktree CSV + current `integration` source (see re-verification note below the table).

| # | File:Line | Complexity | Effort | M-bucket |
|---|---|---:|---|:---:|
| 1 | `packages/axoview-lib/src/interaction/modes/Pan.ts:41` | 16 | 6min | M2 |
| 2 | `packages/axoview-lib/src/components/LayersPanel/LayerRow.tsx:46` | 16 | 6min | M2 |
| 3 | `packages/axoview-lib/src/interaction/modes/DragItems.ts:83` | 17 | 7min | M2 |
| 4 | `packages/axoview-lib/src/components/ItemControls/NodeControls/NodePanel/NodePanel.tsx:55` | 18 | 8min | M2 |
| 5 | `packages/axoview-lib/src/utils/isoMath.ts:335` | 19 | 9min | M2 |
| 6 | `packages/axoview-lib/src/utils/svgOptimizer.ts:301` | 19 | 9min | M2 |
| 7 | `packages/axoview-lib/src/components/SceneLayers/ConnectorLabels/ConnectorLabel.tsx:130` | 19 | 9min | M2 |
| 8 | `packages/axoview-lib/src/clipboard/useCopyPaste.ts:29` | 18 | 8min | M2 |
| 9 | `packages/axoview-worker/src/auth.ts:42` | 18 | 8min | M2 |
| 10 | `packages/axoview-app/src/App.tsx:195` | 20 | 10min | M2 |
| 11 | `packages/axoview-backend/src/routes.js:251` | 21 | 11min | M2 |
| 12 | `packages/axoview-app/src/services/project/projectZip.ts:178` | 19 | 9min | M2 |
| 13 | `packages/axoview-lib/src/components/ConnectorAnchorOverlay/ConnectorAnchorOverlay.tsx:64` | 22 | 12min | M2 |
| 14 | `packages/axoview-lib/src/interaction/usePanHandlers.ts:124` | 17 | 7min | M3 |
| 15 | `packages/axoview-lib/src/interaction/usePanHandlers.ts:203` | 18 | 8min | M3 |
| 16 | `packages/axoview-lib/src/components/LayersPanel/LayersPanel.tsx:32` | 20 | 10min | M3 |
| 17 | `packages/axoview-lib/src/components/LayersPanel/LayersPanel.tsx:234` | 23 | 13min | M3 |
| 18 | `packages/axoview-lib/src/components/ExportImageDialog/ExportImageDialog.tsx:56` | 23 | 13min | M4 |
| 19 | `packages/axoview-lib/src/components/ExportImageDialog/ExportImageDialog.tsx:299` | 28 | 18min | M4 |
| 20 | `packages/axoview-app/src/providers/DiagramLifecycleProvider.tsx:630` | 18 | 8min | M4 |
| 21 | `packages/axoview-app/src/providers/DiagramLifecycleProvider.tsx:1149` | 32 | 22min | M4 |
| 22 | `packages/axoview-lib/src/utils/findNearestUnoccupiedTile.ts:67` | 32 | 22min | M5 |
| 23 | `packages/axoview-lib/src/components/NodeActionBar/NodeActionBar.tsx:55` | 31 | 21min | M5 |
| 24 | `packages/axoview-lib/src/interaction/modes/Connector.ts:67` | 33 | 23min | M5 |
| 25 | `packages/axoview-app/src/services/iconPackManager.ts:239` | 32 | 22min | M5 |
| 26 | `packages/axoview-app/src/services/storage/providers/LocalStorageProvider.ts:21` | 23 | 13min | M5 |
| 27 | `packages/axoview-lib/src/interaction/modes/Cursor.ts:113` | 22 | 12min | M6 |
| 28 | `packages/axoview-lib/src/interaction/modes/Cursor.ts:250` | 62 | 52min | M6 |
| 29 | `packages/axoview-lib/src/interaction/modes/Cursor.ts:384` | 32 | 22min | M6 |
| 30 | `packages/axoview-app/src/components/DiagnosticsOverlay.tsx:220` | 22 | 12min | M6 |
| 31 | `packages/axoview-app/src/components/DiagnosticsOverlay.tsx:297` | 53 | 43min | M6 |
| 32 | `packages/axoview-lib/src/interaction/useInteractionManager.ts:112` | **131** | 2h1min | **M7** |

**Inventory re-verification (2026-06-09, M0).** Checked row-by-row against the worktree Sonar CSV (`sonar-issues.csv`, 2026-05-26 scan) and current `integration` source:

- **32 unique S3776 functions across 25 files.** The CSV reports 33 raw S3776 rows, but one is a duplicate `iconPackManager.ts` entry (same cx 32, empty line field) — there are **no new functions** vs. the original scaffold. LD #2's "31" and the original table header's "31 functions, 26 files" were undercounts; corrected to 32 / 25 here.
- **All 32 rows resolve to a real function in current source.** No row points at a deleted/missing function.
- **8 rows had line-number drift** (edits between the scaffold's line capture and the 2026-05-26 CSV / current HEAD); patched: auth.ts 28→42, App.tsx 194→195 (cx 18→20, effort 8→10min), DiagramLifecycleProvider 610→630 & 1127→1149, iconPackManager 233→239, LocalStorageProvider 18→21 (cx 34→23, effort 24→13min), DiagnosticsOverlay 195→220 & 272→297.
- **S2004 (LD #2 opportunistic set): 9 unique functions across 8 files.** The CSV reports 10 raw rows, one a duplicate `useInitialDataManager.ts` entry. LD #2's "9" is correct.
- **Flag:** `LocalStorageProvider.ts` dropped to cx 23 (mid- not high-complexity); left at M5 pending re-sequencing when the M5 prompt is drafted.

---

## Tier 2 (bugs subset) — 18 sites

| Rule | File:Line | Severity | Class |
|---|---|---|---|
| S2486 | `packages/axoview-app/src/providers/DiagramLifecycleProvider.tsx:431` | MINOR | empty catch |
| S2486 | `packages/axoview-app/src/providers/DiagramLifecycleProvider.tsx:486` | MINOR | empty catch |
| S2486 | `packages/axoview-app/src/services/project/projectZip.ts:182` | MINOR | empty catch |
| S2486 | `packages/axoview-lib/src/components/ColorSelector/CustomColorInput.tsx:36` | MINOR | empty catch |
| S2486 | `packages/axoview-lib/src/schemas/validation.ts:99` | MINOR | empty catch |
| S2486 | `packages/axoview-lib/src/schemas/validation.ts:152` | MINOR | empty catch |
| S2486 | `packages/axoview-lib/src/schemas/validation.ts:202` | MINOR | empty catch |
| S2486 | `packages/axoview-lib/src/schemas/validation.ts:250` | MINOR | empty catch |
| S1854 | `packages/axoview-lib/src/components/SceneLayers/ConnectorLabels/ConnectorLabel.tsx:124` | MAJOR | dead store (`namePosition`) |
| S1854 | `packages/axoview-app/src/components/fileExplorer/ImportDialog.tsx:53` | MAJOR | dead store (`zipFilename`) |
| S1854 | `packages/axoview-lib/src/hooks/useSceneActions.ts:612` | MAJOR | dead store (`currentView`) |
| S1854 | `packages/axoview-lib/src/components/ExportImageDialog/ExportImageDialog.tsx:69` | MAJOR | dead store (`uiStateActions`) |
| S1854 | `packages/axoview-lib/src/components/ExportImageDialog/ExportImageDialog.tsx:301` | MAJOR | dead store (`scaleX`) |
| S1854 | `packages/axoview-lib/src/components/ExportImageDialog/ExportImageDialog.tsx:302` | MAJOR | dead store (`scaleY`) |
| S6479 | `packages/axoview-lib/src/components/ContextMenu/ContextMenu.tsx:31` | MAJOR | array-index React key |
| S6479 | `packages/axoview-app/src/components/DiagnosticsOverlay.tsx:543` | MAJOR | array-index React key |
| S6479 | `packages/axoview-lib/src/components/HelpDialog/HelpDialog.tsx:228` | MAJOR | array-index React key |
| S6479 | `packages/axoview-lib/src/components/HelpDialog/HelpDialog.tsx:275` | MAJOR | array-index React key |

---

## Per-batch sub-task template (applied per PR)

For each PR (M2–M7), the executing agent follows this loop:

1. **Pre-edit investigation.** Read the target function in full. Identify the cognitive-complexity hot spots (nested branches, repeated conditional blocks, long lambda chains). Sketch the decomposition (extract-function / early-return / strategy-table / state-machine collapse) before editing.
2. **Locate the test coverage.** Identify which unit / contract / e2e tests exercise this function. If coverage gap exists, FILE AS FINDING and proceed — but flag in PR description as a future test row.
3. **Refactor in N micro-commits.** Each micro-commit: one extraction or one early-return restructure. Run `yarn build` + the relevant test suite after each micro-commit. Conventional-commits subject: `refactor(<scope>): <what>` (NOT `fix:` — behavior is preserved).
4. **Verify Sonar score.** After the final micro-commit, re-run `npx sonar-scanner` (or document that the local-Sonar re-run is deferred to next user inventory pass). The refactored function MUST drop below 15 cognitive complexity.
5. **Manual smoke (per file's interaction surface).** Use the per-file smoke checklist below.
6. **Open PR.** PR description: links Tier 1 row number(s); cites the cognitive-complexity before/after; lists smoke steps performed; flags any findings.
7. **Pause for user review.** User merges; orchestrator re-consults for next batch.

### Per-file smoke checklist (manual; pick the rows that apply to the PR's file)

| Surface | Smoke step |
|---|---|
| `interaction/modes/*.ts` (Pan, DragItems, Cursor, Connector) | Server-mode: select node + drag + drop; pan canvas; lasso-select; right-click context menu; connector draw + drag-waypoint. ADR 0006 selection invariants must hold. |
| `interaction/useInteractionManager.ts` | All of the above; plus mode switch (select → pan → lasso → back); plus undo/redo across modes. |
| `interaction/usePanHandlers.ts` | Trackpad pan; mouse-middle pan; touch pan (if device); momentum scroll. |
| `components/LayersPanel/*` | Toggle layer visibility; reorder layer; rename layer (F2); add/remove layer. |
| `components/ExportImageDialog/*` | PNG export; SVG export; JPG export; custom-resolution mode; transparent-bg toggle. |
| `components/ConnectorAnchorOverlay/*` | Hover-anchor on connector; drag anchor; release anchor; connector remains valid. |
| `components/NodeActionBar/*` | Hover a node; action-bar appears; all action buttons functional. |
| `components/ItemControls/NodeControls/NodePanel/NodePanel.tsx` | Right-sidebar opens on node click; all property fields editable; undo restores. |
| `components/SceneLayers/ConnectorLabels/ConnectorLabel.tsx` | Connector with name visible; F2 rename; tooltip; positioning. |
| `components/DiagnosticsOverlay.tsx` | Open via Settings → Diagnostics; toggle overlay; model dump; session dump. |
| `utils/isoMath.ts`, `utils/svgOptimizer.ts`, `utils/findNearestUnoccupiedTile.ts` | Pure utilities — unit tests cover; manual smoke = open a diagram, drag a node, verify no visual regression. |
| `clipboard/useCopyPaste.ts` | Copy node; paste in same diagram; cut+paste; paste from another browser tab. |
| `App.tsx` | Server-mode boot; session-mode boot; read-only URL boot; refresh page. |
| `providers/DiagramLifecycleProvider.tsx` | Create blank diagram from explorer; create from canvas card; save; load; delete; unsaved-changes guard (session mode). |
| `services/iconPackManager.ts` | Material icons load in ElementsPanel; AWS pack loads; toggle pack via Settings. |
| `services/storage/providers/LocalStorageProvider.ts` | Server-mode save; session-mode fallback when server unreachable; folder CRUD; tree manifest persistence. |
| `services/project/projectZip.ts` | Export project ZIP; reopen the ZIP; round-trip equality. |
| `axoview-backend/src/routes.js` | `GET /api/diagrams`; `POST /api/diagrams`; `DELETE /api/diagrams/:id`; folder endpoints. |
| `axoview-worker/src/auth.ts` | `AUTH_MODE=none` + `shared-token` + `cf-access` all green per existing suite. |

---

## Findings register (populated as batches ship)

| # | M-bucket | File:Line | Finding | Status |
|---|---|---|---|---|
| 1 | M1 | `providers/DiagramLifecycleProvider.tsx` (Sonar 431, 486) | **Both S2486 sites already remediated** by the prior ADR 0011 retrofit. The 2026-05-26 scan's empty catches are now the public-share / readonly load handlers at the drifted lines 449 (`setPublicShareLoadFailed(true)`) and 506 (`setReadonlyLoadFailed(true)`) — non-empty, contract-conformant. No edit needed. | Resolved (pre-existing) |
| 2 | M1 | All 6 S1854 sites | **No dead store masked a real bug.** Each was a benign leftover: `ConnectorLabel.namePosition` (render computes position inline), `ImportDialog.zipFilename` (redundant — zip name already pre-fills `newFolderName`), `useSceneActions.currentView` (`deleteView` keys off `currentViewId`), `ExportImageDialog.uiStateActions` (never read), `ExportImageDialog.scaleX/scaleY` at the crop-overlay draw (overlay draws in canvas space; the real export crop has its own scale pair at lines 178–179). All removed; behavior preserved. | Resolved |
| 3 | M1 | 6 active S2486 sites (validation ×4, projectZip, CustomColorInput) | **No empty catch hid a failure-of-intent.** All are intentional: `getItemByIdOrThrow` used as an existence probe → translated to a structured validation Issue (×4); JSZip parse failure → domain `ProjectZipError`; EyeDropper user-cancel (ADR 0011 carve-out, not failure-of-intent). Each unused binding dropped to optional-`catch` + one-line justification comment. | Resolved |
| 4 | M1 | 4 S6479 sites | **None reorderable with stateful rows** → all cosmetic. `recentEvents` (DiagnosticsOverlay) is a dynamic sliding log but its rows are stateless text; `ContextMenu`/`HelpDialog ×2` are static lists. Fixed with content/id keys (`item.label`, `shortcut.action`, `interaction.action`, event-tuple hash). | Resolved |

Findings classes:
- **Real bug found during refactor** → file row; do NOT fix in same commit; spawn follow-up commit OR open separate PR depending on user direction at re-consult.
- **Test coverage gap** → file row; flag in PR description; refactor proceeds at agent's risk; user decides whether to add test before or after merge.
- **Decomposition opportunity beyond S3776 scope** → file row; STOP at the S3776-elimination boundary per LD #5; surface as catalogued workstream candidate.

---

## Wrap-up (M8 only)

When all M0–M7 batches have shipped + CI is green + user authorizes wrap:

1. Append one-line entry under PLAN.md "Catalogued next-workstreams" → "v1.1 Sonar-driven refactor priorities" replacing the catalogue entry with a shipped-status line that points to the PR-set + this tactical's deletion commit.
2. Delete this tactical doc (`docs/tactical/v1.1-sonar-refactor.md`).
3. Confirm `.gitignore` has Sonar entries (added in M0 if user re-runs locally) OR confirm no Sonar files remain in worktree.
4. If the user wants to follow up with Tier 2 style work (per DP1 override), spawn a NEW tactical — do not extend this one post-wrap.

---

## Notes for Claude

- **Read only the file(s) for the current batch.** Do not pre-read all 26 source files; the cognitive load defeats the point of file-scoped batching.
- **`useInteractionManager.ts` (M7) is the hardest function in the repo (cx 131).** Re-consult orchestrator before opening the M7 PR if you sense the decomposition surface is unclear; do NOT push a partial refactor that only drops to ≤ 30. Either land below 15 or open a finding documenting why the residual is unavoidable.
- **Behavior preservation is non-negotiable per LD #1.** If a test fails after a refactor commit, revert the commit; do not change the test to match the refactor's new behavior unless the test was demonstrably wrong (find user authorization first).
- **`yarn build` is the cheapest gate.** Run after every micro-commit. The lib must build before app builds; remember the dev-server lib-rebuild friction per memory `project_dev_lib_rebuild.md`.
- **Stay-in-workdir.** No scratch files outside the repo. Use `./.scratch/` if needed (add to `.gitignore` if not present); heredocs preferred for one-off computation.
- **Conventional-commits subject** for each commit: `refactor(<scope>): <what>` for Tier 1; `fix(<scope>): <what>` for Tier 2 bug fixes; `chore(tactical): <what>` for tactical doc updates.
- **`sonar-issues.csv` is gone after M0.** If you need to re-derive the inventory, read this tactical's Tier tables — they are the durable record.
