1. ✅ DONE (Bundle A, 2026-05-15) — currently the context menu when I left click on the node connection or rectangle, we need to change this behavior so that the menu appears when i right click the node. 
2. ✅ DONE (Bundle A, 2026-05-15) — Lock layer functionality does not work.
3. ✅ DONE (Bundle A, 2026-05-15) — F2 rename on for a layer name does not work, double click works.
4. ✅ DONE (Bundle A, 2026-05-15) — Once a layer is assigned there is no way to unassign the element from a layer. I think we just need to continue you showing the unassined section in leayers even when no elements are there, that way the user can drag the element back to unassined.
5. ✅ DONE (Bundle B, 2026-05-15) — Redo doesn't work for the last action. in example i draw a connection A, then connection B. the presss undo, undo, redo, redo the connection B does not appear.
6. ✅ DONE (Bundle A, 2026-05-15) — Change font size to 0.75em for this "used space" control, it is an exception case for our ux guidelines: <span class="MuiTypography-root MuiTypography-micro css-54ro1u">&lt;1% · 9.3 KB</span>
7. ✅ DONE (Bundle B, 2026-05-16) — Three-stage fix for FPS drop dragging 6+ elements: `bba712c` (DragItems drag transaction), `728b229` (Node shell + memoized NodeContent, useSceneActions memo isolation), `7e09fba` (Path 4-true CSS-only drag preview, model commit deferred to mouseup), plus waypoint preview fix in `f66d7be`. Sub-13fps cliff eliminated entirely; sustained 24–44 fps during multi-element drag. See [docs/perf-troubleshooting.md](../perf-troubleshooting.md) and [docs/architecture.md §1 Drag Items](../architecture.md#1-feature-inventory).
8. There is no way to sequentioally select multiple items on the canvas. CTRL + Select. The node edit panel should auto hide when more than 1 node is selected
9. Introduce CTRL + A to select all elements on the canvas. The node edit panel should auto hide when more than 1 node is selected
10. ✅ DONE (2026-05-19) — Incremental loads (pack toggle, diagram swap) auto-expand new categories *and* run a 1.6s soft pulse on the header. First-paint behavior preserved (large packs stay collapsed). See [docs/tactical/mqa-design-shakeout.md#10](mqa-design-shakeout.md) for the outcome.
11. ✅ DONE (2026-05-19) — Canvas rich-text typography polish + 2D-Y rotation. Root cause of "everything bold" was a hardcoded `TEXTBOX_FONT_WEIGHT='bold'` wrapper default; flipped to `'normal'`. Added major-third typography scale (h1 1.875em / h2 1.5 / h3 1.25) shared between visual styles and dimension math via `CANVAS_RICHTEXT_SCALE`. Body line-height 1.5, slider floor lowered to 0.15. Bonus: 2D-mode Y-orientation now rotates text 90° to match the dashed selection box (was rendering horizontally while bounds extended vertically). Pinned by `isoMath.richtext.test.ts` + `useIsoProjection.twoDY.test.tsx`. See [docs/tactical/mqa-design-shakeout.md#11](mqa-design-shakeout.md).
12. ✅ DONE (Bundle B, 2026-05-15) — In rich edit boxes when i start typing a bullet point like 1. <space> the auto matic edit erases the 1. <space>, this does no happen when i have "My Word" and start typing 1. <space> in front of "My Word", it correctly identifes it as a bullet point. 
13. ✅ DONE (Bundle B, 2026-05-15) — When a node is selected on a canvas, the user selects a diagram in the folder explorer, then click f2 to rename -> inline edit appears, the user is able to edit, but the edit immediatly disapears so the user cannot edit anything. Feels like the autosave or some other form of refresh is interfering. This only happend when a node is selected, when a node is NOT selected f2 rename in the file explorer works as expected
14. ✅ DONE (Bundle B, 2026-05-15) — The rename of diagrams sometimes does not propogate. i.e. i create 5 diagrams. Then f2 rename the diagrams from Folder explorer, export the project, import back to a clean session. not all diagrams have the name change. Seems like the rename only propogrates after the user explicitly opens the diagram that was renamed after renaming.
15. ✅ DONE (Bundle A, 2026-05-15) — would be good that F2/rename also works for layers.
16. ✅ DONE (Bundle B, 2026-05-15) — when a user opens the node edit and uses the mouse to select text in the input box or similar the user left clicks and drag selects the name -> if the users goes over the edge of the node Edit deck -> the deck closes automatically, if the user left click and drag selects the text and let's go within the edit deck -> the deck stays in place and user sucesfully selected everything they wanted. This is especially a friction point when a user works fast and opens the edit -> uses the mouse to select the name and accidentally drags the mouse over the edge of the deck while selecting. Happens very often.
17. ✅ DONE (Bundle B, 2026-05-15) — Export compact does not export all the necessary metadata for a diagram i.e. Rectangle, Text, conneciton labels, name, connection color, style, notes, etc. are not exported. Export JOSN seems to work fine. Bringing to a criticla quesiton... why do we even need compact json export -> I think we need to remove it all together from code.
18. ✅ DONE (Bundle B, 2026-05-15) — when a user deleted a diagram and confirms it -> the canvas still shows the opened "deleted" diagram when autosave triggeres it is created again. We need to reset the canvas to the first load screen with  "create new / Import" buttons.
19. revise the Settings, controls, shortcuts. Revise what makes and doesn't make sense. Also in example Element -> shortcut is "N" we need to show this when hovering over the elements icon "Element (n)" . canvas controls - > revise this, to me all these controls look excessive, but i don't know what is the industry standard for this.
20. We need to reimagine the setting dialog, it seems overly excessive, and hard to navigate. we need better simpler ux than tabular view. maybe even left tabs is a better design...
21. ✅ DONE (Bundle B, 2026-05-15) — When importing in docker mode and a project contain a folder-> throws an error saying failed to create a folder, user cannot create a folder via folder explorer as well. 
22. ✅ DONE (Bundle B follow-up, 2026-05-15) — Redesigned per #25: replaced the duplicate-tooltip + body-click navigation with a hover-revealed action chip above the node showing up to 3 affordances (external link / linked diagram / notes-popover) depending on availability. Body-click no longer navigates; bottom-right link badge is a passive indicator only. 
23. ✅ DONE (Bundle A, 2026-05-15) — Share link pop-over controls don't seem to follow UX guidelines in example the button text is all caps.
24. ✅ DONE (Bundle B, 2026-05-15) — Share link link points to a different port than expected in example the app is deployed on http://localhost:3000 but the link is to http://localhost:3001/display/p/kiDG45pBjPQQYRlRnkPJR suprisingly the "Preview" button sends the user to the correct link "http://localhost:3000/display/p/kiDG45pBjPQQYRlRnkPJR"
25. ✅ DONE (Bundle B follow-up, 2026-05-15) — Resolved jointly with #22. Hover chip surfaces external link, linked diagram, and notes-popover affordances. Body-click is intentionally inert; name-link click still navigates external links.
26. I wan to be able to remove imported icons. right now if i import a custom icon there is no way to remove it. At the same time we need guardrails that would say that if a icon that was imported is used in one of the diagrams we need to warn the user, what actually happens when a user removes an icon that is used, i assume it is just not rendered?
27. ✅ DONE (Bundle A, 2026-05-15) — Make it so that new file, new folder, import, export, refresh collapse controls in the folder explorer appear only when the user hovers over the Folder explorer dock. This is the same behaviour as in VS Code. *(Pattern extended to Layers panel actions in the same bundle.)*
28. ✅ DONE (Bundle A, 2026-05-15) — Where we have an option to import icons, we need to add some kind of icon with a tip indicating that a user can generate icons using a prompt: "Create an image of 'my object', use folowing parameters: Transparent background. True isometric projection, 30-degree isometric view (mathematical isometric, not 2:1 pixel-art). Orthographic camera at 45° azimuth and 35.264° elevation (arctan 1/√2). All three axes foreshortened equally; the two horizontal edges of any cube run at exactly +30° and -30° above horizontal on screen, and vertical edges stay perfectly vertical. Tile footprint width:height ratio is √3 : 1." I want to make sure people can copy paste it into something like chatgpt and generate the image. *(Reworded to "image-generating AI"; uses AutoAwesome sparkles affordance.)*

---

## Follow-ups landed in Bundle A but not originally listed

- **Page-tab cap at 5.** Interim guard for the >15-page case. Proper overflow/scroll UX deferred — tracked in [known_issues.md](../../known_issues.md). *(2026-05-15.)*